[
 {
  "clip_n": 3,
  "dominant_mechanism": "Download-queue starvation in crate_engine.py `_dl_priority` (defined at line 4449 inside `find_edit`). Its sort key puts `-edit_titled` at index 4, where `edit_titled = EDIT_WORDS.search(title) and not OTHER_RENDITION.search(title)`, so EVERY non-edit-titled upload sorts below EVERY edit-titled one, and `find_edit`'s `max_dl=14` (line 4103) truncates there. I re-ran the engine's own search for this clip: `build_queries` emits 14 queries including 'Omens' (index 0) and 'EsDeeKid Omens' (index 1), `search_edits(q, 8)` returns 460 rows, and EsDeeKid's own two uploads (youtube VdU_iaEoBKM, 3,717,230 plays; soundcloud.com/esdeekid/omens, 1,129,532 plays) sort to ranks 29 and 30 behind 28 edit-titled rows. They are never downloaded, in either wave: wave 2's head is the same 14-wide `_producer_quota(_web_quota(_sc_quota(cands, max_dl), max_dl), max_dl)` (line 4606), the creator lane is gated off by `_cre_settled` (line 4626) because wave 1 already hit CORE_SAME, and `known_dir` is None so the `extra_dir_dl` sweep never runs.\n\nThe clip is the plain master played straight - I measure core 1.000, speed 1.0000, bass_delta -0.65 dB against EsDeeKid's own upload - so every slowed row that DID get downloaded was tempo-wrong, and the only tempo-correct row inside the 14 was MetalDOCTOR's \"OMENS - Esdeekid | REMIX- BASS/DISTORTED\" at bass_delta -5.61 dB. In `rank_key` (line 5054) all seven survivors sit at core 1.000, so every tier down to `speed_exact` ties and `speed_exact` separates them completely (MetalDOCTOR bucket 0; the slowed rows buckets 3-10). That is why the LOWEST-scoring row in the pool (0.948, 825 plays) beat rows at 0.983 with 30,808 plays: `-fq` and plays sit below `speed_exact` and never voted.\n\nThe structural part, which is what makes this a defect rather than a tuning miss: the tier written for exactly this situation - `((1 if is_official_original(c) else 0) if _clip_is_edit else (0 if is_official_original(c) else 1))` at line 5217, with `_clip_is_edit` False because `edit_label == \"as posted\"` (line 5052) - is dead code here. `is_official_original` (line 4852) requires `not EDIT_WORDS.search(c[\"title\"])`, which is the SAME predicate `_dl_priority` uses to sink a row out of the download head. The two are inverses: the rows rank_key is built to prefer on an unedited clip are precisely the rows the download queue guarantees will not exist. The hard-rules doctrine \"only prefer the plain original when the clip is not an edit\" cannot fire because the plain original is structurally unreachable on any track with more than 14 edit-titled uploads.\n\nThe word \"bass\" reached the screen through the crowned candidate's TITLE, not through a measurement. The badge was correct (`speed: \"as posted\"` -> \"Original\"), the measured bass gate at server.py:1899 correctly did not fire, and `_BASS_CLAIM` (server.py:1171) does not match \"BASS/DISTORTED\" so no caveat printed either. But the caveat is cosmetic: with the master in the pool there is no bass-titled row to caveat.",
  "root_cause_tag": "dl_priority_starves_master",
  "evidence": "ALL NUMBERS BELOW ARE MINE, measured just now with E.get_source + E.dl_clip(seconds=20) + V.verify. No Shazam, no engine calls.\n\nClip: https://vt.tiktok.com/ZSVc6dkVh/  ->  clip _tilt_db 23.74, _tilt_slope 12.759\n\n  official_EsDeeKid  (youtube VdU_iaEoBKM, uploader EsDeeKid, 3.72M plays, ABSENT FROM POOL)\n    core=1.000 speed=1.0000 bass_delta=-0.65 slope_delta=+0.784 cand_tilt=24.39 score=1.0000\n  official_lyrics_VibesOnly (tuleuj8JOFI, independent second upload of the master)\n    core=1.000 speed=1.0000 bass_delta=-0.76 slope_delta=+1.395 cand_tilt=24.50 score=1.0000\n  crowned_MetalDOCTOR (kf7UGMjD4-s, \"OMENS - Esdeekid | REMIX- BASS/DISTORTED\", 825 plays)\n    core=1.000 speed=1.0000 bass_delta=-5.61 slope_delta=+3.464 cand_tilt=29.34 score=0.6845\n  slowedsongs (WRNklxJoqHs, pool row #2, 30,808 plays)\n    core=1.000 speed=1.0794 bass_delta=-1.02 slope_delta=+1.086 cand_tilt=24.76 score=0.9781\n\nThe owner's claim in one line: the clip is 0.65 dB from the untouched master and 5.61 dB from the upload the engine crowned. Two independent uploads of the master agree at 1.000/1.0000. \"Original song, NOT bass boosted\" is correct and measurable.\n\nPOOL RECONSTRUCTION (my own run, /tmp/gdoc/c03judge/):\n  E.get_source -> credit_title='\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b\u044c\u043d\u044b\u0439 \u0437\u0432\u0443\u043a' credit_author='93' handle='93'\n  E.build_queries(...) -> 14 queries, [0]='Omens', [1]='EsDeeKid Omens'\n  E.search_edits(queries, 8) -> 460 rows in 4.7s\n  key_terms={'omens'}; 164 rows title_hits>=1; 43 of those _artist_hit True;\n    28 of those edit_titled True; 15 edit_titled False; 2 with uploader EXACTLY == 'esdeekid'\n  Both of those 2 are non-edit-titled, i.e. both are `is_official_original`, i.e. both sink:\n    rank 29  youtube VdU_iaEoBKM        3,717,230 plays  \"EsDeeKid - Omens\"\n    rank 30  soundcloud/esdeekid/omens  1,129,532 plays  \"Omens\"\n  master's _dl_priority key = (-1, -1, 0, 0, 0, 0, 0, -3717230); 28 rows strictly ahead; max_dl=14.\n\n  The 14 actually downloaded (my faithful replay of _dl_priority + _sc_quota, max_dl=14):\n     1 sc  246824 T-trap M-musicz   Omens - EsDeeKid - slowed + reverb + bass\n     2 yt   91805 n o c t u r n e . EsDeeKid - Omens (SLOWED + REVERB)\n     3 yt   30808 slowed songs      esdeekid - omens (new song) [ slowed + reverb ]\n     4 sc   13049 R\u00e4pane T\u00fchisus    Esdeekid - Omens (slowed + reverb)\n     5 yt    9677 ProwlBoy          EsDeeKid - Omens (slowed + reverb)\n     6 yt    9495 fen1xxy           EsDeeKid - Omens slowed reverb by fen1xxy\n     7 sc    8764 M e t a c a r p i e  EsDeeKid - Omens (slowed + reverb)\n     8 yt    6869 R\u00e4pane T\u00fchisus    EsDeeKid - Omens (slowed + reverb)\n     9 yt    5652 Sulimxn           ESDEEKID - OMENS (SLOWED AND REVERB)\n    10 yt    3908 barcode group     EsDeeKid - Omens (slowed + reverb)\n    11 yt     825 MetalDOCTOR       OMENS - Esdeekid | REMIX- BASS/DISTORTED\n    12 sc    2834 Joviee            EsDeeKid - Omens (Joviee Remix)\n    13 sc     840 95                EsDeeKid & Chief Keef ... Omens (Mashup)\n    14 sc     349 Fourgrad          EsDeeKid - Omens Dnb Remix\n  Slot 11 is the crown. It got in on `edit_char` (\"bass\" in title), not on merit.\n\nWHY THE TIER THAT SHOULD SAVE IT DOESN'T: `_dl_priority`'s index-1 leg `-_artist_hit` is a\nsubstring test over title+uploader, and 43 of the 164 relevant rows pass it (every\n\"EsDeeKid - Omens (slowed + reverb)\" does), so it discriminates nothing. There is no\nuploader-identity leg in `_dl_priority` at all. `is_official_original` - uploader == base_artist\nAND no edit words - exists only in rank_key's scope, downstream of the truncation.\n\nGATE ARITHMETIC I RE-DERIVED FROM SOURCE (all pass, so no gate could have saved it):\n  _crown_tempo_mismatch: |log2(0.9999)| = 0.00014 <= _TEMPO_TOL 0.06  -> pass\n  _OFFICIAL.search(\"OMENS - Esdeekid | REMIX- BASS/DISTORTED MetalDOCTOR\") -> False\n  _SPEED_CLAIM.search(title) -> False  (covers slowed/sped/nightcore/daycore only)\n  RULE 2 tilt: abs(-5.61) = 5.61 < _TILT_MAX 9.0 -> pass\n  _BASS_CLAIM.search(title) -> False -> _unverified_claims returns ('', []) -> claim=null\n  crown bass label (server.py:1899): cand_delta -5.61 <= -6.0 is False -> did not fire\n\nA SECOND, QUIETER CASUALTY OF THE SAME ABSENCE (crate_engine.py:4897): `official_tilts` is\nbuilt from `is_official_original` rows, so with no master in `keep` it is empty and\n`bassy_baseline` falls back to the raw clip tilt 23.74. Family max is MetalDOCTOR's 29.34,\ngap 5.60, against BASS_STRIP_GAP 6.0. The BADGE missed reading \"bass boosted\" on an\nunedited clip by 0.40 dB. With the master present the baseline would be max(23.74, 24.39)\n= 24.39 and the gap 4.95 - a real margin. This is the Blueface \"Respect My Cryppin'\" case\nthe comment at 4884 already documents.\n\nRULED OUT, each with the check: decode window (master matches at core 1.000 inside its\nfirst 20s at the engine's own dl_clip default, no slide needed); download failure (master\nfetched first try in 2.9s); missing query ('EsDeeKid Omens' is query index 1 and returns\nthe master as YouTube's #1 hit at 3.72M views); wrong clip speed (payload speed_measured\n1.0007 / speed_confirmed true / 3 refs, and my verify against the true master reads\nspeed 1.0000); core saturation as the cause (real - 7 of 7 distinct uploads read core\n1.000, including ones 25% slower - but it is why speed_exact became decisive, not why the\nanswer was missing); knockoff or re-record (the crown is genuinely the same recording,\nfp 0.8944, just re-EQ'd); mashup (single recording end to end); score floor (master is\n1.000, far above CORE_KEEP 0.50); Shazam throttle (nothing I ran touched it, and the\ntainted and clean payloads agree the master was absent both times).",
  "confidence": "high",
  "fix": "One leg added to `_dl_priority` in crate_engine.py (~line 4449), gated on the clip already having measured as unedited.\n\nInside `find_edit`, next to the existing `_artist_hit` helper, add an uploader-identity test that mirrors rank_key's `is_official_original`:\n\n    _as_posted = not (edit_label and edit_label.strip().lower() != \"as posted\")\n    _ba = (base_artist or \"\").lower()\n    def _own_master(c):\n        if not (_as_posted and _ba and shazam_reliable):\n            return False\n        up = (c.get(\"uploader\") or \"\").lower()\n        return ((_ba == up or _ba in up) or any(k in up for k in (\"vevo\", \"- topic\", \"official\", \"records\"))) \\\n               and not EDIT_WORDS.search(c[\"title\"]) and c[\"title_hits\"] >= 1\n\nthen insert `-_own_master(c)` into the returned tuple immediately after `-_artist_hit(c)`,\ni.e. above `-edit_titled`. Nothing else changes.\n\nOn this clip that promotes exactly 2 rows of 460 into the head. verify() then scores\nEsDeeKid's own upload 1.000 against the crown's 0.685, and rank_key crowns it with NO\nranking change at all: both tie at speed_exact 0 and bass_off 0, and the existing\nplain-original tier at line 5217 - already inverted to FAVOUR the master because\n`_clip_is_edit` is False - splits them 0 vs 1. The fix's whole job is to give that tier\nsomething to fire on.\n\nWhy this is the safe cut:\n- It fires ONLY when the speed lock already called the clip unedited, which is exactly the\n  condition hard-rules.md's \"only prefer the plain original when the clip is NOT an edit\"\n  scopes. On an edited clip `_own_master` returns False and the queue is byte-identical.\n- It costs at most 2 of 14 slots, and only rows whose UPLOADER is the confirmed artist -\n  not the 43 rows that merely mention them. On this pool it displaces slots 13 and 14\n  (840-play and 349-play remixes), neither of which is a plausible crown.\n- It cannot suppress an edit or veto anything: it changes what gets DOWNLOADED, not what\n  wins. verify() and rank_key still decide, so \"bass and speed NUDGE, never REJECT\" holds.\n- It also repairs `bassy_baseline` at line 4897 for free, since `official_tilts` stops\n  being empty. That is the 0.40 dB near-miss above.\n\nSecondary, cosmetic, and explicitly NOT the fix: widen `_BASS_CLAIM` (server.py:1171) and\nits twin RX_BASSCLAIM (crate.html:1376) to catch \"bass/distorted\", \"bassed\", \"bass remix\".\nThat only adds a caveat under a crown that should not be there. Ship it after, not instead.",
  "disagreement": "The three lenses agreed on every fact and disagreed on which named defect is load-bearing. I resolved it by measurement, not by vote.\n\n1) WHERE THE MASTER SITS. Lens 1 said `_dl_priority` sinks it but gave no rank. Lens 2 said\nrank 27 (YouTube) / 28 (SoundCloud) off a 463-row search. Lens 3 said \"ABSENT\" without\nlocating it. My own fresh run: 460 rows, ranks 29 and 30. The small drift is search churn\nbetween runs; what is robust across all three reconstructions is >14 against max_dl=14, and\nthe reason is identical in all of them - the `-edit_titled` leg. Resolved in favour of the\npool mechanism, now with my own numbers.\n\n2) POOL FIX vs CROWN GATE - the real split. Lens 3 called `_crown_contradicts` (server.py:1423)\n\"the real fix\": add a rule refusing a crown when an as-posted clip meets a remix/bass-titled\ncandidate. Lenses 1 and 2 called the download queue load-bearing. I side with 1 and 2, and the\ntest that settles it is what each change produces on THIS clip:\n  - Crown gate: turns a wrong crown into NO crown. The user sees \"Held back on purpose\" and a\n    shelf of six slowed uploads. The right answer is still nowhere on the screen. It also\n    fires on legitimately-correct remix crowns, where an as-posted clip really did use a\n    remix upload of the same recording at the same tempo - a false refusal by construction.\n  - Pool fix: produces the correct crown, \"EsDeeKid - Omens\" at score 1.000, measured.\nLens 3 conceded this itself (\"with that row present, rank_key already crowns it correctly with\nno code change at all\"). Its own analysis contradicts its own proposed fix. Resolved: the\ncrown gate treats the symptom; the queue is the cause.\n\n3) THE REGEX. Lens 1 and lens 3 both nominated widening `_BASS_CLAIM`. I confirmed the miss\nmyself (`_BASS_CLAIM.search(\"OMENS - Esdeekid | REMIX- BASS/DISTORTED\")` is False,\n`_unverified_claims` returns `('', [])`). But it is a caveat on a row that should not be\ncrowned at all. Demoted to secondary, and I say so explicitly rather than bundling it.\n\n4) WHAT NONE OF THE THREE STATED. Lens 1 and 2 described `-edit_titled` as a mis-tuned\npreference. It is worse than that: `is_official_original` (line 4859) requires\n`not EDIT_WORDS.search(title)`, and `edit_titled` (line 4467) is that same predicate\nnegated. So the rows rank_key's as-posted tier is BUILT to prefer are exactly the rows the\ndownload queue is built to sink. The two predicates are inverses, which is why the tier at\nline 5217 has probably never fired on a popular track in this engine's life - a viral song\nalways has more than 14 edit-titled uploads. I also measured why the tier that looks like it\nshould protect the master does not: `_dl_priority`'s `-_artist_hit` leg passes 43 of the 164\nrelevant rows because it is a substring test over title+uploader, while only 2 rows have the\nartist as the actual uploader. The queue has no uploader-identity leg at all. That is the\nsharpest statement of the mechanism and it is what my proposed fix targets.\n\n5) One thing I checked that no lens raised, and it is a near-miss worth flagging on the\nsibling clips: with no master in the pool, `bassy_baseline` (line 4897) falls back to the raw\nclip tilt, and the family gap came to 5.60 dB against BASS_STRIP_GAP 6.0. The BADGE itself\nmissed asserting \"bass boosted\" on an unedited clip by 0.40 dB. If clips 14, 24 or 25 carry\noverclaim_bass with the badge (not just the crowned title) reading bassy, this same absence is\nthe likely cause and the same fix covers it."
 },
 {
  "clip_n": 5,
  "dominant_mechanism": "The un-edited original was never downloaded, so it could never be crowned. `_dl_priority` in ~/crate/crate_engine.py:4449 puts `-edit_titled` at tuple index 4 and `-plays` at index 7, and `edit_titled = bool(EDIT_WORDS.search(t)) and not OTHER_RENDITION.search(t)` (line 4453). A plain \"No Type\" by Rae Sremmurd carries no edit word, so it sorts behind EVERY slowed/remix/bass-boosted upload of the song regardless of play count. I re-ran the engine's own discovery with the payload's real arguments and captured the download head verbatim: 558 candidates, ranks 1-51 are 51/51 edit_titled, and the first non-edit-titled row is rank 52 - \"Rae Sremmurd - No Type (Official Video)\", 938,123,895 plays. `max_dl=14` (find_edit signature, line 4103), so the canonical original misses the cut by 38 places and all 14 fetched rows are edit-titled. It never entered `keep`, never reached rank_key, and was never refused by a gate; it was starved at the download stage. The engine HAS the doctrine that should have prevented this - `_clip_is_edit` at crate_engine.py:5052, used in rank_key's last tier at 5217 - but `_dl_priority` contains zero references to it, so the \"demote the plain master\" policy is applied unconditionally, including on a clip that is not an edit. This clip is not an edit: 1.0000x, aligned at offset 0 of the master. Because ref_paths for the speed consensus is drawn from that same all-edits pool (server.py:1692-1697), the fabricated \"sped up ~1.30x\" is a downstream symptom of the same starvation, not a separate root cause. Second, independently sufficient gate, which is why the pool fix alone is not enough: `creator_hit` / `_creator_source` (crate_engine.py:4451 and 4991) test cred_toks as a bare substring of the uploader, and cred_toks = ['seth','sethpetermusic'] from the credit \"original sound - SETH\", so the unrelated account soundcloud.com/drekingston (display name \"Seth D.\") matched. That is what pinned a 645-play 2014 upload at download rank 1 of 558 and would still hold rank_key tier index 1 over Rae Sremmurd even if the master were fetched.",
  "root_cause_tag": "original_starved_at_download",
  "evidence": "DOWNLOAD HEAD, MY OWN REPRODUCTION (E.find_edit with the payload's real args, `_download_and_score` and `_sc_quota` monkeypatched to record and skip, no Shazam, no live server). Pool 558 rows. The 14 fetched: [645 (Seth D. \"No Type (Remix)\"), 12,823,004 (slow boi slowed+reverb), 4,394,116 (Jules slowed+reverb), 257,346 (Pegassi Bass Boosted), 3,988 (ULTRA SLOWED), 2,069 (Slowed Nation), 1,285 (slowed reverb), 396 (Pegassi Slowed), 0, 3,888,412 (Diamond Pistols Remix), 2,004,935 (NGHTMRE Remix), 1,554,134 (Gravez Remix), 479,981 (Sushi2go Remix), 391,914 (Nyctonian Hard Techno)]. 14/14 edit_titled. Ranks 1-51 = 51/51 edit_titled. Rank 52 = \"Rae Sremmurd - No Type (Official Video)\", 938,123,895 plays. Rank 57/58 \"No Type\" by Rae Sremmurd 1,244,515 / 1,244,185. Rank 61 \"(Official Audio)\" 608,374. Rank 63 DizRod \"(Official Instrumental)\" 573,075. Rank 69 Mike WiLL \"(Instrumental)\" 162,537. None fetched. The crowned 645-play row sits at rank 1 purely on creator_hit.\n\nCLIP IS THE MASTER'S FIRST 15s, PLAYED STRAIGHT. 25s windows every 5s across the master (yt OQrJro8LSgg), V.verify per window: t=0 core 1.000 fp 0.723 arr 0.640 speed 1.0000 lag -0.56 bass +1.59 score 1.000; t=5 1.000/fp 0.582; t=10 0.371; t=20 0.166; t=50 0.100; t=85 0.984 but fp only 0.530. Real discrimination, so the t=0 alignment is trustworthy. Clip is 15.5s.\n\nTHE CROWN IS A MISLABELLED DUPLICATE, NOT A DIFFERENT VERSION. crowned_head(0-25s of the 200.4s drekingston upload) vs master_head(0-25s): core 1.000, fp 0.816, arr 0.738, speed 1.0000, bass +0.68, same=True. It is the album master re-uploaded with \"(Remix)\" in the title. Sliding the clip across the full 200s crowned upload: best window is t=0 (0.977), everything else 0.07-0.61 - same shape as the master, so no decode-window issue.\n\nFIT AGAINST THE CLIP (V.verify, my downloads): mikewill_instr score 1.000 fp 0.753 arr 0.560 bass +0.19; dizrod_instr 1.000 fp 0.737 arr 0.690 bass +1.18; yt master head 1.000 fp 0.723 arr 0.640 bass +1.59; sc_master 0.958 fp 0.704 bass +2.48; CROWNED 0.977 fp 0.657 arr 0.491 bass +2.27 (0.9504 on the 20s head). Every un-fetched plain/instrumental upload fits the clip better than the row that was crowned.\n\n\"JUST INSTRUMENTAL\" IS THE SONG'S OWN INTRO, NOT A SEPARATE UPLOAD. Master vs a full 201s instrumental, 10s windows: t=0-10 arr 0.836, t=10-20 arr 0.696, then 0.460 / 0.426 / 0.450 / 0.459 / 0.405 / 0.522 from 20s on. The two uploads track each other closely only across the first 20s and diverge once the vocal enters, so the clip's instrumental character comes from where it sits in the master, and the plain official original at rank 52 is a sufficient correct answer.\n\nCODE FACTS READ DIRECTLY: `_clip_is_edit` defined crate_engine.py:5052, used only at 5217; `awk NR>=4449,NR<=4470 | grep -c _clip_is_edit` = 0. EDIT_WORDS (2857) contains \"instrumental\"; OTHER_RENDITION (3947) contains \"instrumental\" - so an instrumental can never be edit_titled and is also demotable by `_rendition_loses`. Neither regex contains screwed, chopped, bootleg or remake, which is why the server.py:1698-1712 \"clean master\" fallback can accept a screwed upload as a speed reference.",
  "confidence": "high",
  "fix": "In find_edit, before `wave1 = _sc_quota(sorted(cands, key=_dl_priority), max_dl)[:max_dl]` (crate_engine.py:4499) and before the wave-2 head at 4602, force-include the 2 highest-play candidates with `title_hits >= 1 and not edit_titled(c)`, unconditionally. It must NOT be keyed on `_clip_is_edit`: phase 1 labelled this clip \"slowed ~0.83x\" when it measures 1.0000x, so that switch would have been True and the guard would not have fired. Cost is 2 of 14 slots and two ~2s SoundCloud/YouTube fetches; on this clip it puts a 938M-play row scoring 1.000 into `keep`, onto the shelf, and into `ref_paths`, which also kills the fabricated \"sped up ~1.30x\" at source. Do not simply demote `-edit_titled`; that tier is load-bearing for niche edits. One companion change is required for the crown itself to move, and it is one edit in one place: build cred_toks (crate_engine.py:4426-4430) from the resolved @handle or a token of length >= 6, not any 4-char word, so \"seth\" cannot match the stranger account drekingston. Both `creator_hit` (4451) and `_creator_source` (4991) read that same list, so tightening it fixes download rank 1 and rank_key tier 1 together. The payload already carries free corroboration nobody uses: sound_created 1700204059 (2023-11-17) against the candidate's 2014-10-27 upload date makes the provenance claim impossible on its face.",
  "disagreement": "Lens 1 named the fabricated speed reference plus core saturation as dominant; lenses 2 and 3 both named `_dl_priority` download starvation. I resolved it by reproducing the download head myself rather than by vote: 51/51 edit_titled ahead of the official original, 14/14 fetched rows edit_titled, official original at rank 52 of 558. The speed error is downstream of the same starvation - `refs` is seeded from `edit[\"ref_paths\"]` (server.py:1692-1697), and that pool contained no plain master, which is exactly what forced the `len(refs) < 2` fallback into accepting a bad reference. Core saturation is an enabler, not the cause: the crown was fixed at download time, before any core existed. Lens 1 also argued the right answer is structurally unreachable because \"instrumental\" sits in OTHER_RENDITION; I ruled that out - my master-vs-instrumental window test (arr 0.836 / 0.696 over the first 20s, 0.41-0.55 after) says the intro is instrumental in the master itself, so the plain official original, which no rendition rule touches, is a sufficient correct answer. Lens 2 called the crowned row \"the worst fit on every axis\"; that overstates it - crowned vs master is core 1.000, fp 0.816, speed 1.0000, bass +0.68, so it is the same recording under a false \"(Remix)\" title, and the audible version was right while the name and the transform label were wrong. Lens 2's pool ranks (49/65/74/84) and lens 3's (49/53/57) both disagree with my run; I used my own numbers (52/57/61/63/69 of 558). Where lenses 2 and 3 split on whether the fix is a download quota alone, I side with lens 3's caveat and go further: I confirmed from rank_key that `_creator_source` sits at tuple index 1, above every audio tier, so the quota alone puts the master on the shelf and fixes the speed label but does not dethrone drekingston. Both changes are needed, and I say so rather than claiming one suffices."
 },
 {
  "clip_n": 7,
  "dominant_mechanism": "The second song is emitted from a field the anti-false-mashup floor does not guard. `_fingerprint_core` (~/crate/crate_engine.py:2440-2448) de-duplicates the Phase-1 scan hits on `_title_key` alone and sets `primary[\"songs\"] = merged` / `primary[\"multi\"] = len(merged) > 1` with NO support requirement, so one 12s window out of six is enough to create a second song. `_scan_windows(65.6)` returns [0,12,24,36,48,60] (ran it), and the t=48 window came back \"Bass Scientist / King of Bass\". The two keys never collapse: `_title_key(\"The Scientist\")` -> 'scientist', `_title_key(\"Bass Scientist\")` -> 'bass scientist', and `_key_alias('bass scientist', ['scientist'])` -> 'bass scientist' because that merge is word-PREFIX only; `_junk_id` returns False (no bass-mill term in `_MILL`). `annotate_mashup` then DID its job: MASHUP_MIN_SUPPORT = 2 (crate_engine.py:2495) rejected the pair, which is why the payload has no `mashup` and no `sections`. But the reject branch (crate_engine.py:2648-2653) writes `fp[\"mashup_rejected\"]` and returns WITHOUT touching `fp[\"songs\"]` or `fp[\"multi\"]`, and `mashup_rejected` / `mashup_probes` appear zero times in server.py and crate.html - they are write-only. server.py:822 emits `res[\"songs\"]` on `fp.get(\"multi\")` alone, never on `fp.get(\"mashup\")`, and crate.html:1576 renders the block on `secs.length>1 || alt.length>1` with `alt = d.songs`, printing \"2 songs in this clip\" at line 1581. So a one-window Shazam mis-ID that the engine's own floor already refused reaches the screen as an answer, and it is never downloaded or verified: candidates[] holds six Coldplay-family uploads plus one Despacito, nothing named Bass Scientist.",
  "root_cause_tag": "false_mashup",
  "evidence": "REPRODUCTION, no network, no Shazam (stubbed E.shazam to an async no-op, real annotate_mashup, real clip audio): feeding the two hits the payload records through the crate_engine.py:2442-2448 merge gives keys ['bass scientist','scientist'], multi=True, len(songs)=2. Running the real annotate_mashup on that fp returns mashup absent, sections absent, `mashup_rejected = [('bass scientist', 1)]` (one window, floor is 2), and fp['multi'] STILL True with songs ['The Scientist','Bass Scientist']. Applying server.py:822 to that fp emits exactly [{'song':'The Scientist','at':12},{'song':'Bass Scientist','at':48}] - c07.json's list, reproduced without a single probe.\n\nONE SONG, MEASURED MYSELF. Reference = Coldplay \"The Scientist (Official Audio)\" yt zVyvpladEms, full 309.22s pull. Each of the engine's own six 12s scan windows aligned INDEPENDENTLY against the master by eq-invariant frame cosine (verify._spectrogram + _eq_invariant, master de-meaned with a matched-length moving window):\n  clip  0-12 -> master  96.27 (pred  96.36, delta -0.09) cos 0.4625, median -0.0038\n  clip 12-24 -> master 108.34 (pred 108.36, delta -0.02) cos 0.4866, median -0.0018\n  clip 24-36 -> master 120.37 (pred 120.36, delta +0.01) cos 0.5631, median -0.0114\n  clip 36-48 -> master 132.35 (pred 132.36, delta -0.01) cos 0.4989, median -0.0154\n  clip 48-60 -> master 144.38 (pred 144.36, delta +0.02) cos 0.4127, median -0.0136   <- the \"Bass Scientist\" window\n  clip 60-65.6 -> master 156.36 (pred 156.36, delta +0.00) cos 0.5211, median -0.0082\nLeast-squares fit of those six: slope 1.00131 master-s per clip-s, intercept 96.306, max residual 0.036s (frame resolution is 0.0464s). Six independent alignments on one continuous timeline to under one frame. Speed is 1.000 +/- 0.001, so edit_label \"as posted\" is correct and the payload's vspeed 1.0196 is pairwise against the crown, not the clip's rate.\n\nBASS SCIENTIST CONTROL (yt dBL_vOzARwY \"Bass Scientist / King Of Bass\", 365.18s, full pull). Whole clip slid across it at 0.85/0.90/0.95/1.00/1.05x: best peak 0.1736. Whole clip vs the Coldplay master: 0.4293. At the disputed window, clip[48-60] peaks 0.4127 on the master vs 0.3123 on Bass Scientist - and only the master's peak is the one continuous with the other five windows. Is Bass Scientist just a retitled copy of the master? No: master[96-126] slid across it at 0.80-1.10x peaks 0.3370 (at 1.00x), against a master-vs-itself ceiling of 0.9794 on the identical segment. Different recording, so the t=48 hit is a true Shazam mis-ID.\n\nCORE IS USELESS HERE AND I DID NOT RANK ON IT. V.verify(clip[48-60], BassScientist[0-20]) returns core 0.7585, same=True; V.verify(clip[48-60], master[144.4-164.4]) returns core 0.8628, speed 1.0, bass_delta -0.49, same=True. Both \"same\". This is the core-saturation trap in references/findings/core-saturation.md, so every ranking number above is eq-invariant frame cosine with an explicit all-lag median as the null.\n\nCORPUS CHECK FOR THE FIX (all 33 graded payloads in /tmp/gdoc). Exactly four carry more than one song: n4 mashup=True -> graded wrong_speed (list not objected to); n7 mashup=False -> false_mashup; n18 mashup=False -> false_mashup; n19 mashup=True -> graded \"right\". Perfect separation: every songs-list with `mashup` present was accepted, every one with it absent was graded a false mashup.",
  "confidence": "high",
  "fix": "One line, server.py:822: change `if fp.get(\"multi\"):` to `if fp.get(\"mashup\") and fp.get(\"multi\"):`, so `res[\"songs\"]` ships only under the same condition that already gates `res[\"mashup\"]` / `res[\"sections\"]` two lines below - i.e. only when tier-2 proved the second song against the audio. Measured on the graded corpus this is 2 fixed (n7, n18) and 0 regressions: the only other multi-song payloads, n4 and n19, both carry `mashup` and both keep their list; n17 (missed_mashup) has one song and is untouched. It changes no crown, no candidate, and no ordering - it removes a payload field - so the five-clip regression gate should come back identical on crowns. Gate at the emission point rather than pruning inside the reject branch, because `annotate_mashup` is wrapped in `except Exception: pass` in `fingerprint()` and an exception would skip a prune while server.py would still see multi=True. Second, one-word change on the display side, crate.html:1576: drop the `|| alt.length>1` fallback so the mashup block renders off `sections` only, since right now the UI treats the ungated list as interchangeable with the gated one. Do NOT fix this by name-matching (suffix-merging `_key_alias`, or adding bass-mill terms to `_MILL`): I measured Bass Scientist as a genuinely different recording from the master, so the next mis-ID may share no words at all and the missing floor would produce this verdict again.",
  "disagreement": "Lens 1 and lens 3 named the same mechanism (ungated `songs`/`multi` plus a write-only `mashup_rejected`); lens 2 named the candidate pool and a knockoff crown. I resolved it for lens 1/3 by reproducing the payload's exact two-song list in process with Shazam stubbed out - the field is built before any candidate is downloaded, and lens 2's own report concedes the second song generated zero queries and zero candidates (no `mashup` key, so server.py:1612 passes pair=None). Lens 2's pool findings are real but are a different defect: my clip-vs-master slope of 1.00131 combined with the payload's clip-vs-crown vspeed 1.0196 does put the crowned TommyHasArrived upload at ~0.981 of master speed, so the app hands out a link that plays about 2% slow while labelling it \"as posted\". The owner did not grade that here (\"got the song right tho\"), so it belongs on the knockoff_master pile, not this verdict. I also corrected lens 1 on its own control: it cited V.verify cores against Bass Scientist (max 0.704) as proof of no match, but I measured core 0.7585 with same=True for clip[48-60] vs Bass Scientist[0-20] - that is saturation, not a match, and the conclusion has to rest on the continuity of six independent alignments and on eq-invariant cosine (0.4127 master vs 0.3123 bass), which it does. Lens 3's \"probes=1\" aside is misleading: fp[\"probes\"] is the per-hit sweep counter set at crate_engine.py:2203, not the scan-window count, so it neither supports nor undercuts anything. Finally, all three lenses asserted the floor \"fired\" rather than annotate_mashup crashing; the payload cannot distinguish them because server.py emits neither `mashup_rejected` nor `mashup_probes`, so I settled it by reproduction - the reject branch runs and records support of 1."
 },
 {
  "clip_n": 4,
  "dominant_mechanism": "The engine's speed READING is right and the version it put in front of him is wrong, and the 20s decode window is why. dl_clip (crate_engine.py:3806, `seconds=20`, enforced at :3847 by `--download-sections *0-%d`) fetched only the head of every candidate. The one upload in the pool that plays at exactly the clip's speed, youtube pdl56Px3a_U \"Chief Keef - Love Sosa (slowed + reverb)\" (244s, 3.17M plays), has an intro that does not match: its first 20s scores core 0.633 against the clip while t=20/30/50s all score core 1.000 at speed 1.0000. That 0.633 drops it out of the CORE_SAME band, and rank_key's core-band tier (crate_engine.py:5188) sits ABOVE speed_exact (:5190), so joana's 7Wdq6NAUAek - whose head 20s happens to align and reads core 1.000 - takes rung 0 and wins before the speed tier is ever consulted. In that tier the right upload scores bucket 0 and joana's scores bucket 5 (:5076). server.py:1763 then correctly refuses to crown joana's on tempo (|log2 0.8833| = 0.179 > _TEMPO_TOL 0.06) but sets `top = None` at :1768 with no step-down, and :1790 hardcodes `exact = candidates[0]`, so the run ends with no crown. The UI (crate.html:1660-1712) prints \"Held back on purpose\" and then lists the candidates in payload order, never re-sorted, each row an `<a href>` to the upload with a play glyph. The single version the app handed him to click plays 13.4% FASTER than his clip (0.9301x absolute vs the clip's 0.8203x, about 2.2 semitones up), which is exactly the shelf he heard when he wrote \"even more slowed than you thought\". The right upload, at 0.8197x, was sitting one row down.",
  "root_cause_tag": "decode_window",
  "evidence": "METHOD CONTROLS FIRST (my own code, /private/tmp/claude-501/-Users-rohamghiasi-Desktop-Mover-Files/3ecec15c-7dea-411d-9158-e452f71c79ca/scratchpad/j04/spd.py, nothing from verify.py in the speed numbers). My first log-spectrum method FAILED its control - a known 0.822x resample of the Apple master read back 0.9998 - so I discarded it and rebuilt with spectral whitening. Controls then passed: apple vs apple 1.0000; apple resampled 0.822x -> pitch 0.8221, tempo 0.8224; apple atempo 0.822x (pitch preserved) -> pitch 1.0004, tempo 0.8237.\n\nCLIP SPEED. Against the Apple Music commercial preview named in the payload's own preview_url: whitened log-frequency pitch ratio 0.8203, onset-envelope autocorrelation tempo ratio 0.8203 (clip beat period 2.2174s vs master 1.8190s). Pitch and tempo identical, so it is a plain resample, not a pitch-preserving stretch. Engine printed 0.8222. It is right to 0.2%. The owner's literal claim - that the clip is more slowed than 0.82x - is not supported.\n\nABSOLUTE SPEED OF ALL SIX CANDIDATES vs the same Apple master, median of four interior 25s windows (I downloaded every one in full):\n  reverb vibes  pdl56Px3a_U      0.8197   <- clip is 0.8203. 0.07% off. THE match.\n  YG Diego      soundcloud       0.8088   (1.4% fast)\n  Miamari       soundcloud       0.7949   (3.1% slow)\n  Melodii       soundcloud       0.8610   (5.0% fast)\n  kvxr          jrpN1aOPc1w      0.8910   (8.6% fast)\n  joana's       7Wdq6NAUAek      0.9301   (13.4% FAST)  <- this is what the app listed at #1\n\nDECODE WINDOW, PROVEN. True durations from yt-dlp metadata (android client): pdl56Px3a_U 244s, 7Wdq6NAUAek 216s. Sliding V.verify with a 25s window across the full files:\n  reverb vibes  t=0 core 0.633 bass -6.87 | t=10 0.639 | t=20 1.000 bass -2.11 | t=30 1.000 bass -0.87 | t=40 0.948 | t=50 1.000 bass +0.08 ... speed 1.0000 at EVERY offset.\n  joana's       t=0 core 1.000 speed 0.8832 bass -15.20.\nBoth t=0 rows reproduce the payload to the decimal (payload: 0.632/-7.0/1.0004 and 1.000/0.8833/-15.5), so the head 20s is exactly what the engine scored.\n\nRANK ARITHMETIC. core band (crate_engine.py:5188, CORE_SAME 0.95 / CORE_MERIT 0.38): joana 1.000 -> rung 0, reverb vibes 0.633 -> rung 1. Decided there. speed_exact (:5076, :5190, one tier below): reverb vibes |log2(1.0004)| = 0.0006 -> bucket 0; joana |log2(0.8833)| = 0.1791 -> 1+int((0.1791-0.03)/0.03) = 5. Never consulted. With the window slid, reverb vibes reaches rung 0 too and wins on the very next tier, 0 against 5.\n\nCROWN PATH. server.py:1647 top = verified[0]; CORE_KEEP 0.50 floor passed; :1763 _crown_tempo_mismatch refuses joana (0.179 > _TEMPO_TOL 0.06 at :1337, _SOFT_TEMPO_TOL 0.0, source escape blocked because the title matches _SPEED_CLAIM); :1768 top = None, no step-down; :1790 exact = candidates[0]. Result: no crown, with a candidate at vspeed 1.0004 and core 0.632 (over CORE_KEEP) sitting at index 1.\n\nMASHUP IS FALSE (not his complaint here, separate ticket). Clip slices 0-9s, 9-18s, 0-18s, 18-30s and 0-30s each lock to the SAME single upload (full_reverbvibes) at core 1.000 and speed 1.0000. There is no second song and no 18.0s boundary.\n\nLABEL VOCABULARY. speed_from_master.py formats every slow as bare \"%s ~%.2fx\" (lines 114 and 267), no intensity tier. The repo's own eval tool, eval/gdoc/transform_report.py speed_word() lines 67-73, grades |1-s| >= 0.12 as \"super\" and >= 0.25 as \"ULTRA\". 0.8203 is 18.0% off, i.e. \"super slowed\" by the engine's own ladder, printed to him as plain \"Slowed ~0.82x\".\n\nWorking files: /private/tmp/claude-501/-Users-rohamghiasi-Desktop-Mover-Files/3ecec15c-7dea-411d-9158-e452f71c79ca/scratchpad/j04/ (clip.wav, apple.wav, apple_slow822.wav, apple_stretch822.wav, full_reverbvibes.wav, full_joana.wav, full_kvxr.wav, full_ygdiego.wav, full_melodii.wav, full_miamari.wav, spd.py). Nothing in ~/crate was touched, the running engine was never called, and no Shazam path was reached.",
  "confidence": "high",
  "fix": "server.py:1761-1790 - make the crown refusal fall through instead of terminating. When _crown_tempo_mismatch (or _crown_contradicts, or _time_reversed_null) refuses candidates[0], walk down `verified` for the next row that still clears CORE_KEEP and passes the same gates, and set `exact` to that survivor instead of hardcoding candidates[0] at :1790. On this payload that crowns reverb vibes (pdl56Px3a_U, core 0.632 over the 0.50 floor, vspeed 1.0004 so |log2| 0.0006 well inside _TEMPO_TOL 0.06, and _crown_contradicts cannot fire because the measured label is \"slowed ~0.82x\" not \"as posted\") - the upload I measured at 0.8197x against the clip's 0.8203x. It is about five lines, needs no new download or verify, and provably cannot regress a currently-succeeding crown because it only runs on a path where `top` is already None. That makes it the surgical one; it is NOT the root. The root is the decode window (crate_engine.py:3806/3847) and it needs the fix hard-rules.md already tickets as crate#10: when a candidate is longer than the decode window and its head core lands in the ambiguous band while its speed lock is near 1.0, slide 20s windows across the file and keep the best core rather than reinstating the reverted blind 90s re-decode. That alone lifts reverb vibes 0.633 -> 1.000, puts it on rung 0, and the existing speed_exact tier (0 vs 5) crowns it at #1 on merit with no gate change at all.",
  "disagreement": "FOUR disagreements, all resolved by my own measurement rather than by vote.\n\n1. WHICH UPLOAD IS THE RIGHT ONE. Lenses 1 and 3 said reverb vibes (pdl56Px3a_U); lens 2 said YG Diego (soundcloud/brandon-j-rimes) and called reverb vibes \"a 22s snippet of a different section\". Lens 2 is wrong, and I reproduced the exact artefact that misled it: E.dl_clip's fast path _dl_direct (crate_engine.py:3779, called at :3841 with a budget of min(6, timeout)) returns a TRUNCATED file. Asking it for 400s of pdl56Px3a_U handed me 22.29s and of 7Wdq6NAUAek 81.92s, while the subprocess path with the identical `--download-sections *0-400` gave the true 244.0s and 215.6s. Lens 2 measured a truncated file. My absolute speeds settle it: reverb vibes 0.8197 against the clip's 0.8203 (0.07%), YG Diego 0.8088 (1.4% off). reverb vibes is the answer. Worth its own small ticket that _dl_direct silently returns short.\n\n2. IS THE MASHUP REAL. Lens 1 said fabricated, lens 3 said \"the boundary at 18.0s is real\". Fabricated. Every slice of the clip (0-9, 9-18, 0-18, 18-30, 0-30) locks to one upload at core 1.000 and speed 1.0000.\n\n3. IS THE 0.8222 READING WRONG. All three said no; I re-derived it independently and agree, but only after my first method failed its own control (a known 0.822x resample read back 0.9998) - which is why I rebuilt it with whitening and re-ran the controls before trusting a single number. Pitch 0.8203 and tempo 0.8203 against the payload's own Apple master.\n\n4. WHICH FIX IS PRIMARY. Lenses 1 and 3 put the window slide first; lens 2 put the crown fall-through first. Both fix this clip, which is why they could argue. I split the two roles: decode_window is the CAUSE (it is why the right upload was not #1 on merit, and it is already the ticketed crate#10 defect), the missing fall-through is the AMPLIFIER (it turned a mis-order into no answer at all), and the fall-through is the surgical patch because it cannot touch any crown that currently succeeds.\n\nONE THING I CANNOT MEASURE, stated plainly. His words admit two readings. (a) He clicked row 1 - the rows are `<a href>` links to the uploads with a play glyph - heard joana's at 13.4% faster than his clip, and wrote \"even more slowed than you thought\". (b) He read the bare badge \"Slowed ~0.82x\" and felt the word undersold an 18% slow, reaching for the subgenre term \"ultra slowed\". I favour (a): 0.8222 is measurably correct, so the version is the only thing on that card that IS wrong about slowness, and his phrase is comparative against something he experienced. But (b) is live and I cannot rule it out from the payload. It does not change the fix ordering, because crowning the 0.8197x upload satisfies (a) and (b) both, while the vocabulary change alone satisfies neither if he is on (a). Cheap insurance for (b): adopt transform_report.py's existing speed_word() ladder in speed_from_master.py so 0.82 prints \"super slowed ~0.82x\". That same change is probably the whole answer on clip 30 (\"should be SUPER slowed\"), where by contrast the number really is under-read - c30 carries no speed_measured at all, so its \"slowed ~0.93x\" came from the pairwise fallback rather than a master anchor. c4 and c30 share the owner's wording but not the cause, and should not be tagged together."
 },
 {
  "clip_n": 9,
  "dominant_mechanism": "The literal word `instrumental` inside OTHER_RENDITION (crate_engine.py:3947) demotes the correct upload TWICE, on title alone, and nothing anywhere in the pipeline ever asks whether the CLIP has vocals. Stage 1, download selection: `_dl_priority` (crate_engine.py:4449) computes `edit_titled = bool(EDIT_WORDS.search(t)) and not OTHER_RENDITION.search(t)` at line 4453, so every instrumental is forced to edit_titled=False and sorts below every Lollipop row whose title says bass/slowed/remix/album version. I reproduced the real 442-row pool and the real sort: \"Lil Wayne Lollipop Instrumental\" (YouTube WGVYT4BCKjQ, 3,159,248 plays) lands at rank 104 of 442, while `max_dl=14` (crate_engine.py:4103, wave1 at :4499) downloads only the first 14. Deleting ONLY the `and not OTHER_RENDITION.search(t)` clause moves that exact row to rank 10, inside wave1. Stage 2, ranking: the same regex sets `not_other=False` (line 4784) and `_rendition_loses` (lines 5026-5027) returns True whenever a non-rendition rival sits within 0.05 core, which is automatic once both saturate; it is rank_key tier 4 (line ~5133), above speed, bass, core and plays. So even after the download fix, \"Lollipop (Album Version (Explicit))\" (xmj2QSbRZLE, 18.3M plays, already wave1 slot 7, not_other=True) crowns the instrumental out. A third, independently fatal blocker sits between them: `dl_clip` sections `*0-20` (crate_engine.py:3806), and on its first 20s the true instrumental reads core 0.4433, under CORE_KEEP 0.50, versus 1.000 at 13 of 23 sliding windows. Net: the engine is structurally incapable of returning an instrumental whenever any vocal upload of the same song is in the pool, and the clip genuinely IS the instrumental.",
  "root_cause_tag": "instrumental_demoted",
  "evidence": "All numbers are mine, run read-only from /tmp/gdoc/c09judge. No Shazam, no engine call, no edit to ~/crate.\n\nCLIP IS GENUINELY THE INSTRUMENTAL (whisper-cli, ggml-base.en, 16k mono, 20s spans):\n  clip (12.9s, whole thing) -> \"(upbeat music)\"\n  full_INSTR_A (WGVYT4BCKjQ) at t=0/20/40/90/150 -> \"[MUSIC PLAYING]\" / \"(upbeat music)\" x4, no words anywhere\n  full_VOCAL_ALB (xmj2QSbRZLE) at t=20 -> \"She gave me like a lie, bop ... shot at one of the bottles in the club\"; t=40 -> \"I make them feel right when it's wrong like\"; t=60 -> \"She never had a little black mind...\"\n  Those are exactly the vocal windows that match the clip at core 1.000 (t=20, t=30). Lyrics in the vocal master at the matching offset, none in the clip. Owner is right.\n\nFIRST 20s, i.e. what dl_clip hands verify today (V.verify against the clip):\n  INSTR_A    core 0.4433 score 0.1423 fp 0.5354 arr 0.2552 speed 0.9002 bass -9.65\n  INSTR_B    core 0.4645 score 0.1352 fp 0.5354 arr 0.2626 speed 0.9002 bass -10.05\n  VOCAL_ALB  core 0.4948 score 0.4800 fp 0.5316 arr 0.2732 speed 0.9002 bass -0.65\n  VOCAL_OFF  core 0.1895 score 0.1838 fp 0.5215 arr 0.1663 speed 0.9002 bass +0.48\n  Every one below CORE_KEEP 0.50. Reproduces both prior lenses to 4 decimals.\n\n25s SLIDE, step 10s, full uploads:\n  INSTR_A (248s): core 1.000 at t=10,20,30,40,80,90,100,150,160,170,180,210,220 -> 13 of 23 windows.\n  VOCAL_ALB (239s): core 1.000 at t=10,20,30,90,100,150 -> 6 of 22.\n  Both saturate. _best_real (best not_other core) = 1.000, so _rendition_loses(instrumental) = True and (1.000 >= 1.000 - 0.05) = True. It fires.\n\nPOOL AND DOWNLOAD SORT, reproduced end to end:\n  E.build_queries with the payload's own inputs -> exactly 16 queries, cap hit, ZERO containing \"instrumental\". Slots 3-11 are junk hints (\"Finance x Law x softwear\", \"Finance\", \"Law\" x3 variants each); trimming hints to the one good one returns slots 7-15 to the Lollipop edit family (mashup / x / kryd / bass boosted / slowed / slowed reverb).\n  E.search_edits(those 16, 8) -> 442 rows in 5.3s. 4 instrumental rows present. WGVYT4BCKjQ entered via query 'Mackadenice Lollipop' on YouTube.\n  Faithful _dl_priority replay (key_terms={'lollipop'}, artist_toks=['mackadenice'], cred_toks=['bymgc'], credit_toks=[], dir_word='slowed'): WGVYT4BCKjQ at sort rank 104/442; the other two Lollipop instrumentals at 147 and 161. wave1 = 14 vocal/remix rows, slot 0 \"Lollipop | Bass Boosted\", slot 7 \"Lollipop (Album Version (Explicit))\".\n  Same replay with ONLY `and not OTHER_RENDITION.search(t)` removed from edit_titled: WGVYT4BCKjQ at rank 10, in wave1 = True. (dir_word='' gives rank 8, also in wave1.)\n\nnot_other, computed with the live regex: 'Lil Wayne Lollipop Instrumental' -> False; 'Lollipop (Album Version (Explicit))' -> True; 'lil wayne - lollipop ( slowed + reverb )' -> True; 'Lollipop | Bass Boosted | Lil Wayne' -> True.\n\nCOUNTERFACTUAL, each leg measured:\n  download fix alone -> instrumental downloaded, first-20s core 0.4433, admitted by `keep` (crate_engine.py:4791-4794, title_hits rescue at CORE_KEEP-0.15) but the crown is nulled by server.py:1648 (`core < E.CORE_KEEP`). Visible answer goes from weak_exact 0.241 to weak_exact 0.443, still unsure=True. NO improvement.\n  window fix alone -> instrumental still at rank 104, never downloaded; the wave1 vocal Album Version slides to 1.000 and gets crowned. Confidently wrong, exactly the owner's complaint.\n  download + window -> both at 1.000, _rendition_loses fires, vocal crowned. Still wrong.\n  All three -> correct.\n\nSeparate, not this lens: verify returned speed 0.9002 identically against instrumental and vocal at every window, against the shipped label \"slowed ~0.95x\". Same under-read direction as clip #4.",
  "confidence": "high",
  "fix": "There is no single change that fixes this clip. Three blockers fire in sequence and each is independently fatal; I measured all three. The most surgical change for the mechanism I am naming is one regex split, and it must ship with the other two.\n\n1. crate_engine.py:3947 - split OTHER_RENDITION into PERFORMANCE_RENDITION (cover|guitar|piano|live|acoustic|karaoke|remaster|1 hour|hour loop) and STEM_RENDITION (instrumental), and have `_dl_priority`'s edit_titled at line 4453 exclude only PERFORMANCE_RENDITION. An instrumental is the same master with a stem muted; a guitar cover is a different performance. Measured effect of exactly this clause: WGVYT4BCKjQ rank 104 -> rank 10, inside wave1's 14 slots. Changes only what gets a download slot, nothing about ranking.\n\n2. Ship it with the decode-window ticket (rohamghiasicw-star/crate#10). Without it the download fix is inert: first-20s core 0.4433 versus CORE_KEEP 0.50, and 1.000 at 13 sliding windows.\n\n3. Make `_rendition_loses` (crate_engine.py:5026) SIGNED, and gate it on vocal presence measured ONCE on the CLIP, cached on clip_ctx beside tilt/reverb/slope - demote a stem rendition only when the clip has speech, prefer it when it does not. Do NOT use an arr-margin tiebreak: I measured it and it is unsafe. Across the windows where each candidate reaches its top score 0.9699, the instrumental's arr spans 0.5002-0.7566 (11 windows) and the vocal's 0.3854-0.5110 (3 windows), so the worst-case margin is -0.0108, not the +0.19 to +0.28 one lens reported from its own chosen windows. A 0.15 arr bar would silently fail here. Vocal presence is binary and held cleanly in my test.\n\nRegression gate twice, per the skill. The case that must not break is the Zelgin Jackson \"(Official Instrumental)\" miss that _rendition_loses was written for (comment at crate_engine.py:5015-5019) - a signed rule fixes that one in the correct direction rather than trading it away - and Anytunz's \"(Marimba Ringtone Cover)\", which is a PERFORMANCE rendition and is untouched by the split.\n\nWorth a separate ticket, not this fix: build_queries emits no instrumental lane at all, and 9 of its 16 hard-capped slots went to caption noise (\"Finance\", \"Law\"). Requiring a hint to be >= 2 tokens or to share a word with base_title returns those slots to the Lollipop edit family. The instrumental was reachable without it here (it came in on 'Mackadenice Lollipop'), so this is depth, not the cause.",
  "disagreement": "Three real conflicts, all resolved by my own runs rather than by vote.\n\n1. Was the right row in the pool? Lens 3 said it was ABSENT because build_queries can never emit an \"instrumental\" query. Lens 2 said it was present at download-sort rank 104. Lens 2 is right and Lens 3 is wrong. I re-ran E.search_edits on the real 16 queries: 442 rows, and \"Lil Wayne Lollipop Instrumental\" (WGVYT4BCKjQ) is in them, surfaced by the generic query 'Mackadenice Lollipop' on YouTube. Both lenses are correct that no query contains \"instrumental\" - I reproduced that too - but the missing query is not what killed the clip. The kill site is `_dl_priority` plus max_dl=14, and I reproduced rank 104/442 independently.\n\n2. Would _rendition_loses have fired? Lens 2 ruled it out using first-20s cores (instrumental 0.443 vs _best_real 0.334, so 0.334 >= 0.393 is False). That ruling-out is conditional on the decode window staying broken, not general. I measured the aligned windows: the instrumental hits core 1.000 at 13 of 23 windows and \"Lollipop (Album Version (Explicit))\" (xmj2QSbRZLE, 18.3M plays, not_other=True, already wave1 slot 7) hits 1.000 at 6 of 22. The instant the window fix lands, _best_real = 1.000 and the predicate is trivially True. Lenses 1 and 3 are right on this leg.\n\n3. Is arr the discriminator? Lens 1 and Lens 3 both proposed ranking the tie on raw arr, quoting margins of +0.19 to +0.28 (Lens 3 proposed a 0.15 bar). I checked the full window set instead of a chosen one: among windows where each candidate reaches its own top score 0.9699, instrumental arr spans 0.5002-0.7566 and vocal arr spans 0.3854-0.5110, so the worst-case margin is -0.0108. Their margins are real but window-cherry-picked; the fix built on them is not safe. Lens 1's other proposal - vocal presence read on the clip - is the one that holds, and I reproduced it (clip and instrumental both transcribe as \"(upbeat music)\"; the vocal master returns Lollipop lyrics at t=20/40/60, the same offsets that match the clip at core 1.000).\n\n4. Which is dominant, core saturation (Lens 1), download selection (Lens 2), or query absence (Lens 3)? None on its own. I ran the counterfactual for each and every single-leg fix leaves the answer wrong: download-only takes weak_exact from 0.241 to 0.443 and stays unsure; window-only crowns the vocal Album Version, which is precisely the complaint; download+window still hands the crown to the vocal via _rendition_loses. The one policy that fires at two of the three stages, and is the last one standing after the others land, is OTHER_RENDITION treating \"instrumental\" as a wrong rendition on title alone. That is what I named. Lens 1's ceiling story (FP_HI 0.72 / ARR_HI 0.45 clipping core to 1.000) is a real contributor and is what manufactures the tie, but it is the already-documented core_saturation finding rather than what makes this clip specifically an instrumental miss - clip #9 groups with #5 and #23, not with the saturation clips."
 },
 {
  "clip_n": 11,
  "dominant_mechanism": "The clip's ~6s near-silent lead-in (the owner's \"fades in and out, there is a sound effect\") leaks straight into verify.py's supposedly content-only representation and becomes the thing `core` measures, so four rows printed \"100% match\" on audio that does not contain the clip's music at all.\n\n`_eq_invariant` (~/crate/verify.py:279-288) removes each BAND's time-mean and then unit-L2-normalises each frame, but it never removes each FRAME's level. On a clip with a big loudness step, every quiet frame becomes a vector of all-negative band residuals and every loud frame all-positive, so after normalisation the quiet frames collapse onto one unit direction and the loud frames onto its negation. `_arr_score`'s mean cosine (verify.py:290+) then scores loudness-envelope agreement, not music. Measured on this clip: rank-1 SVD energy share of `_eq_invariant(clip)` = 0.665, mean |cos| of each frame to the mean direction = 0.776; cut the clip into its two halves and the same numbers fall to 0.224 (0-6s) and 0.208 (6-14s).\n\n`arr` is then inflated past `ARR_HI = 0.45` (verify.py:63) and `core = max(_norm(fp,0.55,0.72), _norm(arr,0.10,0.45))` (verify.py:543) clamps to exactly 1.000. `crate.html:1360` renders raw `c.core` through `confChip` as \"N% match\" on every candidate row, independent of the crown gates, so the four saturated rows reached the screen even though the server had already refused the crown (exact=null, crown_rejected, unsure=true).\n\nThe fade is not a coincidence, it is the cause. I built a synthetic clip out of 6s of numpy-generated quiet noise followed by 8s of Daft Punk \"Get Lucky\" and scored it against the Tove Lo bass-boost upload that got crowned here: core 1.0000. The same 8s of Get Lucky with the noise lead-in removed scores 0.1398 against the same upload. Six seconds of noise turns an unrelated Tove Lo upload into a 100% match for Daft Punk.",
  "root_cause_tag": "core_saturation",
  "evidence": "All numbers are from my own runs in /tmp/c11judge. Shazam was never called, the engine was never called, ~/crate is unmodified (verify.py mtime still Jul 29 19:21; the one-line patch went into a copy at /tmp/c11judge/lab).\n\nCLIP ENVELOPE (my 0.25s RMS measurement, confirms the owner's words): 0.00-5.00s sits at -22.8 to -31.2 dB; a spike at 5.25s to -15.1 dB (the sound effect); music from 6.00s at -7.8 to -12.6 dB; fade-out 14.50s -28.8 dB, 14.75s -37.0 dB. A ~14 dB step at t=6.\n\nREPRODUCED THE PAYLOAD (V.verify, full clip vs each candidate's first 20s):\n  crown k68RzTezHPk  core 1.0000 fp 0.5253 arr 0.6271 score 0.3000 bass -15.80\n  COSTA remix        core 1.0000 fp 0.5503 arr 0.6677 score 0.7692 bass  -4.64\n  xTechzz bassboost  core 1.0000 fp 0.5559 arr 0.5547 score 0.3000 bass -12.00\n  Crankdat remix     core 1.0000 fp 0.5159 arr 0.5444 score 0.3000 bass -10.80\nMatches the payload's cores and bass figures digit for digit.\n\nMY OWN NEGATIVE CONTROLS, unrelated songs none of the three lenses used, same code path, same full clip:\n  Rihanna \"Umbrella\"      core 0.7348 (arr 0.3572)  -> the UI would print 73% match\n  Adele \"Rolling in the Deep\" core 0.9011 (arr 0.4154) -> 90% match\n  Daft Punk \"Get Lucky\"   core 0.0257\n\nTHE FALSIFYING EXPERIMENT (synthetic clip, ground truth known by construction). 6s of numpy pink-ish noise at -28 dBFS + 8s of Get Lucky at -11 dBFS, mimicking this clip's envelope:\n  cand            with lead-in     lead-in removed\n  crown (Tove Lo)  core 1.0000  ->  core 0.1398\n  xTechzz          core 0.9982  ->  core 0.3117\n  Crankdat         core 0.7416  ->  core 0.4091\n  Adele            core 0.7561  ->  core 0.1068\n  Get Lucky (true) core 1.0000  ->  core 1.0000 (fp 0.9731, arr 0.9795)\nSix seconds of noise is the whole difference between \"unrelated\" and \"100% match\".\n\nCAUSAL FIX TEST (one line added to the lab copy's _eq_invariant: `M = M - M.mean(axis=1, keepdims=True)`):\n  real clip 11:  crown 1.0000->0.6510, COSTA 1.0000->0.9514, xTechzz 1.0000->0.5166, Crankdat 1.0000->0.5443, Umbrella 0.7348->0.1332, Adele 0.9011->0.3103, master 0.4543->0.2600. No row reads 100% any more.\n  synthetic:     crown 1.0000->0.0752, xTechzz 0.9982->0.0742, Adele 0.7561->0.0168, Get Lucky (true) stays 1.0000. True/false ratio goes from 1.00 to 13.3.\n\nTHE OTHER CANDIDATE FIX DOES NOT WORK. Recomputing core with ARR_HI raised from 0.45 to 0.93 (the level a genuine same-master pair reaches) on the synthetic clip: crown 0.517, xTechzz 0.421, Adele 0.319, true Get Lucky 0.662. Still 52% on a completely unrelated song, ratio 1.28. Raising the ceiling hides the \"100%\" text and leaves the wrong ordering intact; removing the envelope fixes both.\n\nTIME-REVERSED NULL, shipped code, full clip: crown forward 1.0000 / reversed 1.0000; xTechzz 1.0000 / 1.0000; Adele 0.9011 / reversed 1.0000 (reversal IMPROVES it). Time reversal destroys the recording and preserves the envelope, so this is direct proof the score carries no recording-specific evidence. `_time_reversed_null` (server.py:1782) would have fired, but it sits behind `if top:` at the end of the chain and `_crown_contradicts` (server.py:1770) had already set `top = None`, so it never ran and the UI got the tilt reason instead of the honest one.\n\nWHY THE 100%s WERE FALSE ON THEIR OWN TERMS: scored against the clip's MUSIC ONLY (6.0-14.0s), every pool candidate's first 20s collapses - crown 0.1347, COSTA 0.1633, xTechzz 0.1048, Crankdat 0.0924, official master 0.2433. None of the scored windows contains the clip's music. Sliding 25s windows across the full 236.8s official master with the same music-only probe finds it at fp 0.7391-0.8685 / core 1.000 across t=30-60, 90-120 and 160-210 (best fp 0.8685 at t=120). So the music is real and findable, and nothing that was scored contained it.",
  "confidence": "high",
  "fix": "One line in ~/crate/verify.py, inside `_eq_invariant` at line 286, double-centring before the L2 norm:\n\n    M = M - M.mean(axis=0, keepdims=True)\n    M = M - M.mean(axis=1, keepdims=True)   # NEW: remove each frame's level too\n    norm = np.sqrt((M * M).sum(axis=1, keepdims=True)) + 1e-6\n\nThe docstring already claims this (\"what's left is the time-varying content\"); the code only half does it. Removing the per-frame level is what makes the representation actually level-invariant, and it is the minimum change that both kills the false 100% and repairs the ordering. Measured above: unrelated candidates drop to 0.017-0.075 on the synthetic control while the true source holds 1.0000 with fp 0.9731.\n\nIt is surgical in the sense that matters here - it does not touch a threshold, a tier or a gate, so nothing that currently ranks on `fp` or on the `score` tilt penalty moves. But it does change `arr` on every clip, so it has to clear the five-clip regression gate in references/accuracy.md twice before shipping, and the kyks / mason clips (heavy slow and reverb, low transients) are the ones most likely to lose arr headroom - check those two crowns first.\n\nTwo follow-ons that are not the cause and should be separate tickets, in this order:\n1. crate.html:1360 prints raw `core` through `confChip` on every row regardless of the crown gates. This clip's server had already written crown_rejected / exact=null / unsure=true and the user still saw \"100% match\". Suppress the row percentage whenever the crown was refused, or replace it with the gate's reason.\n2. Move `_time_reversed_null` (server.py:1782) ahead of `_crown_contradicts`, or run it unconditionally on any core >= CORE_SAME row. It fires on every saturated row here and names the real defect; gate ordering gave the UI the tilt reason instead.",
  "disagreement": "Two real disagreements.\n\n1. Envelope leak (lens A) versus the ARR_HI clamp (lenses B and C) as the root. Resolved by measurement, not vote. I built a controlled counterfactual on the synthetic clip where I know ground truth: with ARR_HI raised to 0.93 the unrelated Tove Lo upload still reads 0.517 against Daft Punk audio and the true source only 0.662, a ratio of 1.28; with the envelope removed the unrelated upload reads 0.075 and the true source 1.000, a ratio of 13.3. The clamp is the amplifier that turns an inflated number into a printed \"100%\", the envelope leak is what inflates it. Lens A is right on the root and its double-centring result reproduced on my run.\n\n2. Was the official master in the pool? Lens B says it was never downloaded (_dl_priority index 67 of 240 against max_dl 14). Lens C says it was downloaded and lost at the strong_core tier. I did not re-run search_edits, so I have not adjudicated that, and I am not going to claim I did. It also does not change the answer: I measured the master's own first 20s at core 0.4543 against the full clip and 0.2433 against the clip's music alone, so on lens C's story it loses anyway, and on lens B's story it was never asked. Either way the four rows that reached the owner's screen are false on their own terms, which my synthetic control proves independently of the pool.\n\nWhere I part company with lens A on a detail: it reported the fade region as DEPRESSING arr, based on swapping the frame gate. That is consistent with what I measured and is not a contradiction, it is an ordering artefact. `_eq_invariant` is computed over the whole matrix, so the band means already carry the fade before any gate runs; changing which frames the gate keeps cannot undo it, only trimming the audio before the transform can. My 0-6s and 6-14s splits (rank-1 share 0.665 whole, 0.224 and 0.208 apart) show the correlation lives in the step between the halves, not in either half.\n\nLens C's decode-window observation is real and I confirmed it (master at t=120 gives fp 0.8685 against fp 0.5487 at t=0), but it is a second defect and already ticket #10. If the window were fixed and the envelope left alone, the master would read 100% and so would Daft Punk. If the envelope is fixed and the window left alone, no row reads 100%, the engine falls back to base song only, and the owner's complaint goes away. That ordering is why core_saturation is the dominant mechanism here and decode_window is not."
 },
 {
  "clip_n": 10,
  "dominant_mechanism": "The correct version was never retrieved, because an inverted `edit_label` steered every query in the wrong tempo direction and pushed the one query that finds the answer past the hard cap.\n\nChain, all three links measured:\n\n(1) INVERSION. `_consensus_id` (crate_engine.py:2044-2090) ends `return min(pool, key=lambda h: len(h.get(\"title\") or \"\"))`. Inside the winning group every title is the same string, so the length key is a no-op and Python's `min` returns the FIRST-INSERTED hit. Insertion order is FINE_SWEEP order (crate_engine.py:1815-1821), which front-loads all five sped-up counter-rates ahead of every slowed one. Any clip that answers at both a low and a high counter-rate is therefore deterministically labelled SPED UP. This clip measures 0.9002x SLOWED and shipped `edit_label: \"sped up ~1.11x\"`.\n\n(2) THE LABEL PICKS THE QUERIES. `build_queries` (crate_engine.py:3255-3375) sets `edit_word` from `edit_label` at line 3281. With \"sped up\", five of the sixteen surviving slots go to sped-up variants (positions 2, 4, 9, 10, 11), and the unconditional slowed/slowed-reverb safety net at lines 3364-3371 (added after the Trophies clip for exactly this failure) lands at positions 18 and 19 and is cut by `return q[:16]` at line 3375. With the correct label \"slowed ~0.90x\", \"Fetty Wap Trap Queen slowed\" sits at position 10, inside the cap. The engine's own correct reading (`speed_measured` 0.9015, `speed_confirmed` true) exists but is produced by `speed_from_master.measure_consensus` at server.py:1724, 118 lines AFTER `E.find_edit(... edit_label ...)` at server.py:1606, so it can never reach the search.\n\n(3) THE POOL IS THEREFORE EMPTY OF THE ANSWER. The 16 emitted queries returned 289 rows and ZERO of the five correct slowed uploads. The single truncated query returns all five in 1.9s.\n\nCO-REQUISITE, not a separate ticket: the 20s decode window. Fixing the queries alone does NOT flip this clip. All five correct uploads read core 0.347 to 0.402 on their first 20s, none clears CORE_EDIT 0.62, so none earns `strong_core`, and rank_key's `0 if c.get(\"strong_core\") else 1` (crate_engine.py:5150) sits ABOVE the core band, so the SoundCloud row at 0.684 still leads. Fixing the window alone does nothing either, because the answer is not in the pool. This clip needs both.",
  "root_cause_tag": "missing_query_variant",
  "evidence": "Every number below is mine, measured this session. No Shazam, no engine calls, no writes to ~/crate.\n\nTRUE TRANSFORM. verify(clip, official master bIR5tBW5hlQ, 222.03s), 25s windows / 10s step, 21 windows: core 1.000 at t=40/50/60/70/100, speed 0.9002 at every window, same=True, best score 0.970 at t=50. Clip is SLOWED 0.9002x. Payload says `edit_label: \"sped up ~1.11x\"`, the reciprocal.\n\nINVERSION IS DETERMINISTIC (pure Python, E._consensus_id, three synthetic same-title hits at rates 0.90 / 1.08 / 1.12):\n  FINE_SWEEP order -> rate 0.9,  \"sped up ~1.11x\"\n  reversed         -> rate 1.12, \"slowed ~0.89x\"\n  same with hints=[\"Trap queen by fetty wap\"] -> identical flip.\nReversing the input list flips the reported direction. The pick is list position, nothing else.\n\nQUERY TRUNCATION (E.build_queries with the clip's real inputs, and a cap-lifted copy):\n  edit_label \"sped up ~1.11x\", uncapped 19 queries. Cut by q[:16]:\n     17 Fetty Wap Trap Queen bass boosted\n     18 Fetty Wap Trap Queen slowed\n     19 Fetty Wap Trap Queen slowed reverb\n  edit_label \"slowed ~0.90x\": \"Fetty Wap Trap Queen slowed\" moves to position 10, inside the cap, and slowed variants also occupy 2, 4, 9, 11.\n  Positions 1-16 are otherwise identical under both labels, so raising the cap displaces nothing.\n\nRETRIEVAL (E.search_edits, per=5):\n  query #18 \"Fetty Wap Trap Queen slowed\" alone -> 55 rows in 1.9s, containing RHEncbsHd0w, bZgjYC0et18, tXT5hiZaa8E, T8IhnqW6IUM, AHGhJZIvEBA (all five correct uploads).\n  query #19 \"Fetty Wap Trap Queen slowed reverb\" alone -> 18 rows in 1.2s, all five plus soundcloud yair-siegel/trap-queen-ii.\n  the 16 actually-emitted queries -> 289 rows, ZERO of the five (captured list re-checked by substring: RHEncbsHd0w False, bZgjYC0et18 False, tXT5hiZaa8E False, T8IhnqW6IUM False, AHGhJZIvEBA False).\n\nTHE ANSWER. \"fetty wap - trap queen [ slowed + reverb ] (lyrics)\", youtube.com/watch?v=RHEncbsHd0w, 239.00s:\n  first 20s (all verify ever sees): core 0.372  score 0.111  speed 0.9717  bass +18.94  same=False\n  aligned at t=50:                  core 1.000  score 0.992  speed 0.9717  bass  +0.98  same=True\n  core 1.000 also at t=60/70/80/100. |log2(0.9717)| = 0.0414, inside _TEMPO_TOL 0.06, so it passes the crown tempo gate. Title matches the owner exactly: slowed and reverb.\n\nTHE SHELF LEADER IS NOT A KNOCKOFF (this is where the lenses split, so I ran the control the hard rules require). Reference = a clean 20s of the official master at t=49, slid across each full file:\n  MASTER vs MASTER (control): core 1.000, score 1.000, same=True at t=50, speed 1.0000\n  MASTER vs shelf1 (yair-siegel/trap-royalty-extended-slowed, 466.93s, 45 windows): core 1.000, score 0.960, same=True at t=0, speed 1.2222\n  MASTER vs RHEncbsHd0w: core 1.000, score 0.978, same=True at t=50, speed 1.0794\n  MASTER vs tq2 (yair-siegel/trap-queen-ii, 244.30s): core 1.000 at t=220, speed 1.1003\nSpeed family, all against the master: clip 0.9002x, tq2 0.9089x, RHEncbsHd0w 0.9264x, shelf1 0.8182x. Duration corroboration with no audio scoring: 244.30 / 222.03 = 1.1003 for tq2; shelf1's payload vspeed 1.1003 equals 0.9002 / 0.8182 = 1.1002 to four decimals.\nSo shelf1 is the GENUINE Trap Queen master at a deeper slow preset, not a re-recording. Its crown refusal (\"clip plays 10% faster than this upload\") was arithmetically correct.\n\nCONJUNCTION PROOF (why the query fix alone is not enough). First-20s core for the five correct uploads: 0.372 / 0.389 / 0.402 / 0.375 / 0.347. CORE_EDIT is 0.62, so none earns strong_core; they enter only via the speed-family rescue at crate_engine.py:4834-4849 with strong_core=False. shelf1 at core 0.684 earns strong_core=True. rank_key orders strong_core (line 5150) ABOVE the core band (line 5188), so shelf1 still leads. And the window fix alone gains nothing: shelf1's own ceiling across all 45 windows of its 466.93s is 0.725.",
  "confidence": "high",
  "fix": "Most surgical single edit: crate_engine.py:3375, `return q[:16]` -> `return q[:19]`.\n\nIt is purely additive. I verified positions 1 through 16 are identical under both the wrong and the right label, so no existing query moves and no other clip's pool is reordered. It costs three more concurrent SoundCloud plus YouTube round trips, measured at 1.2 to 1.9s each fired concurrently. What it buys is that the Trophies-era unconditional slowed / slowed-reverb safety net actually reaches the wire on any clip that also carries comment hints plus a named credit, which is the case that silently re-broke it. Query #18 alone returns all five correct uploads.\n\nIt must ship together with the decode-window anchor (rohamghiasicw-star/crate#10), because the cap raise alone does not flip this clip: the right upload arrives at core 0.372 on its first 20s, fails strong_core, and still loses to the 0.684 SoundCloud row. Anchor the clip inside the candidate before scoring (a 10s-step / 25s-span slide found core 1.000 on all five here) and RHEncbsHd0w comes in at core 1.000 / score 0.992 / speed 0.9717 / bass +0.98 and crowns unaided. Do not reintroduce a blind 90s re-decode.\n\nTwo follow-ups, both ranking-path so both need the five-clip gate twice, and neither is needed for this clip:\n- crate_engine.py:2086-2090, `_consensus_id`. Stop returning `min(pool, key=len(title))` inside a same-title group, where it is a no-op that resolves to FINE_SWEEP insertion order. Break the tie on which counter-rate actually restored the audio.\n- server.py, after measure_consensus at ~1724. When speed_confirmed is true and speed_measured contradicts the DIRECTION of the edit_label that built the queries, overwrite edit_label too, not just `speed`, so the search and the display cannot diverge. Today the payload ships 0.9015 and \"sped up ~1.11x\" side by side.",
  "disagreement": "Three real splits. I resolved all three by measurement, not by vote.\n\n1. IS THE SHELF LEADER A KNOCKOFF? Lens 1 said no, it is the genuine master at 0.8182x. Lenses 2 and 3 said yes, its core ceiling is only 0.684 to 0.725 across its whole 7:47. Lens 1 is right and I confirmed it with the control the hard rules demand: a clean 20s of the official master slid across shelf1 gives core 1.000 / score 0.960 / same=True at t=0, speed 1.2222, against a master-vs-self control of core 1.000 / score 1.000. Lenses 2 and 3 only ever slid the CLIP against it, and clip-vs-candidate cannot separate \"different recording\" from \"same recording, 10% slower, heavily reverbed, compared against a lossy 15s phone rip\". The owner's knockoff instinct fired on the right row for the wrong reason: it is a real master edit at the wrong slow preset, and the engine's own refusal string was correct.\n\n2. WHICH UPLOAD IS THE ANSWER? Lens 2 said RHEncbsHd0w (YouTube, slowed + reverb). Lens 3 said soundcloud yair-siegel/trap-queen-ii, a 9-play upload cut by `_dl_priority`'s plays sort. I downloaded lens 3's candidate and reproduced its first-20s reading (core 0.801, speed 0.9905, versus their 0.812), so their measurement is sound, but sliding its full 244.30s tops out at score 0.299 with same=False in every window and a 14 to 20 dB low-end mismatch. RHEncbsHd0w reaches score 0.992 with bass +0.98 and same=True. Lens 2's pick is the answer. Lens 3's diagnosis is also wrong about WHERE the loss happened: the five correct uploads appear in zero of the 289 retrieved rows, so they never reached download selection at all and the plays sort never got to touch them.\n\n3. WHICH DEFECT IS DOMINANT? Lens 1 named `_consensus_id`, lens 2 named the q[:16] cap. Both are true and they are one chain, which I confirmed at both ends: the tie-break flip is deterministic and reversible in pure Python, and the cap truncation is exactly what stops the corrective query from being emitted. Either fix independently restores retrieval. I put the cap first only because it is additive and provably displaces nothing, while the tie-break is a ranking-path change. Where I part company with all three lenses is on sufficiency: none of them states that no single fix flips this clip. Lens 2 came closest by flagging ticket #10 as load-bearing. I measured the conjunction directly. Query fix alone still leads with the 0.684 SoundCloud row, because rank_key's strong_core boolean outranks the core band and the right upload sits at 0.372 on its first 20s. Window fix alone changes nothing, because the right upload is absent. Both, or the clip stays wrong."
 },
 {
  "clip_n": 8,
  "dominant_mechanism": "FALSE CREATOR PROVENANCE AT rank_key INDEX 1. The TikTok credit is \"original sound - Jack-Dean \u271d\ufe0f\". crate_engine.py:4426-4430 tokenises that DISPLAY NAME into cred_toks; I ran it in-process and got ['jack','dean','hanbury_2011']. `_creator_source` (crate_engine.py:4991, match at line 5007) searches those tokens across title+uploader, so the 4-letter common first name \"dean\" hits \"Bobby Welcomes **Dean** to Heaven | Supernatural\" - Dean Winchester, a TV character, with no relation to the sound owner. The gate's only other conditions are same=True and core >= CORE_EDIT 0.62, and the crown's head window satisfies both (I measured core 0.8931, same=True). `_creator_source` is element [1] of the rank_key tuple (crate_engine.py:5111), sitting above _artist_own [4], strong_core [5], the CORE_SAME/CORE_MERIT band [6] and speed_exact [7]. Both surviving rows tie at [0] (editmatch), so the crown was DECIDED at [1] and no audio-merit tier was ever consulted between the TV rip and Bryan Adams' own master. That is the owner's \"weird clip\", and that single tier is what put it on top. The 20s decode window (crate_engine.py:3806 dl_clip seconds=20, verify.py:87/433) is a real and necessary co-factor, not the decider: it reads the true master's intro instead of its chorus and reports 0.496 where the truth is 1.000, which is what stops the master from beating the HUTS remix once the collision is removed. I proved the split with a validated rank simulation: fixing the decode window ALONE still crowns Supernatural (it keeps tier [1]); fixing the collision ALONE crowns the \"HUTS 2020 Remix\" at 1.20x (the master's head core 0.496 loses strong_core to the remix's 0.665); fixing BOTH crowns \"Bryan Adams - Heaven (Classic Version)\", core 1.000, slowed 0.9002x, which is exactly what the owner asked for.",
  "root_cause_tag": "false_creator_provenance",
  "evidence": "All measured by me in /tmp/gdoc/c08judge. No engine calls, no Shazam. Clip via E.get_source (32.6s), candidates via yt-dlp with youtube:player_client=android at FULL length, scored with V.verify on 25s windows stepped 5s.\n\nHEAD WINDOWS (reproduce the shipped payload to 3-4 decimals, which validates the rig):\n  Supernatural FHliANSYvzo t=0 : core 0.8931 same=True  speed 1.0000 bass -0.61   (payload 0.893 / 1.0 / -0.6)\n  BA Classic   vf3zFvcKQkw t=0 : core 0.4960 same=False speed 0.9002 bass -8.19   (payload 0.496 / 0.8974 / -8.2)\n  HUTS remix   (soundcloud) t=0: core 0.6648 same=True  speed 1.1991 bass -4.47   (payload 0.665 / 1.1991 / -4.5)\n\nFULL SLIDES (best core anywhere):\n  BA Classic vf3zFvcKQkw (271.0s): 1.0000 same=True speed 0.9002 at t=30, 35 and 40 (bass -2.87 / -2.03 / -1.08); 0.9397 at 45, 0.9546 at 60. Head 0.4960. Swing = 0.504.\n  Official MV 3eT464L1YRA (241.0s): head 0.4561 (BELOW CORE_KEEP 0.50, which is why it never reached the payload); 1.0000 same=True at t=30,35,40,45,50,60,110.\n  Supernatural FHliANSYvzo (258.7s, 52 windows): 1.0000 same=True at t=230. Head 0.8931. So it DOES contain the master, at 3:50.\n  HUTS (125.0s, 11 windows): best is its own head, 0.6648. Sliding does not lift it.\n\nSPEED, GROUND-TRUTHED WITH A SYNTHETIC CONTROL (I resampled the master to 0.9002 and re-measured):\n  slowed_master vs master        speed 0.9002  sconf 0.856   (control returns the truth)\n  clip vs master@30              speed 0.9002  sconf 0.617\n  clip vs slowed_master@30       speed 1.0000  sconf 0.732   -> the clip IS the master at 0.9002x\n  SN_head vs master@30           speed 1.0000  sconf 0.396\n  SN_head vs slowed_master@30    speed 1.0000  sconf 0.425   -> impossible; the crown row's lock is a CONFIDENT FALSE 1.0000, not an sconf<0.10 default\nEngine's speed_measured 0.8984 and speed \"slowed ~0.90x\" are correct against my 0.9002. The payload's edit_label \"slowed ~0.85x\" is the stale field.\n\nTOKEN COLLISION, run in-process:\n  E.clean_name('Jack-Dean \u271d\ufe0f') -> 'Jack Dean'; cred_toks = ['jack','dean','hanbury_2011']\n  hits on \"Bobby Welcomes Dean to Heaven | Supernatural Supernatural\" -> ['dean']\n  hits on \"Bryan Adams - Heaven (Classic Version) Bryan Adams\"        -> []\n  artist_toks ['bryan','adams']: artist_hit False on the crown, True on the master.\n\nRANK SIMULATION (faithful reimplementation of rank_key using the real OTHER_RENDITION / EDIT_WORDS / _editmatch_calc / CORE_* from crate_engine; VALIDATED because it reproduces the shipped order exactly: Supernatural, HUTS, Classic, Slowed+Reverb, Roxanne):\n  A as shipped                  -> CROWN Supernatural. keys: SN (0,0,...) vs Classic (0,1,...). Decided at index [1].\n  B decode window fixed only    -> CROWN Supernatural. SN core 1.000 (0,0,0,0,1,0,0,0) still beats Classic core 1.000 (0,1,0,0,0,0,0,5).\n  C token collision fixed only  -> CROWN HUTS 2020 Remix. HUTS (0,1,0,0,0,0,...) beats Classic (0,1,0,0,0,1,...) at strong_core [5], 0.665 >= 0.62 > 0.496.\n  D both fixed                  -> CROWN Bryan Adams - Heaven (Classic Version), core 1.000, 0.9002x. Supernatural falls at _artist_own [4].\n\nNot load-bearing here: server.py:1454 `_OFFICIAL` refuses any \"official music video\" outright, and I confirmed _OFFICIAL.search(\"Bryan Adams - Heaven (Classic Version)\") is False, so the winning row in D passes. It would bite if a window slide ever promoted 3eT464L1YRA instead.",
  "confidence": "high",
  "fix": "There is no single change that fixes this clip; I tested that and am reporting it rather than picking a plausible-sounding one. Two changes are jointly necessary and jointly sufficient (counterfactual D above). PRIMARY, and the smallest of the two: crate_engine.py:5007 - `_creator_source` must stop matching cred_toks against the candidate TITLE. Match the UPLOADER field only, the way `_dl_priority`'s own creator_hit already does at crate_engine.py:4451-4452, and keep the title path only for the `creator_link` branch at 5000-5003 where provenance is stated rather than inferred. Reinforce it at crate_engine.py:4426-4430 by not splitting a personal display name into standalone tokens - \"Jack-Dean\" should yield the joined form or nothing; the len>=4 filter does not stop a common first name. A channel called \"Supernatural\" is not the editor, and a first name landing inside an arbitrary video title is not provenance. SECOND, required to land on the original rather than the HUTS remix: locate the clip inside long candidates instead of blind-decoding the head. In `_download_and_score` / `dl_clip` (crate_engine.py:3806), for candidates longer than about 90s whose head core lands in the ambiguous 0.35-0.75 band, fetch a strided set of 25s windows (t=0,30,60,90...), keep the best core, and record the offset in the existing `aligned_at` field, which is null on every row of this payload. Do not reintroduce a blind 90s re-decode - that was removed for making near-misses worse. This is the same defect already ticketed as rohamghiasicw-star/crate#10 (Drama / cl0aked0, 0.509 at head vs 1.000 at 2:20), now reproduced a second time with a 0.496 -> 1.000 swing. Correct answer for clip 8 after both: Bryan Adams - Heaven (Classic Version), youtube vf3zFvcKQkw, core 1.000, slowed ~0.90x, no meaningful bass change. Separately and cosmetically, edit_label \"slowed ~0.85x\" contradicts speed \"slowed ~0.90x\" and speed_measured 0.8984 in the same payload; my measurement is 0.9002, so edit_label is the wrong field and the two should be reconciled.",
  "disagreement": "Four real conflicts, all settled by my own measurement, none by vote.\n\n1. Does the crowned Supernatural upload ever reach same-recording grade? Lens 1 said no (\"best core anywhere = 0.894 at t=0, it never reaches same-recording grade\") and used that to argue the crown is not even the same passage. Lens 2 said it hits 1.000 at t=230. My 52-window slide across the full 258.7s file: core 1.0000, same=True at t=230. LENS 2 IS RIGHT, LENS 1 IS WRONG. It is a genuine upload carrying the real master with TV dialogue over it, not a knockoff - which is why no knockoff or re-record detector would have caught it, and why the fix has to be in the provenance tier rather than in a content filter.\n\n2. The crown's head core. Lens 3 reported 0.6929; lenses 1 and 2 and the payload say 0.893. I measured 0.8931. LENS 3 IS THE OUTLIER and its downstream reasoning about that row's audio should be discounted.\n\n3. Is the crown's vspeed 1.0 a real reading? Lens 2 checked sconf, found 0.39-0.62, and concluded the reading is real. Lens 3 called it an ~11% mis-read but did not prove it. I built a control: resampled the master to 0.9002 and confirmed the lock returns 0.9002 on it at sconf 0.856. The crown's head then reads speed 1.0000 against BOTH the master (sconf 0.396) and that slowed master (sconf 0.425), which cannot both be true. So LENS 3 IS RIGHT ON THE SUBSTANCE and LENS 2 IS RIGHT ON THE MECHANISM: it is not an sconf<0.10 default, it is a confident false lock on speech-dominated audio. Consequence for lens 2's proposed fix: a \"tiebreak toward the candidate whose vspeed agrees with speed_measured\" would work here by accident, but it is being fed a number this content class is known to fabricate, so it should not be the load-bearing repair.\n\n4. Which defect is dominant. Lens 2 said the decode window is \"the whole loss here\"; lenses 1 and 3 said the token collision is primary. I resolved it by reimplementing rank_key with the real constants and regexes, validating it against the shipped ordering (it reproduces Supernatural, HUTS, Classic, Slowed+Reverb, Roxanne exactly), then running the counterfactuals. Window fix alone still crowns Supernatural, so lens 2 is wrong that it is the whole loss. Collision fix alone crowns the HUTS remix, so lens 1 is right that neither alone is enough. Lens 1 also asserted the official master \"only survives at all through the artist_hit rescue\" - correct, I confirmed strong_core is False at 0.496 and _editmatch_calc admits it via artist_hit and core >= 0.38. I call the collision dominant on the narrow, factual ground that the shipped crown was separated from the runner-up at rank_key index [1] and indices [5] and [6] were never reached between those two rows; the decode window owns the runner-up ordering, not the decision the owner saw. Lens 3's server.py:1454 _OFFICIAL refusal is a real gate but I verified it does not fire on \"(Classic Version)\", so it is not load-bearing on this clip in either direction."
 },
 {
  "clip_n": 12,
  "dominant_mechanism": "The owner's exact answer won the pool and was then destroyed by the null-control gate `_time_reversed_null` (~/crate/server.py:1279, called at server.py:1782, decision line 1321 `if rc >= fwd_core`). candidates[0] is \"Chief Keef - I Dont Like Bass Boosted\" (xRULERS, SoundCloud), core 0.961, vspeed 0.9997, bass 0.2 dB off the clip. It cleared every score and tempo gate. The last gate refused it, `exact` went null, and the UI fell back to the \"Closest versions\" shelf whose rows 3, 4 and 5 are the Kanye West / Big Sean remix. That shelf is the \"remix\" the owner is reacting to.\n\nThe deeper fault, which none of the three lenses isolated, is in `_arr_score` (~/crate/verify.py:291). It returns `max(mean_cos)` over ~496 admissible lags x 5 freq shifts, and verify.py:499 then takes `max(ungated, gated)` on top. It never tests whether that maximum is a PEAK. A true match produces a genuine alignment spike; a reversed or unrelated recording produces a flat plateau whose top argmax still picks. The two are then compared as if they were the same kind of number. They are not, and the plateau can sit HIGHER than the spike.\n\nMeasured on this clip: the correct forward match is the ONLY pair with an alignment peak, max 0.3944 standing 0.0620 above its own 99th percentile. Every reversed copy and every wrong candidate has a peak prominence of 0.0058 to 0.0081, about ten times smaller, because there is no alignment at all. Yet Adele \"Hello\", a recording with nothing to do with Chief Keef, plateaus when reversed at 0.4357, which is ABOVE the correct crown's genuine forward peak of 0.3944.\n\nCore saturation at ARR_HI = 0.45 (verify.py:63, verify.py:543) is a real amplifier, not the root. Because fp = 0.5322 sits below FP_LO = 0.55, the fingerprint path normalises to exactly 0.0 and core rides entirely on arr. That both clips the true match's advantage away (arr 0.4283 -> core 0.938, arr 0.4564 -> core 1.000) and confines the gate's entry window to arr in [0.4325, 0.4500], a band 0.0175 wide, so the gate can only ever run where it has almost no headroom left.\n\nSeparately and secondarily, the transform label is wrong in both directions: \"slowed ~0.89x\" names the one transform that is not happening and omits the one that is.",
  "root_cause_tag": "gate_refusal",
  "evidence": "All measured by me today. No Shazam, no engine endpoint, no writes to ~/crate. Work in /tmp/gdoc/c12judge/.\n\n1. GATE REPRODUCED EXACTLY (E.dl_clip seconds=20 timeout=20, ffmpeg areverse -ac 1 -ar 44100, V.verify(clip, rev, 20), same as server.py:1313-1320):\n   xRULERS bass boosted (RIGHT)  fwd core 0.938 fp 0.5322 arr 0.4283 speed 1.0 bass -0.03 same=True | rev core 1.000 arr 0.4564 -> rc >= fwd_core -> CROWN REFUSED\n\n2. THE CONTROL HAS NO DISCRIMINATING POWER HERE. Reversal RAISES arr on 4 of the 5 pool candidates regardless of whether they are the right recording:\n   xRULERS   (right)  arr 0.4283 -> 0.4564  (+0.028)  core 0.938 -> 1.000\n   Krl Beats (wrong)  arr 0.3397 -> 0.4537  (+0.114)  core 0.685 -> 1.000\n   daydreamer remix   arr 0.2831 -> 0.4056  (+0.123)  core 0.523 -> 0.873\n   slow boi remix     arr 0.2771 -> 0.4030  (+0.126)  core 0.506 -> 0.866\n   Jay Rising         arr 0.2321 -> 0.2051  (-0.027)  core 0.378 -> 0.300\n   Four unlike recordings converge on a reversed arr of 0.40-0.46. The reversed number is a property of the clip, not of the pair.\n\n3. NULL-OF-THE-NULL, the decisive test no lens ran. Three genuinely unrelated recordings, reversed:\n   Adele \"Hello\"   fwd core 0.3809 arr 0.2333 -> REV core 0.9591 arr 0.4357  (+0.202)\n   Linkin Park     fwd core 0.2907 arr 0.2018 -> REV core 0.3057 arr 0.2070\n   Rick Astley     fwd core 0.0633 arr 0.1222 -> REV core 0.1284 arr 0.1449\n   A pop ballad the engine never considered scores rev core 0.9591 against this clip, versus 1.000 for the correct crown. The control cannot tell the right answer from an unrelated song.\n\n4. WHY, from the lag distribution inside _arr_score (496 admissible lags, min_overlap = 0.4*min(Tc,Tk), clip Tc = 328 frames):\n   pair                      MAX     p99     peak prominence (max - p99)\n   xRULERS FORWARD (right)  0.3944  0.3324  0.0620   <- a real alignment spike\n   xRULERS REVERSED         0.3179  0.3108  0.0072\n   remix FORWARD (wrong)    0.2124  0.2061  0.0064\n   remix REVERSED           0.3611  0.3553  0.0058\n   Adele FORWARD            0.1773  0.1754  0.0019\n   Adele REVERSED           0.4357  0.4276  0.0081\n   The right answer is the only row with a peak. Everything else is a flat field whose top argmax reports as if it were an alignment. And Adele's reversed plateau (0.4357) exceeds the correct crown's true peak (0.3944), so the fault survives even in RAW arr with the ceiling removed.\n\n5. GATE ENTRY WINDOW, arithmetic against the live constants: fp 0.5322 < FP_LO 0.55, so _norm(fp,...) = 0.0 and core = _norm(arr, 0.10, 0.45). Entry core >= CORE_SAME 0.95 requires arr >= 0.4325; the ceiling is 0.4500. The gate can only ever run on arr in a 0.0175-wide band, and a reversed copy needs only +0.0175 of arr to tie.\n\n6. TRANSFORM LABEL. Direct tilt measurement, no alignment dependency: clip tilt 13.94 dB slope +11.276; clean master (official upload) tilt -6.06 dB slope +9.671; delta +20.01 dB. Heavily bass boosted. Speed reads 1.0000 against all three same-master uploads (xRULERS, Krl Beats, daydreamer), and the payload's own consensus is speed_measured 0.9999 with speed_confirmed true and speed_refs 2. The displayed \"slowed ~0.89x\" matches no measurement in the run.\n\n7. HONEST CAVEAT. My forward core today read 0.938 where the graded run recorded 0.961. 0.938 is below CORE_SAME, so today the gate would not even have been eligible. Both values sit inside that 0.0175-wide band, which means whether this clip gets a crown at all is decided by roughly 0.005 of arr. The graded payload does record crown_rejected carrying the reversal string verbatim, so the gate demonstrably ran and fired in the graded run.",
  "confidence": "high",
  "fix": "One constant, at the entry guard of `_time_reversed_null`, ~/crate/server.py:1310:\n\n    if not clip_audio or not cand_url or fwd_core is None or fwd_core < E.CORE_SAME:\n                                                             ^^^ change 0.95 to 0.999\n\nThe function's own docstring says it exists because \"a core of 1.000 is supposed to mean provably the same recording\", and all four of its calibration cases (STRUCT, Omens, ur LYING, GALE) have a forward score of exactly 1.000. The gate was built to second-guess SATURATED claims. E.CORE_SAME = 0.95 also admits unsaturated ones, and clip 12's crown was 0.961, never saturated. Raising the entry to 0.999 keeps all four calibration cases eligible, keeps GALE caught (forward 1.000, reversed 1.000), and makes clip 12 ineligible, which restores the xRULERS bass-boosted crown the owner asked for. It touches nothing that currently passes.\n\nTwo follow-ups, not part of the surgical fix:\n\nDo NOT reach for the \"compare raw arr with a margin\" fix that two lenses proposed. I tested it: on clip 12 fwd 0.4283 vs rev 0.4564 is +0.028, so a 0.05 margin does happen to save this clip, but Adele reversed reaches raw arr 0.4357 against a forward true peak of 0.3944, so the same fix still inverts on an unrelated song. It is a band-aid on a clip-specific number.\n\nThe durable fix is to make `_arr_score` return peak prominence alongside the max, and have any null control compare prominence rather than magnitude. Measured separation on this clip is 0.0620 for the true match versus 0.0058-0.0081 for every reversed copy and every wrong candidate, roughly ten to one, which is far cleaner than anything the absolute score offers. That also directly addresses the core-saturation weakness the skill already documents, since prominence has no ceiling.\n\nFor the label only, never for a ranking gate: when speed_confirmed is true, let speed_measured override the FINE_SWEEP probe string in res[\"speed\"]. Here that turns \"slowed ~0.89x\" into \"as posted\" and lets the +20.01 dB tilt say \"bass boosted\", which is the owner's exact wording. Keep it out of the crown gate; server.py:1740 records that wiring speed_confirmed into the gate regressed clips 24, 29 and 30.",
  "disagreement": "All three lenses correctly named `_time_reversed_null` as the killer, so there was no dispute about WHERE. They split on the numeric ROOT, and I resolved it by measurement rather than by vote.\n\nLens 1 argued the cause is that magnitude spectra are time-reversal invariant, so reversal preserves the beat-class grid and costs almost nothing. Lenses 2 and 3 argued the cause is core saturation at ARR_HI = 0.45, turning a 0.028 arr difference into a 0.062 core inversion.\n\nMy lag-distribution measurement shows both are downstream of something neither isolated. `_arr_score` returns an argmax over ~496 lags x 5 freq shifts with no prominence test, so a flat plateau and a genuine alignment spike are reported as the same statistic and then compared. Lens 1 is right that reversal is cheap on this material but frames it as \"the texture survives\", when what actually survives is the search: argmax over a few thousand draws always returns something in the 0.40-0.46 band for this clip no matter what audio it is handed. Lens 2 and 3 are right that the ceiling makes it worse, but I falsified the fix that follows from their framing: Adele reversed reaches raw arr 0.4357 against the true forward peak of 0.3944, so removing the ceiling and comparing raw arr still inverts. Saturation is an amplifier, not the root.\n\nSecond disagreement. Lens 2 made pool hygiene a co-mechanism, claiming the Kanye remix takes 5 of 14 download slots because OTHER_RENDITION at crate_engine.py:3948 does not list \"remix\", putting the correct upload at slot 11 on a plays tiebreak. I did not reproduce the download head and I am not asserting it. It is also not load-bearing for this clip's failure by lens 2's own evidence: the correct upload WAS downloaded and DID rank first. Pool composition only decided what the shelf looked like after the crown died, so it explains why the fallback was ugly, not why there was a fallback. I kept it out of the mechanism and out of the fix.\n\nThird, a point only lens 3 raised and I confirmed and would keep: my forward core today read 0.938 against the graded run's 0.961, and both sit inside the 0.0175-wide band between the gate's entry threshold and the arr ceiling. Whether this clip is crowned at all turns on roughly 0.005 of arr. That is not an alternative explanation, it is a reason to treat the gate as unstable rather than merely wrong on this one clip."
 },
 {
  "clip_n": 13,
  "dominant_mechanism": "Reference-side decode window. The owner's URL was already rank 1 at core 1.000, and the tempo gate `_crown_tempo_mismatch` (server.py:1380) vetoed it because |log2(0.9284)| = 0.1072 > `_TEMPO_TOL` 0.06 (server.py:1337). That gate has exactly the right escape hatch for this clip (server.py:1408-1411: if the clip's independently measured speed corroborates the pair's vspeed, the candidate is crowned as the SOURCE with the clip labelled slowed), and the hatch went unevaluated because `measured` was None. `measured` was None because all 5 official-audio references failed `speed_from_master.confirm_ref`, and they failed for one reason only: the engine fetches each reference with `E.dl_clip(..., seconds=20)`, which passes yt-dlp `--download-sections *0-20` (crate_engine.py:3806), and `confirm_ref` then decodes a 25s head slice of that. On this track the first 20-25s is the intro, and it is the ONLY window in the whole 252s master that does not lock. I slid a 25s window across the full master: t=0 gives mean confidence 0.301 and 0/5 windows over the 0.40 floor (confirm_ref False), while t=20 through t=180 all give confirm_ref True at mean confidence 0.527-0.635, every one reading speed 0.9016-0.9066. So the master is perfectly measurable against this clip; the engine just never looks anywhere except at the one slice that fails. This is the `dl_clip` head-slice defect already in hard-rules.md (ticket #10, \"candidates are scored on their FIRST 20s only\"), showing up on the REFERENCE side rather than the candidate side. Feeding the gate the reading it never got flips the verdict to the owner's exact words.",
  "root_cause_tag": "decode_window",
  "evidence": "All measured by me, no Shazam, live tree untouched.\n\nBaseline reproduced: V.verify(clip, E.dl_clip(wanhuten)) = core 1.000, speed 0.9264, bass_delta -9.6, slope -4.308, score 0.3273. candidate_speed_lock = 0.92836 (this is the payload's vspeed 0.9284, i.e. `vspeed_locked`). d = |log2(0.9284)| = 0.1072 vs _TEMPO_TOL 0.06. _SPEED_CLAIM on \"Drama - Roy woods(jairtheshadow remix) bass boosted wanhuten\" = False.\n\nServer reference path reproduced verbatim (server.py:1697-1727): E.search_edits -> 58 rows, filter picked 5 clean-original uploads, all 5 downloaded at 20.0s each, confirm_ref = False x5, candidate_speed_lock = None x5. refs empty, measure_consensus never called.\n\n25s window slid across the FULL 252s clean master (soundcloud.com/mariaa-santos-840128104/roy-woods-drama-feat-drake):\n  t=0   confirm=False  med 0.9060  meanconf 0.301  pass 0/5   <- the only slice dl_clip ever fetches\n  t=20  confirm=True   med 0.9025  meanconf 0.583  pass 5/5\n  t=40  confirm=True   med 0.9023  meanconf 0.595  pass 5/5\n  t=60  confirm=True   med 0.9037  meanconf 0.589  pass 5/5\n  t=80  confirm=True   med 0.9026  meanconf 0.562  pass 5/5\n  t=100 confirm=True   med 0.9025  meanconf 0.581  pass 5/5\n  t=120 confirm=True   med 0.9066  meanconf 0.527  pass 5/5\n  t=140 confirm=True   med 0.9017  meanconf 0.595  pass 5/5\n  t=160 confirm=True   med 0.9016  meanconf 0.635  pass 5/5\n  t=180 confirm=True   med 0.9017  meanconf 0.613  pass 5/5\n  t=200 confirm=False  med 0.9033  meanconf 0.408  pass 3/5\n\nEnd-to-end counterfactual, run against the shipped gate code:\n  measure_consensus(clip, [master slices t=40/80/120/160]) = {\"speed\": 0.9034, \"label\": \"slowed ~0.90x\", \"confident\": True, \"nrefs\": 4, \"agree\": 4, \"spread\": 0.007, \"reason\": \"cluster 20 win / 4 ref\"}\n  SRV._crown_tempo_mismatch(top, None)      -> (\"clip plays 7% slower than this upload, so it is a different edit of the same recording\", None)   [byte-identical to c13.json crown_rejected]\n  SRV._crown_tempo_mismatch(top, measured)  -> (None, 0.92836)   [crown allowed, crown_is_source, clip labelled slowed ~0.93x]\n  agreement |log2(0.9284/0.9034)| = 0.0393 <= _SOURCE_AGREE_TOL 0.06\n\nADVERSARIAL CONTROL, sliding does not manufacture confirmations. Same sweep against an UNRELATED song (SZA Good Days, 240s): 11 windows, confirm_ref False on every one, mean confidence 0.258-0.379, never clearing the 0.40/0.45 floors. The true master clears at 0.527-0.635. Note the separation: the correct master's intro (0.301) reads WORSE than an unrelated song's best window (0.379), which is exactly why the head slice cannot be the only thing examined.\n\nSecond control, the poison reference: \"roy woods - drama [ edited audio for your edits ]\" is an off-speed edit locking at 1.198, and it is only 22.06s long, so it cannot be slid. Its windows score conf 0.372-0.411, mean under 0.45, so confirm_ref keeps rejecting it with or without the fix. Lowering conf_min instead would admit it and produce a confident \"sped up ~1.20x\", the opposite of the truth.\n\nCost of the fix, measured: E.dl_clip on the master at seconds=20 / 90 / 150 took 1.8s / 1.3s / 2.6s wall (yt-dlp startup dominates, extra audio is nearly free). Fetching more alone is NOT enough: confirm_ref on the 90s and 150s files still returns False because it decodes only the head. Slicing the 150s fetch: offset 0 False, offset 25 True (lock 0.9022), offset 60 True (lock 0.9044).\n\nCorpus shape: 7 of the 33 saved payloads carry a tempo veto. c03/c04/c10/c14 all carry a speed_measured (1.0007 / 0.8222 / 0.9015 / 1.0004) so their hatch was evaluated and declined. c13, c20 and c30 fired the veto with no measurement at all, so on those three the gate degenerated into an unconditional reject.",
  "confidence": "high",
  "fix": "Stop judging speed references on their first 20-25 seconds. Two touch points, one behaviour, and the gate itself is not modified.\n\n1. ~/crate/server.py:1707-1712, the official-audio reference fetch: call `E.dl_clip(ic[1][\"url\"], path, seconds=90)` instead of the 20s default. It applies to the ~5 reference uploads only, never to the 20+ candidates, and I measured it at 1.3s vs 1.8s wall, so there is no latency cost.\n2. ~/crate/speed_from_master.py: make `confirm_ref` (line 148) and `measure_consensus` (line 217) evaluate the reference at a few offsets (0 / 25 / 60s) and keep the best-locking 25s slice, instead of `V._decode(ref_path, 25)` from the head. Same DSP, same thresholds, just a different slice of the same file.\n\nOn this clip that converts 0 of 5 confirmed references into 4 agreeing, produces measure_consensus 0.9034 confident, and the untouched `_crown_tempo_mismatch` then returns (None, 0.9284) so wanhuten is crowned with crown_is_source and the clip reads \"slowed ~0.93x\", which is verbatim the owner's verdict.\n\nDo NOT lower `_win_speeds` conf_min from 0.40 (it admits the 1.198 off-speed edit and yields a confident wrong \"sped up ~1.20x\"). Do NOT loosen `_TEMPO_TOL`. Do NOT let a candidate corroborate its own vspeed; the gate's independence is the thing keeping ski slopes from being re-crowned.\n\nBlast radius to check: this makes `measured` available on clips that currently have none (17 of 33 payloads carry no speed_confirmed), and `measured` is read by both `_crown_tempo_mismatch` and `_crown_contradicts`. Run the five-clip regression gate in references/accuracy.md twice, and re-check c20 and c30, the other two vetoes that fired with no measurement.\n\nSeparate ticket worth filing: nothing in the payload distinguishes \"measured the clip at 1.0x\" from \"never managed to measure it\". c13 says speed \"as posted\" with no speed_confirmed key, and that silence is what hid this failure. Record a reason such as \"0 of 5 references confirmed\" even when no number is produced.",
  "disagreement": "All three lenses agreed on the proximate refusal (the tempo veto discarding a core 1.000 rank-1 candidate) and all three correctly ruled out retrieval, ranking, download failure, bass, mashup and candidate-side truncation. They split on WHY the escape hatch never fired, and that split decides the fix.\n\nLenses A and B said the hatch is structurally unreachable for this family: the clip is the jairtheshadow remix, verify core against the Roy Woods master is only 0.16-0.30, so no master reference can ever confirm and any remix clip is permanently unmeasurable. Their fixes followed from that, letting the top candidate corroborate its own vspeed. Lens C said the master locks fine and `confirm_ref` failed on a confidence floor caused by judging the reference on its first 25 seconds.\n\nI resolved it by measurement, not by vote. I reran the server's reference path (0 of 5 confirmed, matching A and B) and then slid a 25s window across the full 252s master myself: 9 of 12 windows confirm at mean confidence 0.527-0.635 and read speed 0.9016-0.9066, and t=0 is the only failure in the first 200s. So A and B's \"structurally unreachable\" is refuted. Their low verify-core is real but irrelevant, since confirm_ref exists precisely because core collapses on a bass-boosted clip and it is core-independent. What they measured as \"the remix cannot be referenced\" was actually \"the intro cannot be referenced\", and the 20s head fetch made those two indistinguishable from where they looked.\n\nI also stress-tested Lens C rather than accepting it: sliding the window against an UNRELATED song produced zero false confirmations in 11 windows (max mean conf 0.379 against the true master's 0.527-0.635), and the off-speed 1.198 edit stays rejected because it is only 22s long and cannot be slid. So the window fix is safe where the conf_min fix, which A and B both correctly warned against, is not.\n\nOne thing no lens raised. This clip's sound is cl0aked0, and hard-rules.md's ticket #10 records a Drama/cl0aked0 case where the true exact edit (\"Girl that's not ME\", online16e, 4:16) reads 0.509 on its first 20s and 1.000 at 2:20, against a same-recording upload at speed 0.926 / bass -9.6, which is this wanhuten row exactly. That upload is not in this pool. It does not change the verdict, because the owner named wanhuten himself and the deliverable he wants is the source plus the slowed label, but it is the same defect on the candidate side of the same clip, which is further reason to tag this decode_window rather than gate_refusal."
 },
 {
  "clip_n": 14,
  "dominant_mechanism": "The plain master was returned by the search and then denied every download slot, so the verifier only ever saw slowed/bass-boosted uploads and the screen could only be built out of edit titles. `_dl_priority` in ~/crate/crate_engine.py (defined 4449, applied at 4499 wave 1 and 4604 wave 2) sorts `-edit_titled` as its 5th key, ABOVE `-plays` and below nothing but song-relevance / artist-hit / compilation / creator-hit. 259 of the 433 search rows for this clip carry an EDIT_WORDS token, so all 14 slots of `max_dl=14` (signature 4103; `_download_and_score` truncates `todo[:max_dl]` at 4034) go to edits. I re-ran the real quota chain `_producer_quota(_web_quota(_sc_quota(cands,14),14),14)` on the reproduced pool and got a head of 14/14 edit_titled, zero plain rows, in both waves. The first non-edit row with a title hit sits at rank 38. So the six-row shelf the owner read is not a ranking accident, it is the entire downloaded pool. Everything downstream of the pool is already correct and I proved it counterfactually: the plain original (YouTube KH7FG6rQrFw, RapHype, 1.08M plays, rank 40) downloads in 2.1s, gets a full 20s file, and verifies at core 1.000 / speed 1.0000 / same=True on its FIRST 20 seconds; it is editmatch (core >= CORE_EDIT, not_other) so it clears rank_key's top rung, ties the crowned edit in core band 0, then beats it on the very next rung (speed_exact 0 vs 4), and rank_key's plain-original preference at 5210-5218 explicitly favours it because `_clip_is_edit` is False for edit_label \"as posted\". I then ran all three crown gates against it and every one returns None: `_crown_tempo_mismatch` -> (None, None), `_crown_contradicts` -> None (`_OFFICIAL` does not match \"Mac Miller - Cinderella ft. Ty Dolla $ign\"/\"RapHype\", no speed claim, bass_delta 4.79 under _TILT_MAX 9.0), `_time_reversed_null` -> None. Had that one row been fetched it would have been crowned as the exact version and the word \"slowed\" would not have appeared on the screen at all.",
  "root_cause_tag": "plain_original_never_downloaded",
  "evidence": "All numbers are from my own runs against a freshly fetched clip (E.get_source, no Shazam, no engine call).\n\nVERIFY, dl_clip at the engine's default seconds=20:\n  raphype_yt KH7FG6rQrFw (PLAIN, rank 40, NEVER downloaded)  dl 2.1s  file 20.000s  core 1.0000  speed 1.0000  bass +4.79  lag -0.98  same True  score 0.756\n  akira SC weon-aburrio-88109001 (the shelf's row 1)          dl 1.7s  file 20.000s  core 0.9760  speed 1.1003  bass +2.93  lag -1.02  same True  score 0.8724\n  official yt 0fcsGS1dEro (PLAIN, rank 38, never downloaded)  dl 3.7s  file 11.471s  core 0.6973  speed 1.0000  bass +11.90 same False\n  macmiller topic HCgltcEaB_0 (PLAIN, rank 41)                dl 4.2s  file 12.516s  core 0.6597  speed 1.0000  same False\n  larryfisherman SC (PLAIN, rank 39)                          dl FAILED 1.3s (SoundCloud 403)\nThe akira row reproduces the saved payload exactly (payload: core 0.976, vspeed 1.0991, bass 2.9), which validates my clip fetch and verify path.\n\nPOOL, reproduced offline from the 433-row search in /tmp/gdoc/c14work/sorted.json, re-sorting with my own copy of _dl_priority (artist_toks ['mac','miller'], E.EDIT_WORDS / E.OTHER_RENDITION / E._is_compilation):\n  edit_titled 259 / 433\n  wave1 = _sc_quota(sorted,14)[:14]                          -> edit flags [1]*14, plain rows: []\n  head  = _producer_quota(_web_quota(_sc_quota(cands,14),14),14) -> edit flags [1]*14, plain rows: []\n  head sources: yt, sc, sc, sc, sc, yt, yt, yt, yt, sc, yt, yt, yt, sc\n  first non-edit rows with title_hits>=1: rank 38 (44,125,367 plays, uploader \"Mac Miller\"), rank 39 (8,948,589, larryfisherman), rank 40 (1,076,163, RapHype), rank 41 (844,207)\n  E.EDIT_WORDS on \"Mac Miller - Cinderella ft. Ty Dolla $ign\" -> False, so these are genuinely plain rows losing on the -edit_titled key, not mislabelled.\n\nCOUNTERFACTUAL GATES, run for real on the plain original:\n  S._crown_tempo_mismatch(top, measured) -> (None, None)\n  S._crown_contradicts(top, \"as posted\", None, measured, tilt_readable=True) -> None\n  S._time_reversed_null(clip, KH7FG6rQrFw, fwd_core=1.000) -> None\n  S._OFFICIAL.search(\"Mac Miller - Cinderella ft. Ty Dolla $ign RapHype\") -> False\n  E._editmatch_calc(1.000, not_other=True, artist_hit=True) -> strong True, editmatch True\n  speed_exact = 0 if |log2 v| <= 0.03 else 1+int((|log2 v|-0.03)/0.03): v=1.0000 -> 0, v=1.0991 -> 4.\n  Constants read from source: CORE_KEEP 0.5, CORE_MERIT 0.38, CORE_EDIT 0.62, CORE_SAME 0.95; _TEMPO_TOL 0.06, _TILT_MAX 9.0.\n\nPAYLOAD (/tmp/gdoc/c14.json): edit_label \"as posted\", speed \"as posted\", speed_measured 1.0004, speed_refs 3, speed_confirmed true, exact null, unsure true, weak_exact 0.976, crown_rejected \"clip plays 10% faster than this upload, so it is a different edit of the same recording\", clip_secs 59.8, win [0.0, 12.0]. All 6 candidates are edit-titled (3 slowed+reverb, 2 bass boosted, 1 remix); zero plain rows.\n\nSIDE FINDING, not the mechanism: dl_clip on the official upload 0fcsGS1dEro returns a deterministically TRUNCATED 11.471s file (same byte length on 3 separate fetches, 3.7s / 5.7s / 3.8s), not the 20s it asked for. That costs that particular row 0.30 of core. It is not what broke this clip, because it was never in the download head and because a different plain upload hits 1.000 inside the same 20s.",
  "confidence": "high",
  "fix": "Add a plain-original download quota in ~/crate/crate_engine.py, APPENDED to the head rather than substituted into it, following the `_creator_extra` pattern at line 3085-3108 where the caller raises max_dl to match (the three existing quotas at 3977-4011 displace, which would cost some other clip a slot). Shape: `_plain_quota(cands, max_dl, min_plain=2)` reserving up to two rows that have `title_hits >= 1` AND `_artist_hit(c)` AND NOT `edit_titled`, fired only when `edit_label` is \"as posted\" and no comment hint or pair named an edit, so an edited clip's pool is untouched. Measured cost on this clip: one extra download at 2.1s. Nothing else needs to change - rank_key's `_clip_is_edit` branch (5052, 5210-5218) already prefers the plain original on an unedited clip, and all three crown gates already pass it, so the only reachable new outcome is that a verified plain master wins the crown it should have won. Do NOT take the alternative fixes the other two lenses proposed as the primary: wiring `speed_confirmed` into the gates is explicitly recorded as tried and reverted at server.py ~1748-1753 (SIM-B, one win for three regressions), and loosening rank_key's core band would touch every clip's ranking to fix a row that was never in the pool. The crate.html:1657 guard change is worth doing as a cheap second line of defence, but it only makes the wrong screen quieter; it does not put the right answer on it.",
  "disagreement": "All three lenses agree the base song and the transform label are right and that the complaint is about the shelf's lead row, and their raw verify numbers agree with mine. They disagree on which layer is causal, and I resolved it by measurement, not by vote.\n\nLens 3 says the dominant cause is rank_key reading the core band before speed_exact, amplified by dl_clip's 20s decode window costing the plain original 0.303 of core. I ruled this out on two measurements. First, the 0.697 they attribute to the 20s window is not a window effect at all: I probed the file and dl_clip returned a TRUNCATED 11.471s download for that URL, reproducibly, three times. Second and decisively, a different plain upload of the same master (RapHype, rank 40) gets a full 20s and reads core 1.000 in that window with no slide needed, so no decode-window change is required to make a plain original win. And with a plain original present both rows sit in core band 0, so the band rung ties and speed_exact - the very rung lens 3 says is buried - is what picks the correct one. Their mechanism is a real code observation that is non-causal here because the candidate it operates on was never in the pool.\n\nLens 1 says the dominant cause is the over-guarded \"clip is just the track\" branch at crate.html:1657-1659. I agree the guard is wrong and that fixing it would remove the offending wording, but it is downstream: it only matters because the pool held nothing but edits, and in a correct run it would never fire (there would be a crown, not an unsure shelf). Treating it as the root cause would leave the engine still unable to find the version that actually played and would only change how it says so.\n\nLens 2's mechanism survived every check I could put to it, and I closed the one thing it left open - whether the plain original would actually have been crowned rather than merely shelved - by running rank_key's rungs and all three server-side crown gates against it. Every gate returns None and it wins the ranking, so the download slot is the whole story."
 },
 {
  "clip_n": 23,
  "dominant_mechanism": "The base song ID is wrong (the clip is Gwen Stefani \"Hollaback Girl\" INSTRUMENTAL at 0.900x, not morgue \"leaveamsg - slowed\"), and the engine ships it as fact because the one gate designed to catch exactly this has its audio input hardcoded to None.\n\nwrong_song.shazam_untrustworthy(base_title, base_artist, freqskew, corpus_titles, master_core=None) is a WRONG-SONG detector. Its body computes `master_fail = master_core is not None and master_core < 0.35` (wrong_song.py:46) and `untrust = bool(strong) or master_fail or (len(reasons) >= 2)` (line 50). The module's own docstring, lines 7-8, tells the caller to compute master_core as V.verify(clip, dl_clip(master_of(base)))[\"core\"]. There is exactly ONE call site in the whole tree, server.py:863, and it passes the literal `None`:\n\n    untrust, why = wrong_song.shazam_untrustworthy(\n        base_title, base_artist, skew, corpus, None)\n\nSo the audio floor under the BASE SONG is written, documented, and disconnected. I measured what that argument would have carried on this clip: morgue's own \"leaveamsg\" master reads core 0.024 against the clip on its first 20s (exactly what dl_clip hands back), and the exact Shazam track \"leaveamsg - slowed\" reads 0.308. Both are under the module's own 0.35 threshold. Running the real function: with None it returns (False, ['base-title-absent-from-credit/comments/candidates']) - one reason, not strong, so untrust is False. With 0.024, 0.147 or 0.340 it returns True. One argument is the entire difference between \"found: morgue - leaveamsg - slowed\" with an exact=true Apple Music link and the honest \"uncertain\" that server.py:1936-1953 already knows how to emit.\n\nWhy nothing else could catch it. (a) The ID rests on one probe that no later measurement can revise: E._junk_id on the returned title is False, so Phase 1 returned at crate_engine.py:2464 and the Phase-2 counter-speed FINE_SWEEP at 2453 was never reachable. CORROB (1.12 / 1.20 / 0.85) can only overturn the posted read by NAMING a rival title; silence is treated as confirmation. Its 1.12 rate is within 0.9% of the exact counter-rate 1/0.9002 = 1.1109, so it was aimed correctly and still produced no rival, which is the expected outcome for a vocal-free instrumental. (b) base_uncertain (server.py:1936) is reachable ONLY from res[\"shazam_suspect\"], which is set only by that one crippled call. (c) result = \"found\" at server.py:1949 fires on `if exact or (fp and not res.get(\"base_uncertain\"))` - the mere existence of a Shazam fp. So 14 downloaded candidates all failing CORE_KEEP (top core 0.147, barely over the SPC_GATE junk floor of 0.12) and an empty confirm_ref set are two independent audio measurements screaming \"not this song\", and neither has a wire to the decision.\n\nThe cost is essentially zero because the masters are already downloaded. server.py:1697-1712 already fetches \"morgue leaveamsg official audio\" originals and runs confirm_ref on them; the file needed for master_core is in hand inside that same block.",
  "root_cause_tag": "unverified_base_id",
  "evidence": "All numbers below are mine, measured this session with V.verify / ffmpeg. No Shazam call, no engine call, no write to ~/crate. Working dir /tmp/gdoc/c23J, audio reused from /tmp/gdoc/c23work.\n\nTRUE IDENTITY (25s windows slid at 15s steps over each full candidate, clip = /tmp/gdoc/c23work/clip.wav, 52.46s):\n  Hollaback Girl INSTRUMENTAL (looped upload, 614.4s): BEST core 0.977 @t=510, speed 0.9002, bass -2.42, same=True, score 0.912\n  Gwen Stefani official VOCAL master (200.8s):          BEST core 0.753 @t=30,  speed 0.9002, bass -0.17, same=True\n  (instrumental beating the vocal master by 0.224 is the instrumental signature)\n  Shells x Hollaback Girl mashup (78.3s):               BEST core 0.726 @t=0,   speed 0.8917, same=True\n\nNOT CORE SATURATION (time-reversed null on the same window, per findings/core-saturation.md):\n  instrumental t=510: forward core 0.977 same=True -> REVERSED 0.201 same=False\n  leaveamsg   t=75:  forward core 0.509            -> REVERSED 0.199\n\nTHE NAMED BASE IS REFUTED BY ITS OWN CATALOGUE:\n  morgue \"leaveamsg\" master, FIRST 20s (what dl_clip returns):        core 0.024  same=False  speed 1.0000\n  morgue \"leaveamsg - slowed\" = the exact Shazam track, FIRST 20s:    core 0.308  same=False  speed 1.0000\n  same track, BEST over a full 176.4s slide:                          core 0.509  same=False  speed 1.0000\n  engine's own crowned candidate (payload):                           core 0.147  (weak_exact 0.147, SPC_GATE 0.12, CORE_KEEP 0.50)\n\nTHE DISCONNECTED GATE (ran the real function, /usr/bin/python3, import wrong_song):\n  shazam_untrustworthy(\"leaveamsg - slowed\", \"morgue\", 0.0, [\"Original audio\"], None)\n    -> (False, ['base-title-absent-from-credit/comments/candidates'])          <- what server.py:863 does today\n  ... same call with master_core=0.024  -> (True, [..., 'clip-fails-verify-vs-named-master(core=0.02)'])\n  ... with master_core=0.147            -> (True, [..., 'clip-fails-verify-vs-named-master(core=0.15)'])\n  ... with master_core=0.340            -> (True, [..., 'clip-fails-verify-vs-named-master(core=0.34)'])\n  ... corpus WITH candidate titles (as the module docstring says to build it), master_core=0.340\n    -> (True, ['clip-fails-verify-vs-named-master(core=0.34)'])                <- master_fail alone carries it\n  So the flip False -> True holds at every plausible reading of master_core, and holds whether or not\n  the corpus bug is also fixed.\n\nCODE FACTS I CHECKED DIRECTLY:\n  grep -rn \"shazam_untrustworthy\" *.py -> exactly one call site, server.py:863, fifth arg literal None.\n  wrong_song.py:46  master_fail = master_core is not None and master_core < 0.35\n  wrong_song.py:50  untrust = bool(strong) or master_fail or (len(reasons) >= 2)\n  wrong_song.py:7-8 docstring instructs the caller to compute master_core.\n  server.py:1936    base_uncertain is set ONLY under `if res.get(\"shazam_suspect\") and not exact`.\n  server.py:1949    `if exact or (fp and not res.get(\"base_uncertain\")): res[\"result\"] = \"found\"`.\n  server.py:1689    the master-fetch block is itself guarded by `if fp and shazam_reliable and base_title`.\n  server.py:1697-1712 that block already downloads the base song's official originals and confirm_ref's them.\n  E._junk_id({\"title\":\"leaveamsg - slowed\",\"artist\":\"morgue\"}) -> False, so crate_engine.py:2464 returned\n    the single hit and the Phase-2 FINE_SWEEP at 2453 was unreachable.\n  E.CORROB -> [(1.12,'slowed ~0.89x'), (1.2,'slowed ~0.83x'), (0.85,'sped up ~1.18x')]; 1/0.9002 = 1.1109,\n    so the 1.12 corroboration probe was aimed within 0.9% of the correct counter-rate and still yielded no rival.\n  Payload: probes 1 (crate_engine.py:2203 stamps n[\"i\"] onto the winning hit, so this is \"the first answering\n    probe won and was never contradicted\", not \"only one probe fired\"); no shazam_suspect key; no\n    speed_measured / speed_confirmed keys (confirm_ref returned an empty refs list); links[0].exact = true.",
  "confidence": "high",
  "fix": "One change, in the block that already has the audio in hand.\n\nserver.py, inside the master-fetch block at 1689-1725: after `got` (the downloaded official originals of the base song) is built, compute\n\n    master_core = max((V.verify(src[\"audio\"], p) or {}).get(\"core\") or 0.0 for p in got)\n\nand re-run `wrong_song.shazam_untrustworthy(base_title, base_artist, skew, corpus, master_core)`. If it now returns untrust, set `res[\"shazam_suspect\"] = why`. The existing block at server.py:1936 then flips base_uncertain on, blanks base_song/base_artist, and server.py:1949 emits `result: \"uncertain\"` instead of \"found\" - all of that machinery is already written and tested. Cost: one V.verify per already-downloaded file, no extra network, no extra Shazam.\n\nMeasured payoff on this clip: master_core 0.024 (or 0.308 if you use the Shazam track rather than the artist's master), untrust False -> True, and the payload stops asserting \"morgue - leaveamsg - slowed\" with an exact=true Apple Music album link.\n\nTwo implementation notes that matter:\n1. The re-check must live INSIDE that block, not at line 863. Line 863 runs in phase 1 before any audio is downloaded, which is why the None is there; and the master fetch at 1689 is itself gated on `shazam_reliable`, so the check cannot simply be moved earlier without hoisting the fetch. Re-evaluating at 1725 with the same function is the smallest edit.\n2. Do NOT let this touch candidate ranking. It downgrades the BASE-ID confidence only. The 0.35 threshold is far under CORE_MERIT 0.38 and CORE_KEEP 0.50, so it cannot fire while any real match sits in the pool, which keeps it inside the \"bass and speed nudge, never reject\" doctrine.\n\nWhy this and not the two rival fixes:\n- Widening the decode window would not fix this clip. I slid the full 176.4s of the crowned upload: best core 0.509, same=False. Sliding the full Hollaback instrumental only raises 0.863 to 0.977, and it was never in the pool anyway.\n- Reserving download slots for title_hits==0 rows (lens 2) would have surfaced \"Shells x Hollaback Girl\" at core 0.726, which is a real match but is a Lil Uzi Vert mashup, not the instrumental (0.977). Crowning it trades one wrong version for another, and it only works when the pool happens to contain a mashup row naming the true song. That is luck, not a mechanism.\n\nHonest limit: the correct outcome reachable by this fix is \"uncertain\", not \"Hollaback Girl instrumental\". The credit is a bare \"Original audio - togi\", desc is empty, and there are no comment hints, so with the base name discredited the engine has nothing left to query. Naming the real track requires the counter-speed sweep to run on a clip whose 1.0x probe answered plausibly, which is a separate ticket and needs Shazam to test.",
  "disagreement": "All three lenses independently landed on the same identity (Hollaback Girl instrumental at 0.900x) and the same fact that the base ID is wrong. They split on which link in the chain is dominant.\n\nLens 1 (audio measurement) led with the 20-second decode window on the REFERENCE side, showing confirm_ref reads only 0-20s of the master and therefore reported speed \"as posted\" instead of 0.902. I verified the measurement is right but demoted it: the owner's complaint is \"you found some weird version of it\", not a transform label, and the speed reading is downstream of a base song that is wrong regardless. It is a real secondary defect on the same clip, not the cause of the grade.\n\nLens 2 (candidate pool) led with _dl_priority's `-(c[\"title_hits\"] >= 1)` partition sorting the only Hollaback-bearing row to 332 of 336 so it never got one of the 14 download slots. I confirmed the row's number myself (Shells x Hollaback, core 0.726, same=True, speed 0.8917) and then ruled the fix out as dominant: that row is a Lil Uzi Vert mashup and the clip is the plain instrumental at 0.977, so promoting it would replace one wrong version with another, and it depends on a mashup row coincidentally naming the true song. It is a good secondary ticket, not this clip's mechanism.\n\nLens 3 (ranking and gates) got the root right - the Shazam base is wrong and nothing downstream can revise it - but prescribed writing a NEW \"base-ID sanity downgrade\" in server.py. My measurement corrects that: the gate is already written. wrong_song.shazam_untrustworthy has a master_core parameter, a 0.35 audio threshold, and a docstring telling the caller to fill it, and the sole call site at server.py:863 passes None. So the finding is stronger and much smaller than lens 3 proposed: not a missing gate, a disconnected one. I proved the flip by running the real function on this clip's real inputs (None -> False, 0.024/0.147/0.340 -> True).\n\nResolution: lens 1's second mechanism and lens 3's mechanism converge on the base-ID floor, and that is the dominant one. My contribution over all three is naming the exact line (server.py:863, fifth argument) and the exact block where the audio is already downloaded and free (server.py:1697-1712), plus the measured master_core values that make the gate fire.\n\nUndetermined and honestly flagged: why the 1.0x probe returned morgue/leaveamsg at all, and whether the CORROB probe at 1.12x returned nothing or returned the same wrong title. Both require Shazam, which was off limits. Either way the design flaw is the same and is visible in the code - CORROB can only overturn the posted read by naming a rival, so silence from every counter-speed probe reads as confirmation, which is precisely the expected outcome for a vocal-free instrumental."
 },
 {
  "clip_n": 17,
  "dominant_mechanism": "The engine never learned the SECOND song's name, and without that name the candidate pool is unrankable. The clip is a SEQUENTIAL mashup: Corona \"The Rhythm of the Night\" for the first ~2.5-3s, then Don Toliver \"No Pole\" for the remaining ~16s. `_scan_windows(19.069)` returns exactly [0.0, 6.0, 12.0] at span=12 (crate_engine.py:1830), so the Corona lead-in occupies at most 3 of the 12 seconds of window 0 and 0 seconds of windows 1 and 2. Shazam therefore names No Pole in all three, `annotate_mashup` hits its tier-1 unanimity gate `if len(order) < 2:` (crate_engine.py:2589) and returns before tier 2 ever runs. MASHUP_SPAN = 4 (crate_engine.py:2490) is precisely the resolution a 3s section needs, and it is locked behind a tier-1 disagreement that a section shorter than half a scan window can never produce. With `pair` left None, the hint/pair FAST PATH at crate_engine.py:4177-4243 never arms. That lane is the only one that could have won: it runs its own `search_edits(hq, 6)[:FAST_POOL]` with FAST_POOL = 6 and bypasses `_dl_priority` entirely, and I measured that the pair queries put the correct upload at search position 1 of 130. Instead the engine fell through to the generic pool, where the right row WAS returned (by its own \"No Pole x\" query) but sat at rank 187 of 282 against max_dl = 14, because with `key_terms = {'pole'}` it and 86 other mashup rows are literally indistinguishable to `_dl_priority`. The download-gate starvation the pool and ranking lenses named is real, but it is the downstream symptom: you cannot rank for a song you have not named.",
  "root_cause_tag": "mashup_gate_unanimity",
  "evidence": "All numbers mine, this session. Zero Shazam calls (only V.verify, ffmpeg, E.search_edits, E.build_queries, E.comment_song_hints). Engine never contacted, ~/crate never written.\n\n1. THE CLIP IS SEQUENTIAL, SEAM AT ~2.5-3s. Best fp/arr/core for each 4s clip window over a sliding 20s window of the FULL references (/tmp/gdoc/c17work/coronaf.wav 264s, nopolef.wav 188s):\n   clip[ 0.0-4.0]  CORONA fp=0.906 arr=0.748 core=1.000 @170s | NOPOLE fp=0.670 arr=0.268 core=0.709 @40s\n   clip[ 1.0-5.0]  CORONA fp=0.750 arr=0.641 core=1.000 @180s | NOPOLE fp=0.784 arr=0.390 core=1.000 @50s\n   clip[ 3.0-7.0]  CORONA fp=0.742 arr=0.265 core=1.000 @180s | NOPOLE fp=0.918 arr=0.614 core=1.000 @0s\n   clip[ 6.0-10.0] CORONA fp=0.670 arr=0.193 core=0.709 @190s | NOPOLE fp=0.824 arr=0.546 core=1.000 @0s\n   clip[10.0-14.0] CORONA fp=0.730 arr=0.202 core=1.000 @60s  | NOPOLE fp=0.901 arr=0.641 core=1.000 @0s\n   clip[14.0-18.0] CORONA fp=0.659 arr=0.163 core=0.642 @0s   | NOPOLE fp=0.892 arr=0.596 core=1.000 @0s\n   Corona owns the head and collapses by 3s; No Pole is incoherent at the head and locks to t=0 from 3s on. Not layered.\n\n2. ONLY THE MASHUP UPLOAD COVERS BOTH SECTIONS.\n   clip[0.0-4.0] vs SC mashup arr=0.604 core=1.000 | vs official No Pole arr=0.208 core=0.675\n   clip[6.0-10.0] vs SC mashup arr=0.385 core=1.000 | vs official No Pole arr=0.546 core=1.000\n\n3. FULL-CLIP SCORES (V.verify on /tmp/gdoc/c17pool/):\n   sc \"this is the rhythm of the night x no pole\"  score=1.0000 core=1.0000 fp=0.8153 arr=0.6129 speed=1.0000 lag=-0.14 bass=+0.41\n   CROWNED Remix Harmony 20nAT9Gqitc               score=0.9811 core=1.0000 fp=0.5383 arr=0.6629 speed=1.0000 lag=+2.55 bass=-2.22\n   official No Pole                                score=1.0000 core=1.0000 fp=0.5482 arr=0.6819 speed=1.0000 lag=+2.60\n   Corona RotN alone                               score=0.0000 core=0.0000 fp=0.5449 arr=0.0832\n   CONTROL Bohemian Rhapsody                       score=0.1508 core=0.1797 fp=0.5108 arr=0.1629\n   The control at core 0.1797 proves core is NOT saturating on this clip, and fp separates the right row from the crown by +0.277.\n\n4. SCAN GEOMETRY. E._scan_windows(19.069) -> [0.0, 6.0, 12.0], span=12. Window 0 is ~25% Corona, windows 1 and 2 are 0%. Tier-1 unanimity is structurally guaranteed. Payload carries no `mashup`, `mashup_rejected` or `mashup_probes` key, confirming the early return.\n\n5. THE POOL, REPLAYED FAITHFULLY (282 saved rows, /tmp/gdoc/c17pool/rows.json). Recomputed key_terms={'pole'}, song_terms={'pole'}, artist_toks=['don','toliver'], credit_toks=[], cred_toks=['d1bedard'], dir_word=''. All 6 payload shelf URLs land in my reconstructed 14-slot head (FiXZA #1, Augst #2, SashaVande #5, out-here-vibin #7, moofis #10, crowned Remix Harmony #11), so the replay is faithful. TARGET key = (-1, 0, 0, 0, 0, 0, 0, -2075), rank 187 of 282. 204 rows carry 'pole'; 114 of those are artist hits; 87 are split_mashup-shaped and 0 reach the head.\n\n6. THE COUNTERFACTUAL, THE ONLY ONE THAT WORKS. E.search_edits on each rescue route, target = soundcloud.com/muhammad-raafi-pratama/this-is-the-rhythm-of-the:\n   A. hint fast path [\"rhythm of the night\", \"rhythm of the night sped up 1 11x\"]  -> 65 rows, TARGET NOT FOUND\n   B. hint in main build_queries (4 queries)                                        -> 172 rows, found (as rank-187 pool row)\n   C. PAIR fast path [\"No Pole x The Rhythm of the Night mashup\", ...]              -> 130 rows, found at POSITION 1 of 130\n   D. as-run generic [\"No Pole mashup\", \"No Pole x\"]                                -> 103 rows, found (as rank-187 pool row)\n   FAST_POOL = 6 (crate_engine.py:121), FAST_EXIT_CORE = 0.95. Route C puts the target inside the head, it verifies at core 1.000, and the lane returns decisive.\n\n7. THE ZERO-PROBE TRIGGER THAT WOULD HAVE CAUGHT IT. Each 4s clip window vs the CROWNED candidate:\n   clip[ 0.0-4.0] arr=0.189 fp=0.662 core=0.658   <- dissents\n   clip[ 4.0-8.0] arr=0.671 fp=0.938 core=1.000\n   clip[ 8.0-12.0] arr=0.534 fp=0.935 core=1.000\n   clip[12.0-16.0] arr=0.619 fp=0.901 core=1.000\n   clip[15.0-19.0] arr=0.712 fp=0.889 core=1.000\n   One isolated window at 2.8x-3.8x below the rest. Pure ffmpeg + verify, no Shazam.\n\n8. SEPARATE, NON-CAUSAL DEFECT. edit_label \"sped up ~1.11x\" with edit_certain true, while the same payload carries speed_measured 1.0024, and I measure speed 1.0000 for both the mashup and the official master against the clip. The label is wrong, but it did not cause the miss: dir_word is empty in the replay and the target's title carries no speed word, so the label never touched its priority.\n\n9. CAPTION LANE, MEASURED. Payload top-level desc = \"\" while creator.caption = \"rhythm of the night #connorbedard #fyp #mynhlplayer #viral #trending\". server.py:612-617 reads only src[\"desc\"]. And even fed the real caption, E.comment_song_hints returns []: [\"rhythm of the night #connorbedard...\"] -> [], [\"rhythm of the night\"] -> [], [\"song: rhythm of the night\"] -> ['rhythm of the night']. Two holes, and per route A above, plugging them still would not have fixed this clip.",
  "confidence": "high",
  "fix": "Add a probe-free dissent test after the crown is picked, and let ONLY that dissent unlock the tier-2 mashup probes that crate_engine.py:2589 currently gates behind tier-1 disagreement. Do not touch the tier-1 gate itself and do not reintroduce the blind hidden-layer branch (the 48-combination ablation that killed it is still correct for the layered case it was testing).\n\nConcretely: once a candidate is crowned, cut the clip into MASHUP_SPAN (4s) windows and V.verify each against the crowned candidate's audio, which is already downloaded and on disk. Flag a window whose arr falls to roughly a third of the median of the others. On this clip that is unambiguous and costs zero Shazam probes: clip[0-4] arr 0.189 against 0.534-0.712 for every other window. Only on that flag, spend the existing MASHUP_BUDGET tier-2 probes on the flagged region, which is exactly what MASHUP_SPAN = 4 was built to resolve. That yields \"The Rhythm of the Night\", which yields `pair`, which arms `_pair_queries` and the fast path at crate_engine.py:4177 - and I measured that lane returns the correct upload at search position 1 of 130, inside FAST_POOL = 6, where it verifies at core 1.000 / fp 0.8153 and exits decisive.\n\nThis is surgical in the way that matters: on a single-song clip every window matches the crown, nothing fires, no probe is spent, and the five regression crowns cannot move. It also respects the throttle rule, since probes are spent only after a measured dissent rather than speculatively.\n\nDo NOT ship the three fixes the pool and ranking lenses proposed. I tested all of them against the saved 282-row pool and every one fails:\n  - exempting split_mashup-shaped rows from the `-_artist_hit` rung -> rank 160 of 282, still far outside max_dl = 14\n  - seeding genre_hit from the query that returned the row -> rank 180 of 282, still outside\n  - an `_sc_quota`-style `_mashup_quota` reserving 2 of 14 slots -> selects \"Hamza Oscar de la Hoya x No Pole\" (203 plays) and \"Addison Rae - fame is a gun x no pole\" (1,340,784 plays), NOT the target\nThey fail for one shared reason: 87 mashup-shaped rows carry 'pole', and with key_terms = {'pole'} nothing in `_dl_priority` can tell them apart, so the tier degenerates to plays and the 2,075-play right answer loses. Any download-side fix is spending slots blind until the second song has a name. Fix the naming, and the existing lanes already work.\n\nWorth a separate low-priority ticket, not this one: server.py:612-617 reads src[\"desc\"] (empty here) while creator.caption holds the real caption, and comment_song_hints rejects a bare caption line with no cue word. Both are real holes, but route A above shows patching them would not have saved clip 17.",
  "disagreement": "Five disagreements, all resolved by my own measurement rather than by vote.\n\n1. CORE SATURATION. The audio lens made it fault #1, claiming ARR_HI = 0.45 flattens the score. The pool and ranking lenses denied it. The deniers are right, and the audio lens's own saturation count was measuring the wrong thing (it swept a 20s query over long references, which is a different question). My control: Bohemian Rhapsody scores core 0.1797 against this clip, and the correct row separates from the crown by fp 0.8153 vs 0.5383. The discriminating signal existed on this clip. It was never asked for, because the row was never downloaded. Not the mechanism here.\n\n2. SEQUENTIAL OR LAYERED. The audio lens said sequential, Corona first. The pool and ranking lenses both said layered, No Pole a bed running throughout with Corona overlaid, and cited Corona-alone scores of 0.164/0.000/0.427 as proof. The audio lens is right and the other two tested at the wrong resolution: whole-clip or 20s windows drown a 3s section. At 4s resolution clip[0-4] matches Corona at fp 0.906 / arr 0.748 and matches No Pole at only arr 0.268 / core 0.709, and by clip[3-7] that reverses completely. It is sequential, seam at ~2.5-3s. This matters, because it is exactly why tier-1 can never fire: a 3s lead-in cannot be the majority of a 12s window.\n\n3. THE DOMINANT MECHANISM. Two lenses out of three voted for `_dl_priority` starvation, which would have carried a vote. I ran their fixes against the saved pool and all three fail (160/282, 180/282, and a quota that grabs two wrong mashups). The reason they fail is the tell: 87 mashup rows are indistinguishable under key_terms = {'pole'}. Starvation is what the miss looks like from the download gate, not what caused it. I demoted it to a symptom.\n\n4. THE CAPTION LANE. The pool lens presented it as an independent rescue that \"either fix alone\" would have solved. Measured false for this clip: the fast path built from hint \"rhythm of the night\" returns 65 rows and the target is not among them. The hint only reaches the main pool, where it is buried exactly as before. Real defect, wrong clip.\n\n5. WHAT ACTUALLY WORKS. Only the audio lens named the annotate_mashup gate as a primary fault, and it was the minority position. I promoted it, because it is the only lever I could measure as sufficient: supplying `pair` puts the correct upload at position 1 of 130 in the fast path's own search, inside FAST_POOL = 6, above FAST_EXIT_CORE. Every other route I tested leaves it outside the download head. The ranking lens's second-barrier claim (rank_key's `_artist_own` tier and `_edit_sig` dedup would refuse it even if downloaded) is untested by me and moot on the fast path, which returns before rank_key runs."
 },
 {
  "clip_n": 15,
  "dominant_mechanism": "The crown's score is manufactured by verify.py's own speed-warp, and it contains no recording-specific evidence at all. _speed_xcorr (verify.py:156) searches +/-2.0x and returns 0.8340 for clip-vs-Fenzi. That reading is provably false: Fenzi's upload and the plain \"Rio Da Yung OG - Couple Racks\" measure speed 1.0000 against each other (arr 0.429, reversed control 0.056), and the clip measures 1.0000 against that same original (arr 0.382, reversed 0.084), so transitively the clip is 1.0000 against Fenzi. verify then resamples the candidate by that bogus 0.834 BEFORE fingerprinting (verify.py:472, `xk_sm = _resample_by(xk, speed)`), and the warp pumps chromaprint from 0.632 at sp 1.00 to 0.771 at sp 0.84 (peak of a 0.70-1.32 sweep). Then `core = max(_norm(fp, 0.55, 0.72), _norm(arr, 0.10, 0.45))` scores the crown 0.9873 entirely on that inflated fp path while arr sits at 0.069, BELOW ARR_LO, i.e. literally zero arrangement corroboration, and the `same`-gate's anti-collision guard cannot fire because it only covers FP_LO <= fp < FP_CONF and 0.718 is above FP_CONF 0.66 (\"above FP_CONF chromaprint is trusted alone\"). The project's own reversal null then convicts it: crown forward 0.9873 / reversed 0.7372, ratio 1.34, inside the wrong-crown band (ur LYING 1.31; genuine matches 1.48 and 2.26), and at off 30 reversed actually scores HIGHER (0.9339 vs 0.8826). FP_LO as \"the different-recording ceiling\" is simply false on this bass-heavy low-transient hoodtrap content: a genuinely different song, AT's \"lean lean (AT mixxx)\", reads fp 0.702-0.716 against this clip, higher than the crown's own unwarped 0.632. So the label \"slowed ~0.84x\" is the artifact printed as a fact, and speed_confirmed/speed_refs 2 is the same Fenzi mixxx mirrored on SoundCloud (0.8358) and YouTube (0.8346).",
  "root_cause_tag": "core_saturation",
  "evidence": "All measured by me on downloaded audio. No Shazam, no engine calls. Clip via E.get_source, 52.924s. Candidates: fenzi_sc 108.11s, bobs_orig (soundcloud/bobs-904737456/rio-da-yung-og-couple-racks) 120.19s, at_lean 142.71s. Fenzi YouTube would not download (HTTP 403).\n\nREVERSAL NULL, V.verify, 25s candidate windows, clip_ctx shared (forward core -> reversed core):\n  fenzi_sc off 10  0.9873 -> 0.7372  ratio 1.34   (fp 0.718 -> 0.675, arr 0.069 -> 0.131)\n  fenzi_sc off 15  0.9328 -> 0.6955  ratio 1.34\n  fenzi_sc off 30  0.8826 -> 0.9339  ratio 0.95   (reversed WINS)\n  fenzi_sc off 35  0.9413 -> 0.6442  ratio 1.46\n  fenzi_sc off 40  0.8900 -> 0.5566  ratio 1.60\n  bobs_orig off 35 0.8057 -> 0.0000  ratio inf    (arr 0.382 -> 0.084)\n  bobs_orig off 40 0.7567 -> 0.0619  ratio 12.2\n  bobs_orig off 45 0.7662 -> 0.1774  ratio 4.32\n  at_lean  off 120 0.8929 -> 0.9716  ratio 0.92   (reversed WINS, and it is a different song)\nOnly bobs_orig carries recording-specific evidence.\n\nCORE IS PATH-SPLIT, NOT COMPARABLE. Crown: fp 0.718 -> _norm 0.988, arr 0.069 -> _norm 0.000. bobs_orig: fp 0.514 -> _norm 0.000, arr 0.382 -> _norm 0.806. The max() ranks an fp-only 0.988 above an arr-only 0.806.\n\nTHE 0.834 IS AN ARTIFACT, three ways:\n (a) cand-vs-cand: bobs_orig[30:50] vs fenzi_sc[35] core 0.9389, arr 0.429, speed 1.0000; reversed control collapses to core 0.0000, arr 0.056. Same song, same rate.\n (b) chromaprint vs resample factor, 0.70-1.32 sweep: fenzi peaks sp 0.84 fp 0.771, and reads 0.632 at sp 1.00. bobs_orig never exceeds 0.628 at any speed. at_lean peaks at sp 0.98-1.00 (0.716/0.711) - the different song beats the crown's honest value.\n (c) onset-envelope tempo-ratio scan (rhythm only, no spectral pitch), 130 stretch factors per candidate: bobs_orig peaks r 0.2857 at s=1.000, offset 30.0s; fenzi_sc peaks r 0.1233 at s=1.000 and reads only 0.0917 at s=0.834; at_lean peaks r 0.1274 at s=0.900. The bobs peak is 2.3x above every other reading in the whole scan.\n\nAT THE HONEST 1.0x SPEED (arr scan at 1s stride, best window, with reversal control):\n  bobs_orig arr 0.4333 at off 34s, reversed 0.1132, ratio 3.83  -> _norm 0.952\n  fenzi_sc  arr 0.3767 at off 73s, reversed 0.2555, ratio 1.47  -> _norm 0.791\n  at_lean   arr 0.3256 at off 121s, reversed 0.3270, ratio 1.00 -> noise\nSo unwarped, the plain original beats the crown on both magnitude and null ratio.\n\nLONG-FINGERPRINT CHECK (full 52.9s clip fp, 406 frames, candidate fp length 250): fenzi 0.6927 at sp 0.834, at_lean 0.6356 at sp 1.0, bobs 0.5108. Chromaprint does not separate true from false on this content at any length.\n\nSATURATION CONTROL: full-clip 256-band detailed spectrogram correlation gives fenzi 0.5342, at_lean 0.5459, bobs 0.4796, and the clip against ITS OWN TIME REVERSAL gives 0.5325. The spectral-shape metric is fully saturated here.\n\nTHE RIGHT VERSION IS NOT PUBLISHED. Enumerated AT's SoundCloud (soundcloud.com/user-226656196/tracks) with yt-dlp: 238 tracks, every one an \"(AT mixxx)\", none titled Couple Racks (nearest: \"zip of crack (AT mixxx)\", \"rio da yung og x rmc mike - crack baby (AT mixxx)\"). The sound page's own comment hint in the payload is \"Yall ever find it\". The honest output for this clip was no confident crown, not a different one.",
  "confidence": "high",
  "fix": "verify.py, the combine step (the `core = max(_norm(fp, FP_LO, FP_HI), _norm(arr, ARR_LO, ARR_HI))` line, ~540): when the candidate had to be resampled to line up, the chromaprint path may not carry `core` alone. Concretely, if `abs(log2(speed)) > log2(1.05)` and `arr < ARR_CORROB` (0.25, the constant already defined in this file for exactly this \"borderline fp needs arrangement corroboration\" job), set `core = _norm(arr, ARR_LO, ARR_HI)` instead of the max.\n\nMeasured against every window I scored on this clip: fenzi off10 0.9882 -> 0.0000, off15 0.9353 -> 0.0943, off30 0.8824 -> 0.0771, off35 0.9412 -> 0.0000, off40 0.8882 -> 0.0229, off85 0.8353 -> 0.1943. Every Fenzi window falls below CORE_KEEP 0.50, so the wrong crown and the \"slowed ~0.84x\" label both disappear. Nothing at speed 1.0 moves: bobs_orig off35 stays 0.8057, off45 stays 0.7657, at_lean off120 stays 0.8941. So no unwarped candidate on any clip changes, and the blast radius is confined to candidates the engine had to pitch-shift more than 5 percent that also have essentially no arrangement agreement. This file's own docstring puts a real slowed-plus-bass match at arr ~0.86, which is 3.4x above the 0.25 bar, so a genuine slowed edit is untouched. It also honours the hard rule that bass and speed nudge rather than reject: this does not let a transform cue veto a candidate, it refuses to let a transform WARP manufacture the core in the first place.\n\nNecessary companion, not part of the surgical change: extend server.py's `_time_reversed_null` from the crown alone to every candidate, or at_lean inherits the crown at core 0.8941 while scoring HIGHER reversed (0.9716 vs 0.8929).\n\nDo NOT ship this without the five-clip gate in references/accuracy.md, twice. It changes core on every speed-warped candidate.",
  "disagreement": "All three lenses agreed the crown is \"the right recording at the wrong speed\" and disagreed only on which upload should have won. I measured and none of them is right about the crown.\n\nLens 1 said AT's \"rio da yung og - lean lean (AT mixxx)\" is the answer at core 1.000 offset 125s. Refuted: at off 120 that file scores forward 0.8929 and REVERSED 0.9716, so its score carries zero recording-specific evidence, and it is a different song. Its fp of 0.702-0.716 is the same collision that inflates the crown, and it peaks at sp 0.98-1.00 which is why it reads speed 1.0000. Lens 1's stated saturation check (head-only score spread of 0.25) is not a saturation test; the project's own control is time reversal, and this candidate fails it worse than anything else in the pool.\n\nLenses 2 and 3 both named the plain original (soundcloud/bobs-904737456) and blamed pool admission - lens 2 the `_dl_priority` boolean title-hit key, lens 3 the 20s decode window. I confirm the recording: it is the only candidate that passes the reversal null (0.8057 -> 0.0000; 0.4333 -> 0.1132 at its true 34s window, ratio 3.83) and the only one the rhythm scan locks to (r 0.2857 at s=1.000, off 30.0s, 2.3x above every other reading). But their fixes do not flip the crown, which is the thing both of them claim. rank_key's `_artist_own` tier (crate_engine.py:5011, checked at rank_key index 6) sits ABOVE strong_core, above the CORE_SAME/CORE_MERIT band and above speed_exact. artist_toks resolves to [\"fenzi\"] from Shazam's base_artist, \"Couple Racks Mixxx (Fenzi Mixxx)\" carries it, \"Rio Da Yung OG - Couple Racks\" does not, and _ARTIST_NAMEDROP_ONLY does not match the Fenzi title. So even with the original downloaded, window-slid and scoring core 0.806 at speed 1.0000, it ranks below Fenzi. I also checked the tier above it, `_creator_source`: cred_toks is [\"producedbyat\"] (credit_author \"AT\" is dropped by the 4-char minimum), which no candidate's title or uploader carries, so that tier ties and cannot rescue anything either. Their two mechanisms are real defects and worth their tickets, but neither is dominant here.\n\nLens 3 alone got the \"0.834 is a spectral alias\" call right, and I confirm it independently through the cand-vs-cand path it did not use (bobs vs fenzi core 0.9389 arr 0.429 speed 1.0000, reversed 0.0000). Where I part from lens 3 is its primary fix: it puts the decode window first, and the decode window is not what decided this clip.\n\nResolved by measurement in favour of none of the three: the dominant mechanism is that the crown's core is a chromaprint collision manufactured by verify's own free speed warp, on content where chromaprint does not discriminate at all. Left unfixed, every proposed pool fix just delivers better candidates into a scorer that will still prefer the collision."
 },
 {
  "clip_n": 24,
  "dominant_mechanism": "The plain official master is FOUND by the search and then structurally denied a download slot, so it was never in the crown race and the crown could only ever be an edit title.\n\n`_dl_priority` (crate_engine.py, defined 4449, return tuple 4463-4470) puts `-edit_titled` at position 5 and `-plays` LAST at position 8, and only the head of that sort is fetched (`wave1 = _sc_quota(sorted(cands, key=_dl_priority), max_dl)[:max_dl]`, line 4499, max_dl=14; wave 2 re-uses the same sorted head at 4606). I rebuilt the pool from the engine's own functions: 518 rows, and Katy Perry's own \"Katy Perry - Roar\" (CevxZvSJLk8, 4,367,716,543 plays, 270s) sorts to index 75. 74 of the 75 rows ahead of it are edit-titled. All 14 rows that actually get downloaded are edit-titled, down to a 141-play SoundCloud \"Dj Carlo_Roar-Katy Perry_Ft. Fatman Scoop(BassBoost)_Remix\" taking a slot the 4.37-billion-play master cannot get.\n\nConsequence chain: nothing in `keep` satisfies `is_official_original` (4852), so rank_key's PLAIN-ORIGINAL PREFERENCE tier at 5216 - which is in exactly the right mode here, because `_clip_is_edit` is False for edit_label \"as posted\" (5052) - has no official row to prefer. Every remaining tier is decided among edits, and the speed lock correctly picks the only 1.0x upload in the pool, which happens to be titled \"(Bass Boosted)\". The engine's own payload already says speed \"as posted\" and edit_label \"as posted\" while the headline reads \"(Bass Boosted)\"; that contradiction is the owner's complaint.\n\nSECOND, LATENT GATE - it did not fire on this run (the file was never fetched) but it defeats a pool-only fix: `dl_clip` decodes `--download-sections *0-20` (3847) and `verify()` re-caps independently at `_decode(cand_path, seconds=20)` (verify.py:433/87). The official's first 20s is the spoken plane-crash/jungle intro. I measured core 0.7163 there against 1.0000 at t=30/40/50/90/100/110/120. 0.7163 sits below CORE_SAME 0.95, dropping it to core band 1 while the crowned upload holds band 0 - and that band tier (5183) ranks ABOVE speed_exact, bass_off and the plain-original preference. This is hard-rules ticket #10 landing on the one candidate that was the right answer.",
  "root_cause_tag": "original_never_downloaded",
  "evidence": "POOL, my own run (E.build_queries + E.search_edits, no Shazam):\n  14 queries, q0 \"Roar\", q1 \"Katy Perry Roar\". 518 rows in 4.8s.\n  Official row present: {'title': 'Katy Perry - Roar', 'uploader': 'Katy Perry', 'plays': 4367716543, 'duration': 270.0}\n  Replicated _dl_priority with this clip's real inputs (key_terms {'roar','audio','gf0rc3d'}, song_terms {'roar'}, artist_toks ['katy','perry'], cred_toks ['gf0rc3d'], credit_toks [], dir_word \"\"):\n    OFFICIAL rank 75 of 518, key (-1, -1, 0, 0, 0, 0, 0, -4367716543)\n    rows ahead: 75, of which edit_titled: 74; min plays among them: 141\n    wave1 = _sc_quota(sorted,14)[:14] -> 14/14 edit_titled, official NOT in wave1\n    same rows sorted with -plays leading instead: official rank 0\n\nAUDIO, my own run (V.prepare_clip once, V.verify per window; clip fetched with IG_LOCAL_SESSION=1, clip_tilt 26.93):\n  OFFICIAL first 20s (exactly what dl_clip hands verify): core 0.7163  fp 0.573  arr 0.351  speed 1.0000  bass +5.28  cand_tilt 21.65  same True\n  CROWN MusicPark 20s:                                    core 1.0000  fp 0.775  arr 0.274  speed 1.0000  bass +0.88  cand_tilt 26.05  same True\n  Slide across the full 269.1s official, 25s windows every 10s:\n    t=0 0.7165 | 10 0.8261 | 20 0.9206 | 30 1.0000 | 40 1.0000 | 50 1.0000 | 60 0.6485 | 70 0.6151 | 80 0.6090 | 90 1.0000 | 100 1.0000 (arr 0.753, the clip's true position) | 110 1.0000 | 120 1.0000 | 130 0.9730 | 150 0.8775 | 170 0.4425 | 180 0.2512 | 240 0.0343\n    speed reads 1.0000 at EVERY offset. BEST window t=30, core 1.0000.\n\nTHE EXISTING LONG-DECODE PATH CANNOT RESCUE IT (no lens tested this):\n  crate_engine.py:3079 sets dl_seconds=180 for long uploads, but that is inside creator_search only, and _download_and_score calls `_verify.verify(clip_audio, got, clip_ctx=clip_ctx)` with no `seconds`, so verify re-truncates to 20s regardless. Measured:\n    E.dl_clip(official, seconds=180) produced a file only 33.5s long (the byte-range direct path under its 6s budget)\n    verify(default seconds=20) on that 180s-requested file -> core 0.7163\n    verify(seconds=180) on the same file                   -> core 0.7139  (WORSE than the 20s read)\n  Decoding MORE does not help. Locating the clip does. That is exactly what ticket #10 says.\n\nCOUNTERFACTUAL TIER WALK, official inserted at its measured aligned numbers (core 1.000, vspeed 1.0, cand_tilt 22.39) - I read every predicate in crate_engine.py and checked the two that could separate them:\n  is_official_original('Katy Perry - Roar', uploader 'Katy Perry') -> True (ba == up, EDIT_WORDS no match)\n  is_official_original(crown) -> False (EDIT_WORDS matches \"Bass Boosted\")\n  _artist_own: TRUE for BOTH (\"katy\" is in both titles, _ARTIST_NAMEDROP_ONLY matches neither) -> ties, so it cannot rescue the official above the core band\n  bassy with the official present: official_tilts = [22.39], bassy_baseline = max(clip 26.93, 22.39) = 26.93; max(fam) 27.53 - 26.93 = 0.60 dB < BASS_STRIP_GAP 6.0 -> bassy False -> bass_off 0 for every row (the baseline takes the MAX, so adding the official cannot flip bassy on)\n  _edit_sig buckets differ (official tilt bucket 11, crown 13) so the shelf dedup does not collapse it\n  Every tier ties down to the plain-original preference, where official 0 beats crown 1. Official wins - but ONLY at core 1.000. At the first-20s 0.7163 it loses two tiers earlier at the core band.\n\nCONSTANTS: CORE_KEEP 0.50, CORE_EDIT 0.62, CORE_SAME 0.95, CORE_MERIT 0.38, BASS_STRIP_GAP 6.0, max_dl 14.\n\nNo Shazam call was made at any point. No file in ~/crate was modified. Working files: /tmp/gdoc/c24judge/{rows.json,rank.py,meas.json,meas.py,ds.py}.",
  "confidence": "high",
  "fix": "Add an official-master lane: ONE reserved download slot, scored on an aligned window.\n\nConcretely, after the wave-1 head is built at crate_engine.py:4499, APPEND (do not displace - the same shape `_creator_extra` uses at 4649) the single highest-play row satisfying `is_official_original` against the confirmed base artist, and only when the head contains none. Appending rather than reordering keeps the current 14 scored, so this cannot change any crown on a clip that already had an official row in its head.\n\nThat slot alone is NOT sufficient and must not ship alone: measured, the appended row reads core 0.7163 head-to-head at t=0 and loses at the core band tier (5183), two tiers above the plain-original preference it needs to reach. So score that one appended row with a window slide (25s windows across the fetched audio, keep the best core) rather than a t=0 verify. Measured, that is the difference between 0.7163 and 1.0000 on the correct answer.\n\nTwo things NOT to do, both measured:\n- Do not move `-plays` above `-edit_titled` in `_dl_priority`. That is the exact regression the function's own docstring at 4381-4390 was written to prevent, and it would cost the niche-edit clips.\n- Do not fix the window by raising `dl_seconds`. `verify()` re-caps at `_decode(cand_path, seconds=20)` independently of the download, dl_clip(seconds=180) delivered only 33.5s on this YouTube URL anyway, and verify(seconds=180) on that file read 0.7139, worse than the 20s read. Slide, do not widen.\n\nPresentation, separate and cheap: when `_clip_is_edit` is False the card should not headline the crowned upload's transform-word title. This payload already carries edit_label \"as posted\", speed \"as posted\", and `title_overclaims` \"Titled bass boosted, but it measures 0.9 dB off the clip, which is the same EQ\" - the engine computed the right answer and then printed a title that contradicts it. Show \"as posted\" and demote the upload title to provenance. That would have blunted this specific complaint even before the pool fix lands.",
  "disagreement": "Lenses 2 and 3 both named `_dl_priority` as the mechanism with the 20s window as a required second fix. Lens 1 named the pool too but put its weight elsewhere: it claimed core \"carries zero information on this clip\" (1.000 at 11 of 22 windows), so that speed_exact alone crowned the winner, and framed the plain-original preference as unreachable by construction.\n\nI resolved this by measuring rather than voting, and lens 1's strong form is refuted. My own 25-window slide across the full 269.1s master runs core from 0.0343 to 1.0000, not a saturated 1.000 everywhere - and at the ONE window the engine actually uses (t=0) it reads 0.7163, which is discriminating and points the wrong way. Core saturation is a real background condition (three shelf rows do tie at exactly 1.000, and that tie is why speed_exact ended up deciding among the edits that were present) but it is not the cause: the correct answer was never in the comparison at all. Lens 1's own bass-window artifact finding at verify.py:505 is real - `cand_tilt` is taken on the whole speed-matched 20s, not the lag-aligned window computed 8 lines earlier - but every value in its measured 0.50-5.20 dB range sits under BASS_STRIP_GAP 6.0, so it changed no wording and no ranking here. Correct, and not this clip's mechanism.\n\nLens 3's counterfactual tier walk asserted `_artist_own` ties at 0 for both rows. I checked that specifically because if it did NOT tie, the official would win above the core band and the 20s window would be irrelevant. It does tie: both titles contain \"katy\" and `_ARTIST_NAMEDROP_ONLY` matches neither, so `_artist_own` is True for the Music Park upload as well. Lens 3 was right and the second gate is genuinely load-bearing.\n\nI also checked one thing all three lenses computed with the official ABSENT: whether adding the official to `keep` flips `bassy` on and lets bass_off (tier 9) beat the plain-original preference (tier 11). It does not - `bassy_baseline` takes max(clip_tilt, official_tilt) and the clip's 26.93 already exceeds the official's 22.39, so the gap stays 0.60 dB against a 6.0 threshold. The counterfactual holds."
 },
 {
  "clip_n": 20,
  "dominant_mechanism": "Decode-window miss on a long upload, which handed rank_key a wrong core and cost the owner's link the top core band. The owner's pick is 158.57s and the clip's audio sits at 115-135s inside it. The engine fetches only the candidate's first 20s (dl_clip default seconds=20, crate_engine.py:3806, called at :4041) and verify decodes 20s (verify.py:_decode / verify(seconds=20)), so the row was scored on candidate seconds 0-20, roughly 100s away from the clip's section. Measured true value with the clip window left exactly where the engine puts it (clip[0:20]): core 1.0000, arr 0.8691, speed 1.0000 at candidate offset 115. Measured shipped value: core 0.8559, arr 0.3996. That 0.15 error crosses a band boundary. rank_key's core band (0 if core>=CORE_SAME 0.95 else 1 if >=CORE_MERIT 0.38 else 2, crate_engine.py:5188-5189) puts the owner's row in band 1 and the YouTube row in band 0, and the band is evaluated BEFORE speed_exact (crate_engine.py:5190), so the owner's perfect speed lock (vspeed 1.0017, speed_exact 0) is never weighed against the YouTube row's vspeed 1.2019 (speed_exact 8). The YouTube row takes verified[0], _crown_tempo_mismatch refuses it for exactly that 20% gap, and `top = verified[0]` at server.py:1647 has no walk to verified[1], so exact=null and the owner is shown no version at all. Amplifier, not cause: ARR_HI=0.45 (verify.py:63) turned the YouTube row's head arr of 0.4867 into core 1.0000 (_norm gives 1.105, clipped) while the owner's 0.3996 gave 0.8560, so a 0.087 arr gap on audio that is not the clip's section bought the band. The rescue that exists for this exact failure could not fire: dl_seconds=180 is set only inside creator_search (crate_engine.py:3078-3082) and the _slide block (crate_engine.py:4716-4756) is gated on creator_upload or creator_link, core < CORE_KEEP 0.50, and spectral >= 0.85. The sound is \"original sound - matt.productions\", the candidates came from the main search lane, and from_creator / from_creator_link / aligned_at are null on all six.",
  "root_cause_tag": "decode_window",
  "evidence": "All measured by me in /tmp/gdoc/c20J, no Shazam, no engine call. Clip via E.get_source, 48.33s.\n\nTrue durations, plain yt-dlp: sc_speeds 158.569, sc_slowed 158.569, yt_win 174.173, sc_puppxt 160.380, sc_cyan 165.396, sc_fen 144.567. These match what SoundCloud's own search rows report through the engine's _run_search format (fen1xxy 144.555, CyanTriton 165.375), so the engine already holds every candidate's duration.\n\nR1, head-20 vs head-20, i.e. exactly what the engine does today. Reproduces the payload to 3 decimals:\n  yt_win     core 1.0000 score 0.8115 speed 1.1991 arr 0.4867 fp 0.5821 spec 0.7414 bass -3.77   (payload 1.000 / 1.2019 / -3.8)\n  sc_speeds  core 0.8559 score 0.8029 speed 1.0000 arr 0.3996 fp 0.5451 spec 0.7833 bass -2.71   (payload 0.848 / 1.0017 / -2.6)\n  sc_slowed  core 0.8470 speed 1.0000 arr 0.3965 | sc_puppxt 0.7112 / 0.7652 | sc_cyan 0.5170 / 0.8420 | sc_fen 0.0996 / 1.1003\n\nR2, slide the candidate only, clip context frozen at clip[0:20], 25s windows, 5s step:\n  sc_speeds  off 105 core 0.0986 arr 0.1345 | off 110 core 0.8358 arr 0.3925 | off 115 core 1.0000 score 0.9890 arr 0.8691 speed 1.0000 | off 120 core 1.0000 arr 0.8684 speed 1.0000 | off 125 core 1.0000 arr 0.8678\n  yt_win     best core 1.0000 at off 5, speed 1.1991; best arr only 0.5419-0.5515\n  sc_slowed  never rises: best core 0.8470 at off 0\nThe owner's upload is the only row in the pool that reads speed 1.0000 at a genuine alignment, and its arr 0.8691 is 1.58x the best any rival reaches.\n\nThe engine's own 15s slide grid, range(15, dur-20, 15), lands on offset 120, which measures core 1.0000 / arr 0.8684 / speed 1.0000. The existing machinery would find it if it were allowed to run.\n\nRank arithmetic, computed from the live constants:\n  yt_win    core 1.000 -> band 0 ; |log2(1.2019)| = 0.2653 -> speed_exact 8\n  sc_speeds core 0.848 -> band 1 ; |log2(1.0017)| = 0.0025 -> speed_exact 0\n  band separates first, so speed_exact is never consulted. Slid: sc_speeds core 1.000 -> band 0, ties yt_win, wins 0 vs 8.\n\nSaturation arithmetic from verify.py:63, on the head-20 arrs above: _norm(0.4867, 0.10, 0.45) = 1.1049 clipped to 1.0000; _norm(0.3996, ...) = 0.8560; both fp values sit under FP_LO 0.55 so the fp path contributes nothing. A 0.087 arr gap became the band.\n\nLive crown gates, called on the real payload rows (server.py, _TEMPO_TOL 0.06, _SOFT_TEMPO_TOL 0.0):\n  yt_win    -> _crown_tempo_mismatch: \"clip plays 20% faster than this upload, so it is a different edit of the same recording\" (byte-identical to res[\"crown_rejected\"]); _crown_contradicts also refuses it.\n  sc_speeds -> _crown_tempo_mismatch None, _crown_contradicts None, _SPEED_CLAIM match False. It is the ONLY row of the six that clears every crown gate.\n  sc_slowed -> _crown_contradicts refuses (\"candidate titles itself a speed edit\").\n\nCorroboration that the clip itself is not the problem. clip[24:44] vs a clean 130.45s original: core 1.0000, arr 0.9236, speed 1.0000, at original offset 0. The clip is at true speed, \"as posted\" is correct, and the TikTok sound loops, which is why the clip's head is the track's tail and lands at 115s inside a 158.6s upload.\n\nThe two sibling uploads share a byte-identical first 20s: verify(sc_speeds[0:20], sc_slowed[0:20]) = core 1.0000, fp 0.9993, arr 0.9917, spec 0.9999, speed 1.0000, bass +0.02, while at [60:80] they diverge at speed 1.1108. The 20s the engine scores carries zero information separating the right edit from the wrong one.\n\nCost of the fix, measured on this URL: dl_clip seconds=20 takes 1.59s, seconds=180 takes 3.05s, and a 9-offset slide takes 0.76s.",
  "confidence": "high",
  "fix": "Give long uploads the slide that already exists, gated on duration instead of creator provenance, and unblock the download that feeds it.\n\n1. crate_engine.py:3078-3082. The rule `if float(c.get(\"duration\") or 0) > 45: c[\"dl_seconds\"] = 180` lives inside creator_search only. Apply it to every candidate in the main lane. _run_search (crate_engine.py:3401) already carries `duration` on every row, verified live: the engine's own scsearch returns 144.555, 165.375, 130.507 for these exact tracks. No new fact is needed.\n\n2. crate_engine.py:4716. Widen _align's admission from `(c.get(\"creator_upload\") or c.get(\"creator_link\"))` to also admit `_dur_s(c) > 45`. For that duration route only, raise the ceiling from `core < CORE_KEEP` (0.50) to `core < CORE_SAME` (0.95), because the owner's row sits at 0.848 and is excluded today even with the creator gate removed; and lower the `spectral >= 0.85` evidence floor to about 0.72, because this row's head spectral is 0.7833 and its spectral at the true offset is 0.7244, so 0.85 would block the slide. Keep the [:3] cap and keep \"decisive or nothing at CORE_EDIT\" verbatim. The duration excess is a first-party fact about the file, the same class of evidence as \"the creator posted it\", so this does not become the blanket re-decode that was reverted.\n\n3. BLOCKER that must ship with it, or the slide reads the wrong audio. dl_clip cannot return more than 125s from SoundCloud. _range_to_wav caps its range at `want = min(..., 2_000_000)` bytes (crate_engine.py:3757), which is exactly 125.0s at 128 kbps; it writes `dst`, then raises \"short decode 125.0s < 180s\" without removing it; dl_clip catches, falls through to yt-dlp whose `-o` resolves to that same existing path, and yt-dlp prints \"[download] probe.wav has already been downloaded\", exits 0, so dl_clip returns the truncation as a success. Measured: E.dl_clip(sc_speeds, seconds=180) returns 124.97s into a fresh path, twice, while the identical bare subprocess returns 158.569s. At 124.97s the clip's section is clipped off and the slide reads core 0.3093 at offset 115 instead of 1.0000. Fix: remove `dst` before falling through (or pass --force-overwrites), and scale the 2 MB cap with `seconds`. This silently caps the existing creator-lane rescue today as well.\n\nExpected result on this clip: sc_speeds 0.848 -> 1.0000 at offset 115-120 (the engine's own 15s grid hits 120), joins band 0, ties yt_win there, wins on speed_exact 0 vs 8, clears all three crown gates (verified by calling them), and the crown becomes https://soundcloud.com/k6edxqtrlpcq/esdeekid-mist-speeds-reverb.\n\nFile separately, do not couple: ARR_HI = 0.45 (verify.py:63) is below the arr a genuine alignment produces here (0.8691), so anything from a marginal 0.46 to a near-perfect 0.87 reads core 1.000 and the ranker cannot see the 1.58x gap. Raising it, or carrying raw arr as a tiebreak under the band, moves every core in the corpus and needs its own regression run. Also worth its own ticket: server.py:1647 crowns index 0 or nothing, so one tempo refusal at the top discards a shelf whose second row was gate-clean.\n\nRegression gate: the five clips in references/accuracy.md, crowns unchanged, run twice, not while the owner is demoing.",
  "disagreement": "Lens 2 and lens 1 flatly contradicted each other on whether sliding the candidate alone rescues the owner's upload. I settled it by measuring, and lens 1 is right: with the clip context frozen at clip[0:20], sc_speeds reads core 1.0000 / arr 0.8691 / speed 1.0000 at candidate offset 115. Lens 2's negative result has a traceable cause. It used the engine's own dl_clip, which returns a 124.97s file for this 158.57s upload (the 2 MB range cap at crate_engine.py:3757 plus yt-dlp declining to overwrite the file _range_to_wav left behind), and it slid only offsets 5 to 100, so it never reached the 110-125 region where the match lives. Its duration table is wrong throughout (31.4s for the YouTube row against a true 174.17s, 124.97s for every SoundCloud row against 158.57 / 158.57 / 160.38 / 165.40 / 144.57), so its structural story that \"a 20s head covers 64% of a 31s file versus 16% of a 125s file\" is void, and its conclusion that the clip window must also move is refuted. Its one durable measurement, that the two sibling uploads share a byte-identical first 20s, I reproduced (core 1.0000, fp 0.9993, arr 0.9917) and it supports the window story rather than contradicting it.\n\nLens 3 ruled the decode window out on the grounds that 0.848 is already far above CORE_KEEP so no sliding is needed. That conflates \"kept\" with \"measured correctly\". 0.848 is the wrong number; the true value is 1.000. Its two cogs are both real code and I reproduced every one of its gate calls and tier computations exactly, including the byte-identical crown_rejected string, but they are downstream amplifiers. With the core measured right, the band tier does the correct thing unaided, index 0 IS the owner's link, and no fallback walk is required. Lens 3's fix would produce the right crown here by walking past a shelf of numbers that are still measured on the wrong audio, so I did not take it as dominant.\n\nWhere all three converge, and I confirmed it: the owner's upload was searched, downloaded and scored as candidate 2 of 6, so this is not a pool gap; the clip is genuinely at normal speed (core 1.0000 / arr 0.9236 / speed 1.0000 against a clean original); the YouTube winner is the same master rather than a knockoff; bass and tilt never got a vote; and nothing here is Shazam throttle contamination, since every payload number reproduces on my own fresh downloads within decode noise."
 },
 {
  "clip_n": 25,
  "dominant_mechanism": "The engine has no measurement that can separate the plain Usher master from a bass-boosted re-upload of it, so on a clip it measured as untransformed it crowned the boosted upload and then printed that upload's marketing title as the answer.\n\nChain, every link measured by me offline:\n\n(1) The engine never claimed a boost. res[\"speed\"] = \"as posted\", edit_label \"as posted\", speed_measured 1.0009, bass_boosted never set. crate.html:2382 even announces the card as \"Yeah! (feat. Lil Jon & Ludacris) - original\". The words the owner read are `exact.title` = \"Usher - Yeah (Bass Boosted)\", rendered at crate.html:1637-1639 as the headline under \"The exact version playing\". The card says \"original\" and \"Bass Boosted\" simultaneously.\n\n(2) `core` cannot demote the boosted upload, because core is EQ-blind BY DESIGN (verify.py:531-543, \"This is EQ/bass-INDEPENDENT ... so core is the pure is-this-the-same-recording signal\"). `core = max(_norm(fp, FP_LO=0.55, FP_HI=0.72), _norm(arr, ARR_LO=0.10, ARR_HI=0.45))`. At the clip's true alignment (t=45s in the track) the boosted upload's chromaprint fp = 0.721, which saturates FP_HI, so core = 1.000, while the true master's fp = 0.635 -> 0.500 and its arr = 0.402 -> 0.863, so core = 0.864. The EQ-invariant leg (arr) correctly prefers the master, 0.402 vs 0.355, and `max()` throws that evidence away. Core actively prefers the boosted re-upload.\n\n(3) The one signal that could have caught it, `bass_delta` from verify.py:_tilt_db (mean dB 60-250 Hz minus mean dB 2000-6000 Hz), is not merely coarse here, it is INVERTED. It reports the clip +4.42 dB bassier than the boosted upload. Band-anchored truth at the aligned section: the crown is +5.99 dB (30-60 Hz) and +5.96 dB (60-120 Hz) hotter than the master, and the clip is -4.15 dB and -5.31 dB in those same bands. The crown carries roughly 10 dB more real sub-bass than the clip; the scalar says the clip has 4.4 dB more. The inversion is driven by the clip's own +8.94 dB 250-500 Hz low-mid hump and its -3.3/-4.4 dB roll-off at 2-6k/6-10k, i.e. Instagram re-encode colouration, which a low-vs-high ratio counts as bass.\n\n(4) Because the scalar is inverted, server.py:_unverified_claims (1200, bass leg 1227-1239) evaluates |4.42| < BASS_STRIP_GAP 6.0 and prints \"which is the same EQ\" - it affirms that the boosted upload has the clip's EQ, when the two are about 10 dB apart in real sub-bass. So the engine's own overclaim detector, written for exactly this complaint (the comment at server.py:1906-1919 says \"a title was being read as a finding\" and \"the reverb complaint kept coming back\"), fires, gets the sign wrong, annotates, and ships the row as the headline anyway.\n\nThe clip is the OG. The owner is right, and nothing in the current pipeline can say so.",
  "root_cause_tag": "overclaim_bass",
  "evidence": "All numbers are from my own runs. No Shazam, no engine calls, no writes to ~/crate.\n\nREPRODUCED THE PAYLOAD EXACTLY (E.dl_clip seconds=20 + V.verify against the real IG audio, clip 16.7s):\n  crown tER89GdJTV0        core=0.686 speed=1.0000 bass=+4.42 fp=0.544 arr=0.340 spectral=0.825   (payload: core 0.686, bass 4.4, vspeed 1.0)\n  yt Ifu6TZnytlU (clean)   core=0.474 speed=1.0000 bass=+7.91 fp=0.630 arr=0.188 spectral=0.801\n  yt GxBSyx85Kp8 (Usher)   core=0.457 speed=1.0000 bass=+12.07 fp=0.628 arr=0.231 spectral=0.706\n  sc usher-raymond-music   core=0.531 speed=1.0000 bass=+7.73 fp=0.640 arr=0.173 spectral=0.821\n\nPOOL REPLICA (E.get_source -> build_queries -> search_edits, my own run): 15 queries, plain-original queries present at index 0/1/4 (\"Yeah feat Lil Jon Ludacris\", \"USHER Yeah feat Lil Jon Ludacris\", \"USHER Yeah\"); 553 rows. Applying _dl_priority (crate_engine.py:4449) + _sc_quota with max_dl=14: all 14 wave1 slots are edit-titled, the top 60 rows are 60/60 edit-titled, and the crowned tER89GdJTV0 is wave1 slot 2, so the replica reproduces the real head. The 5 is_official_original rows sit at ranks 152, 158, 165, 168, 178 of 553. Wave2's `head` (crate_engine.py:4606) is the same quota-adjusted 14 plus creator/comment appends, so nothing walks further down the list, and known_dir is None on an as-posted clip so the extra_dir_dl branch at 4665 never runs.\n\nFULL 25s WINDOW SLIDE, every 5s across the whole 250.1s clean master Ifu6TZnytlU:\n  0:0.474 5:0.454 10:0.454 15:0.446 20:0.635 25:0.573 30:0.666 35:0.711 40:0.675 45:0.864 50:0.696 55:0.578 90:0.693 105:0.703 120:0.762 130:0.663 195:0.707 230:0.246\n  BEST t=45s core=0.864 speed=1.0000 bass=+11.00 fp=0.635 arr=0.402 lag=0.65\nSAME SLIDE across the 254.6s crown tER89GdJTV0:\n  0:0.549 30:0.612 35:0.799 40:0.737 45:1.000 50:0.859 55:0.887 70:0.641 120:0.496 230:0.260\n  BEST t=45s core=1.000 speed=1.0000 bass=+6.89 fp=0.721 arr=0.355 lag=-1.86\nBoth peak at t=45, so the clip is ~45s into the track and the crown is the same recording with EQ on it. Speed reads 1.0000 at all 47 offsets I measured, confirming \"as posted\" / 1.0009.\n\nWHY NEITHER PROPOSED FIX FLIPS THIS CLIP, walked against rank_key (crate_engine.py:5130-5220) with real values:\n  Pool quota alone (first-20s scoring, as shipped): official 0.531 vs crown 0.686. `0 if strong_core else 1` (CORE_EDIT 0.62, tier 4) -> crown 0, official 1. Crown wins four tiers ABOVE the plain-original tier at 5217.\n  Quota + window slide: official 0.864, crown 1.000. Both clear strong_core, then the core-band tier `(0 if core >= CORE_SAME else 1 if >= CORE_MERIT else 2)` with CORE_SAME 0.95 -> crown 0, official 1. Crown wins again, and the slide makes it WORSE by promoting the crown from 0.686 to a provably-same-audio 1.000.\n  Tiers above strong_core do not separate them: song_terms = {\"yeah\"} so song_cov is 1.0 for both (_weak_untitled ties 0); neither is OTHER_RENDITION (_rendition_loses ties 0); \"usher\" is in both titles so _artist_own (crate_engine.py:5011) is True for both.\n\nBAND MEASUREMENT AT THE TRUE ALIGNMENT (16s sections, clip@0, master@45.65, crown@43.14; every band referenced to its own 500-2000 Hz anchor so gain cancels), dB:\n  band        CLIP    ORIG   CROWN | clip-orig  crown-orig\n  30-60      -13.42  -9.27   -3.28 |   -4.15      +5.99\n  60-120     +13.07 +18.38  +24.34 |   -5.31      +5.96\n  120-250     +5.01  +3.96   +7.76 |   +1.05      +3.80\n  250-500     +4.33  -4.60   -2.50 |   +8.94      +2.10\n  2k-6k       -8.00  -4.70   -5.62 |   -3.31      -0.93\n  6k-10k     -14.69 -10.28  -10.95 |   -4.41      -0.67\n  _tilt_db   +19.27  +7.87  +11.29 |  +11.39      +3.42\n  _tilt_slope +15.70 +7.91   +8.21 |   +7.79      +0.30\nThe crown is a real, roughly flat +6 dB sub-bass lift on the master. The clip has 4-5 dB LESS sub-bass than the master and a +8.94 dB 250-500 Hz hump with the top end 3-4 dB down. _tilt_db reads the clip as +11.39 dB \"bassier\" than the master while its actual sub-bass is 4-5 dB lower, i.e. inverted, and that inversion is the number that fed \"which is the same EQ\".\n\nRENDER PATH: crate.html:1637 `var xoc=d.title_overclaims||ex.claim||''` then 1638-1639 puts `ex.title` in the \"The exact version playing\" row with xoc as a `flag` tagged \"unverified claim\". The disclaimer is present and subordinate; the title is the headline.",
  "confidence": "high",
  "fix": "One change, at the place that already computes both facts: server.py:1920-1926, the `_oc, _ockind = _unverified_claims(top.get(\"title\"), ...)` block. Today it only annotates. Make it gate the crown instead.\n\nWhen `res[\"speed\"]` is \"as posted\" (the speed lock measured the clip untransformed) AND `_ockind` is non-empty (the crowned upload's title asserts a transform the audio did not confirm), do not present that upload as `exact`. Leave the row in the shelf so the evidence is still visible (\"Other versions we checked\"), and let the card answer with `base_song` / `base_artist`, which on this clip is exactly what the owner said: \"Yeah! (feat. Lil Jon & Ludacris)\" by USHER, original.\n\nWhy this is the surgical one: it needs no new measurement, no reordering of any rank_key tier, and no new download budget. Both conditions are already computed in that same function scope, and `_clip_is_edit` at crate_engine.py:5052 is the same test. Blast radius is exactly the clips where the engine says \"as posted\" and the crown's title says otherwise; on any clip the speed lock measured as an edit, `res[\"speed\"]` is not \"as posted\" and nothing changes, so the doctrine \"only prefer the plain original when the clip is not an edit\" is preserved. It also respects \"bass and speed NUDGE, never REJECT\" because it does not touch scoring or admission at all, only which row is allowed to carry the headline.\n\nDo NOT ship the two fixes the other lenses proposed as the answer to this clip. I measured both: an `_original_quota` in `find_edit`'s head still loses at the `strong_core` tier (official 0.531 vs crown 0.686), and adding the window slide makes it worse by lifting the crown to CORE_SAME 1.000 where it wins the core-band tier outright. Both are real defects, they just are not this one; ticket them separately against rohamghiasicw-star/crate#10.\n\nSeparate, lower-priority ticket that the band table justifies: `verify.py:_tilt_db` (60-250 Hz vs 2000-6000 Hz) is a low-vs-high ratio, not a bass meter, and on this clip it is inverted by 14 dB in the sub-bass. Any future bass claim should be gated on 30-120 Hz referenced to a 500-2000 Hz anchor. Recalibrate BASS_STRIP_GAP on the new band before letting it touch ranking, and keep it a nudge.",
  "disagreement": "Two splits, both resolved by my own measurement rather than by vote.\n\n1. Decode window vs download budget. Lens 1 said the 20s decode window was the cause; lenses 2 and 3 said the download budget was. Lens 3 additionally claimed the decode window was RULED OUT. Lens 3 is wrong on that: they slid only t=0,3,6,9,12,15 into a 250.1s track and concluded no later section matched. My full slide every 5s to t=230 peaks at t=45 with core 0.864, up from 0.474 on the first 20s, so the decode-window defect is real and present. The download budget is also real and I confirmed it independently: the 5 official rows sit at ranks 152/158/165/168/178 of a 553-row pool against max_dl=14, all 14 wave1 slots edit-titled, and my replica reproduces the real head (crown at slot 2). Lens 1 and lens 2 also disagreed about where the match sits (t=120 core 0.762 vs t=45 core 0.897); lens 1's 25-offset grid simply skipped t=45. My dense slide settles it at t=45 on both the master (0.864) and the crown (1.000).\n\n2. Whether either of those is the DOMINANT cause. All three lenses framed the missing plain original as the root and proposed a pool or window fix. I refuted both by walking rank_key with measured values. First-20s: crown 0.686 clears CORE_EDIT 0.62 and the official 0.531 does not, so the crown wins at the `strong_core` tier, four tiers above the plain-original preference at line 5217. With the slide: crown 1.000 reaches CORE_SAME 0.95 and the official 0.864 does not, so the crown wins at the core-band tier, still above 5217. The slide actively strengthens the wrong crown. So the pool and the window are contributing defects that do not reach this complaint, and the decisive mechanism is one none of the three lenses put first: the audio pipeline cannot separate a master from a bass-boosted re-upload of it (core is EQ-blind and its fp leg saturates for the boosted upload), the one bass signal that could is inverted by the clip's own re-encode colouration, and the answer surface therefore ships the boosted upload's title as the headline on a clip it measured as untransformed. Lens 1 and lens 2 both reached the \"title is not a measurement\" observation and both filed it as a third, cheaper afterthought; it is the whole thing.\n\nWhere all three lenses were right and I confirm them: the engine never set bass_boosted and never labelled the clip boosted; the words are `exact.title`; and the crown is genuinely the real master with EQ on it, not a knockoff (core 1.000 at t=45)."
 },
 {
  "clip_n": 18,
  "dominant_mechanism": "base_song and the two-song list are both decided by CLOCK POSITION, never by audio, and nothing downstream can undo it.\n\ncrate_engine.py:2440-2448, `_fingerprint_core`:\n\n    merged, seen_t = [], set()\n    for h in sorted(recovered + real, key=lambda h: h[\"at\"]):\n        ...\n    primary = dict(merged[0])\n    primary[\"songs\"]  = merged\n    primary[\"multi\"]  = len(merged) > 1\n\nTwo distinct title keys survived the scan, so `multi` went True with no audio test on either member, and `merged[0]` (earliest window) became the base. The earliest window here, at=6, carried a cover-remix catalogue row, \"Cheri Cheri Lady / Mentol & ADEN\". The real song sat at at=12. Both of the owner's symptoms, \"not two songs\" and the wrong name on the card, come out of those three lines.\n\nI measured what the clip actually contains, first 20s of candidate, which is what the engine scores:\n  MBY_plain          core 0.7521  same=True\n  MBY_slowed         core 0.8242\n  Cheri Cheri Lady, the EXACT named track (Mentol & ADEN Cover Remix)  core 0.3708\n  Cheri Cheri Lady, Modern Talking official master                     core 0.1677\n  unrelated null                                                       core 0.0000\nAcross sliding 25s windows the Cheri Cheri Lady family peaks at 0.3708 (41 windows) and 0.3092 (69 windows), and the null peaks at 0.4071 (71 windows). Cheri Cheri Lady never beats what an unrelated song reaches by chance. It has zero support in this clip.\n\nWhy it is unrecoverable, and why the download-starvation the other two lenses found is an amplifier rather than the cause: server.py:815-818 writes res[\"base_song\"] from fp[\"title\"] exactly once, and no crowned candidate ever rewrites it (the only later writes are `_credit_qualifier` at 907-910 and the base_uncertain null at 1941-1944). crate.html:1568 renders `d.base_song` as the hero title and 1573+ renders `d.songs` as the two-song block. So even a perfect crown on MONTAGEM BAIXA YOLANDA still leaves the owner reading \"Cheri Cheri Lady\" over a list of two songs. Fixing `_dl_priority` alone changes neither thing he complained about.\n\nBoth guards that exist for this are inert on this exact input, measured:\n  E._junk_id({\"title\":\"Cheri Cheri Lady\",\"artist\":\"Mentol & ADEN\"}) -> False\n  E._junk_id({\"title\":\"Modern Talking - Cheri Cheri Lady (Mentol & ADEN Cover Remix)\"}) -> True\n`_junk_id` (crate_engine.py:1851-1854) tests the SHAZAM ROW, and Shazam's catalogue row launders the word \"Cover\" out of the title, so the cover-mill filter cannot see it and the hit enters `real` at line 2419.\n  wrong_song.shazam_untrustworthy(\"Cheri Cheri Lady\",\"Mentol & ADEN\", skew, [\"original sound\",\"I like the Phonk edits\"], None)\n    -> (False, ['base-title-absent-from-credit/comments/candidates'])\nOne reason, one short of the `len(reasons) >= 2` trip. server.py:864 passes `None` literally as `master_core`, so line 46 of wrong_song.py (`master_fail = master_core is not None and master_core < 0.35`) can never fire. Feed it the number I measured for the named master, 0.168, and the same call returns (True, [..., 'clip-fails-verify-vs-named-master(core=0.17)']), which routes to server.py:1939 and blanks base_song. The most decisive input to the base-trust guard is wired shut.\n\nThe amplifier, confirmed by reading the code: base_title = \"Cheri Cheri Lady\" makes `key_terms` = {cheri, lady} at crate_engine.py:4355-4356, and `_dl_priority`'s first tier `-(c[\"title_hits\"] >= 1)` at 4463 hard-partitions the pool, so every MONTAGEM row sorts behind every Cheri Cheri Lady row and all 14 `max_dl` slots (crate_engine.py:4103, used at 4499) go to the wrong song. That is why the shipped pool topped out at core 0.308 against CORE_KEEP 0.50 and returned exact=None / unsure=True. The score floor did its job and refused to crown. The answer surface printed base_song anyway.",
  "root_cause_tag": "unarbitrated_multi_song",
  "evidence": "All numbers are mine, from V.verify with no Shazam call of any kind. Clip = E.get_source(\"https://vt.tiktok.com/ZSVcj3Gcj/\"), 20.19s. Every candidate window handed to verify is >= 25s so verify's own `_decode(cand, 20)` always gets a full 20s, which is the guard against the short-candidate core saturation in the skill's findings. Work dir /tmp/gdoc/c18J.\n\nFIRST 20s OF CANDIDATE (exactly what the engine scores):\n  MBY_slowed   yt zSFZOdun68A  core 0.8242  score 0.4963  speed 1.0590  bass +6.43\n  MBY_plain    yt b4r6BYMphfY  core 0.7521  score 0.5588  same=True  speed 0.9443  bass +4.80\n  CCL Mentol & ADEN Cover Remix  yt ue0L3NTjGto  core 0.3708  same=False  speed 1.0291\n  CCL Modern Talking master      yt eNvUS-6PTbs  core 0.1677  same=False\n  NULL Rickroll                  yt dQw4w9WgXcQ  core 0.0000  same=False\n\nSLIDING WINDOWS, 25s wide, 2.5s step, none truncated:\n  MBY_slowed   34 windows, peak 0.8242 at t0=0.0,  same=True at t0=45.0-52.5\n  MBY_plain    29 windows, peak 0.7924 at t0=45.0, same=True at 6 separate offsets\n  CCL_mentol   41 windows, peak 0.3708 at t0=0.0\n  CCL_master   69 windows, peak 0.3092 at t0=70.0\n  NULL         71 windows, peak 0.4071 at t0=130.0\nThe Cheri Cheri Lady family never exceeds the sliding-max an unrelated track reaches by chance. MONTAGEM BAIXA YOLANDA beats it 0.82 vs 0.37 and clears CORE_KEEP=0.50 from multiple offsets while Cheri Cheri Lady cannot reach it from any of 41 or 69.\n\nREGION TEST, src length held identical across every row so the comparison is fair. src = clip[0:12], which is the exact stretch the at=6 Shazam window covers and where the scan said \"Cheri Cheri Lady\":\n  MBY_slowed 0.4997 | MBY_plain 0.4412 | CCL_mentol 0.3691 | CCL_master 0.1607 | NULL 0.0000\nsrc = clip[8:20.2], the at=12 window:\n  MBY_plain 0.6135 | MBY_slowed 0.5176 | CCL_mentol 0.2316 | CCL_master 0.1905 | NULL 0.0473\nMONTAGEM BAIXA YOLANDA wins the Cheri Cheri Lady window too. There is no second song anywhere in this clip. Lag is consistent with one continuous alignment (MBY_plain lag +3.44 whole clip, +2.74 on the first half).\n\nGUARDS REPRODUCED WITH THE REAL INPUTS:\n  E._junk_id({\"title\":\"Cheri Cheri Lady\",\"artist\":\"Mentol & ADEN\"})  -> False\n  E._junk_id({\"title\":\"Modern Talking - Cheri Cheri Lady (Mentol & ADEN Cover Remix)\"}) -> True\n  W._MILL_UP.search(\"Mentol & ADEN\") -> None ; W._RENDITION.search(\"Cheri Cheri Lady Mentol & ADEN\") -> None\n  W.shazam_untrustworthy(\"Cheri Cheri Lady\",\"Mentol & ADEN\",skew,[\"original sound\",\"I like the Phonk edits\"],None)\n      -> (False, ['base-title-absent-from-credit/comments/candidates'])\n  same call, master_core=0.168 -> (True, ['base-title-absent-from-credit/comments/candidates',\n                                          'clip-fails-verify-vs-named-master(core=0.17)'])\nserver.py:863-864 passes None for master_core literally.\n\nPAYLOAD, /tmp/gdoc/c18.json: 6 candidates, all Cheri Cheri Lady uploads, core 0.087 / 0.156 / 0.184 / 0.224 / 0.305 / 0.308. CORE_KEEP=0.50. exact=None, weak_exact=0.184, unsure=True, decisive=False. No \"mashup\" and no \"sections\" key, so annotate_mashup never produced a pair and the two-song claim is purely `multi = len(merged) > 1` from crate_engine.py:2448.\n\nTAG GROUPING CHECK, /tmp/gdoc/c07.json: base \"The Scientist / Coldplay\" at=12 plus a phantom \"Bass Scientist / King of Bass\" at=48, no mashup key, exact=True. Identical mechanism at the identical three lines. There the earliest hit happened to be the right song, so only the second entry was wrong. Same cause, different half.\n\nTRANSFORM, secondary: measured against the plain original the clip is slowed 0.9443x with bass +4.80. The shipped label \"slowed ~0.89x\" is not a measurement of this clip against anything it named. Not what the owner complained about here, so not load-bearing.",
  "confidence": "high",
  "fix": "Arbitrate the scan's song list with audio at the multi-song hand-off, in one place, and let the crown write the answer.\n\nConcretely, at server.py:815-818: when `fp[\"multi\"]` is true, stop accepting `fp[\"songs\"][0]` as the base. Seed the edit hunt from EVERY member of `fp[\"songs\"]` (which means computing `title_hits` / `song_terms` at crate_engine.py:4355-4356 as the max over all member titles instead of over `base_title` alone, so `_dl_priority`'s first tier at 4463 stops hard-partitioning the pool), then set `res[\"base_song\"]` / `res[\"base_artist\"]` from whichever member produced a candidate clearing CORE_KEEP, and admit into `res[\"songs\"]` only members that produced one. A member that produces no candidate above the floor is not a song in this clip and must not be listed as one.\n\nMeasured backing that this resolves clip 18 unambiguously: the MONTAGEM BAIXA YOLANDA uploads score core 0.7521 (same=True) and 0.8242 on their first 20s, well clear of CORE_KEEP=0.50, while the entire Cheri Cheri Lady family maxes at 0.3708 across 41 sliding windows and 0.3092 across 69 and never clears the floor from any offset. One member survives, the other cannot. songs[] collapses to one entry, the false mashup disappears, and the hero title becomes the song the owner named. On a single-song clip `fp[\"songs\"]` has one member, so the policy is a no-op and the pool is byte-identical.\n\nI am naming this as one policy rather than one line on purpose, because neither half is sufficient alone and it would be dishonest to imply otherwise. Fix only the download priority and MONTAGEM BAIXA YOLANDA gets crowned as `exact` while the card still reads \"Cheri Cheri Lady\" over two songs, because base_song is written once at server.py:815-818 and crate.html:1568 renders it. Fix only the base_song write-back and there is still no crown to write back from, because all 14 `max_dl` slots go to the wrong song and the pool tops out at 0.308.\n\nCheap companion, one argument, worth taking in the same patch: server.py:864 passes `master_core=None` into `wrong_song.shazam_untrustworthy`, so its strongest rung is dead code. Pass the real number. With the 0.168 I measured against the named master, that call flips to untrustworthy and server.py:1939 answers \"couldn't confidently ID this one\" instead of asserting a song that is not in the clip. That is not the right answer, but it is an honest one, and it is one line.\n\nBoth touch the candidate pool for every clip, so they need the five-clip regression gate, run twice, and not while the owner is demoing.",
  "disagreement": "Lens 1 said the mechanism is the primary-by-timestamp pick plus a sample: that MONTAGEM BAIXA YOLANDA is built on a Cheri Cheri Lady sample, so one recording legitimately Shazams as two titles. Lenses 2 and 3 both said the mechanism is `_dl_priority` starving the right rows of download slots. I resolved both by measurement, not by vote.\n\nAgainst lenses 2 and 3. Their finding is real and I confirmed the code path, but it is an amplifier, not the cause. The test is whether fixing it alone removes what the owner complained about, and it does not. `res[\"base_song\"]` is written once at server.py:815-818 from `fp[\"title\"]`, the only later writes are `_credit_qualifier` (907-910) and the base_uncertain null (1941-1944), and crate.html:1568 renders `d.base_song` as the hero while 1573+ renders `d.songs` as the two-song block. So with `_dl_priority` fixed, MONTAGEM BAIXA YOLANDA gets crowned as `exact` and the card still says \"Cheri Cheri Lady\", still lists two songs. The owner's verdict is false_mashup, and that fix leaves the mashup on screen. It goes in the patch, it is not the root.\n\nAgainst lens 1's sample claim, which I could not reproduce. Lens 1 reported Cheri Cheri Lady [104:124] scoring core 0.812 against the montagem's own first 25s versus a 0.114-0.205 null. At that exact offset I measure 0.1688, not 0.812. Running the same direction with the src length held at 20s for every row and four null excerpts as the control:\n  CCL_master[70:90]  0.5858 | CCL_mentol[0:20]  0.6086 | CCL_master[104:124] 0.1688 | CCL_mentol[60:80] 0.1603\n  NULL[30:50] 0.2420 | NULL[70:90] 0.2517 | NULL[104:124] 0.1409 | NULL[150:170] 0.1655\nSo there is a weak elevation at two offsets, 0.59-0.61 against a null band of 0.14-0.25, nothing reaching same=True and nothing near CORE_SAME. That is consistent with a loose interpolation or with shared eurodance-adjacent material, and it is not proof of a sample. I also found the reverse trap that probably produced lens 1's number: sliding many windows and taking the max inflates the peak badly. My unrelated-track control reaches 0.7159 against MBY across 71 windows, which is HIGHER than anything Cheri Cheri Lady reaches, so a sliding max is not evidence of anything on its own. Lens 1's own null was a fixed probe and therefore not comparable to its sliding-max positive.\n\nNone of that changes the verdict, which is why I did not chase it further. Whether Shazam named Cheri Cheri Lady off a faint interpolation or off a plain cover-mill false positive, the clip's own audio says it is not in there (0.3708 peak against a 0.4071 null sliding max), and the engine has no mechanism that asks. What I could NOT establish, and I am flagging it rather than guessing: the per-window scan support behind each title, because that needs Shazam and Shazam is off limits to me. So I cannot say whether ordering `merged` by support instead of by `at` would by itself have fixed this clip, which is what lens 3 proposed. My fix does not depend on that number."
 },
 {
  "clip_n": 26,
  "dominant_mechanism": "The clip is 6.99 s long, and `speed_from_master._win_speeds` (~/crate/speed_from_master.py:119) hardcodes `W=12.0, step=4.0` with a `while t + 6.0 <= dur` guard, so it emits floor((dur-6)/4)+1 windows: exactly ONE window at 6.99 s. `confirm_ref` (line 148) requires `len(ws) >= min_win` with min_win=3 (line 165-166), so it returns False against EVERY reference regardless of match quality, and it is the only gate between the engine and its own correct speed reading. In server.py:1687-1727 the speed block filters its references through confirm_ref, gets `refs == []`, never calls `measure_consensus`, and leaves `measured = None`. `_crown_contradicts` RULE 1 (server.py:1463, 1482-1487) then takes the `else` branch, falls back to phase 1's placeholder label (hardcoded \"as posted\" for the rate-1.00 scan probe, crate_engine.py:2275), prints \"nothing measured a speed shift on this clip\", nulls `top` at server.py:1770-1776, and the `elif measured and measured.get(\"label\") != \"as posted\"` fallback at server.py:1927 that would have written \"slowed ~0.93x\" into res[\"speed\"] cannot fire because `measured` is None. The reading was not missing, it was computed and thrown away: the one window confirm_ref rejects reads 0.9321-0.9344. This is a pure clip-length floor, not a reference or alignment problem: confirm_ref is False at all 17 offsets across the full 179 s original including the sections where verify scores core 1.000, and the window count is 1 at every one of them. probes=1 is the secondary contributor: Shazam matched the separately distributed \"Ark (Slowed)\" release (itself the original at 0.9353x, verified) on the first rate-1.00 probe, so the counter-speed FINE_SWEEP, the only other route to a \"slowed\" label on a short clip, never ran.",
  "root_cause_tag": "short_clip_speed_floor",
  "evidence": "All numbers below are mine, measured this session, no engine call and no Shazam.\n\nCLIP LENGTH: ffprobe clip.wav = 6.990612 s.\n\nGROUND TRUTH (clip vs clean NCS original \"Ship Wrek & Zookeepers - Ark\", youtube 8xlDwukxjnA, full 178.98 s, 25 s window slid in 5 s steps, V.verify): t=10 core 0.829; t=15 core 1.000 speed 0.9353 bass -0.72; t=20 core 1.000 speed 0.9353 bass -0.16; t=25/30/35 core 1.000 speed 0.9353 bass -0.70/-0.20/-1.14; t=40 core 1.000 bass -3.37; t=45 core 1.000 bass -2.66. Best window full record: score 0.9809, core 1.000, speed 0.9353, bass_delta -0.72, reverb_delta -0.0638, slope_delta 1.315. The clip is the plain original slowed ~6.5%, bass flat. The owner is right.\n\nTHE ENGINE'S OWN REFERENCES, THROWN AWAY: I reran the exact server.py:1704 fallback. E.search_edits([\"Star Party & Zookeepers Ark official audio\", \"Star Party & Zookeepers Ark audio\"], 4) -> 8 rows, 4 survive the EDIT_WORDS/OTHER_RENDITION filter (8xlDwukxjnA, IVSDjqQnnuY, pS9kyRy_-7o, A8Wg51xk1eA), all 4 downloaded by E.dl_clip in 4.1 s. confirm_ref on each: om0 False (1 window, 0.9323 conf 0.636), om1 False (1 window, 0.9324 conf 0.638), om2 False (1 window, 0.9321 conf 0.628), om3 False (1 window, 0.9344 conf 0.447). refs kept: 0.\n\nTHE GATE BYPASSED, NOTHING ELSE CHANGED: measure_consensus(clip, those same 4 files) = {\"speed\": 0.9327, \"label\": \"slowed ~0.93x\", \"confident\": true, \"nrefs\": 4, \"agree\": 4, \"spread\": 0.002, \"reason\": \"cluster 4 win / 4 ref\"}. That is the owner's verdict, from the engine's own code, on the engine's own downloads.\n\nREFERENCE CONTENT IS IRRELEVANT (decode window ruled out by direct test): confirm_ref(clip, original[t:t+25]) for t = 0,10,20,...,160 returned False 17 of 17, with windows=1 at every single offset, including t=20-40 where V.verify scores core 1.000. A wider or correctly located decode window cannot change this.\n\nLENGTH IS THE ONLY VARIABLE (controlled truncation ladder): I built a synthetic same-recording pair (original[15:75] resampled to 0.9353x) and truncated only the clip side against one fixed 25 s reference. 6.99 s -> 1 window, confirm_ref False; 9.90 -> 1, False; 11.90 -> 2, False; 13.50 -> 2, False; 13.90 -> 2, False; 14.00 -> 3, True; 14.10 -> 3, True; 18.00 -> 4, True; 30.00 -> 5, True. The cliff is at exactly 14.0 s, exactly where the loop arithmetic puts it.\n\nRULE 1 SIMULATED FROM THE SHIPPED CONSTANTS: with measured=None it returns the payload's crown_rejected string verbatim, \"nothing measured a speed shift on this clip, candidate titles itself a speed edit\". With measured = the consensus dict above it returns None, no refusal. _OFFICIAL does not match the ItzSpace_ title; _SPEED_CLAIM does.\n\nCORPUS CONFIRMS THE BOUNDARY: across the 33 saved payloads, 9 of 9 clips under 14.0 s carry speed_measured = None (7.0, 9.6, 10.4, 10.8, 11.4, 11.9, 12.1, 12.9, 13.5 s). The first clip at or above 14.0 s (c33, 14.2 s) carries 0.9995, and 16 of 24 clips >= 14 s carry a value. Zero exceptions.\n\nWHY THE OTHER ROUTE ALSO DIED: the Apple preview of \"Ark (Slowed)\" measured against the clean original reads core 1.000 speed 0.9353 at t=60 and 0.9469 at t=80, i.e. the Shazam base is itself the 0.9353x release, the same ratio as the clip. That is why the rate-1.00 probe hit on probe 1 and no counter-speed sweep ran. Two clips in the corpus at 10.8 s and 12.9 s DO carry a \"slowed\" label with speed_measured None, so the sweep is the short-clip escape hatch that happened to be unavailable here.",
  "confidence": "high",
  "fix": "One change, in ~/crate/speed_from_master.py only. In `_win_speeds` (line 119) the stride is hardcoded `step=4.0`, which is what caps a 7 s clip at one window. Derive it from the clip: `step = min(4.0, max(0.25, (dur - 6.0) / 2.0))`. That yields 3 windows for any dur >= 6.5 s and is a bit-for-bit no-op for every clip >= 14 s, so no long clip's behaviour moves. Do NOT instead lower min_win to 1: a single window with no cross-window agreement is precisely the unverified reading confirm_ref was written to reject, and the tight-cluster test (tol=0.02) plus conf_min=0.45 stay real tests under the stride change. On this clip the windows cluster at 0.932-0.934 with conf up to 0.638, well clear of both thresholds, and measure_consensus then returns \"slowed ~0.93x\" at spread 0.002 across 4/4 refs, which is the owner's answer.\n\nThree cautions before shipping, two of them mine rather than the lenses'. First, `measure_consensus` is NOT independently short-clip-safe, contrary to one lens report: I measured it on a single perfectly aligned core-1.000 reference and it returned {\"confident\": false, \"reason\": \"insufficient reference windows\"} because it needs len(logs) >= 2. It only succeeds here because 4 refs x 1 window = 4 pooled logs. Fixing the stride fixes both call sites at once, which is another reason to fix it there and not in confirm_ref. Second, this newly produces a `measured` value on 9 of the 33 graded clips, and `measured` is consulted by _crown_contradicts (server.py:1463) and _crown_tempo_mismatch, so it changes crown outcomes: run the five-clip regression gate twice, and re-run the SIM-B simulation recorded at server.py:1730-1740 (it regressed n=24, n=29, n=30, all sub-14s clips, so its inputs change under this patch and its old conclusion cannot be assumed). Third, on this clip specifically, removing the RULE 1 refusal lets ItzSpace_ (core 0.625) survive to the crown, which is a weak crown on a clip where nothing in the pool is actually the source; check that CORE_KEEP/unsure still catch it, because the label fix must not buy itself a wrong-version crown.\n\nSeparately and independently, the 20 s decode window (verify.py:87, crate_engine.py:3806) still corrupts the candidate numbers this payload showed the owner, ticket rohamghiasicw-star/crate#10, but it is not this bug.",
  "disagreement": "The three lenses converged on the same mechanism, so the real disagreements were about the secondary defects, and I settled each by measurement rather than by counting votes.\n\n1. Is the 20 s decode window co-dominant? Lens 2 pushed it hardest, arguing the exact release (It4P_OACs3I) reads core 0.3676 on its head and 1.000 at t=16, so the crown was lost to the decode window. I tested the thing that actually decides the label: confirm_ref against 17 different 25 s sections of the full 179 s original, including the sections where verify scores core 1.000. False 17 out of 17, with windows=1 every time. The window count is a function of the CLIP's duration and cannot be moved by any choice of reference section. So the decode window is real (I do not dispute lens 2's candidate-side numbers) but it is downstream and separate: it cost a crown, it did not produce \"as posted\". Resolved against lens 2's co-dominance framing.\n\n2. Was the clip's version in the pool at all? Lens 2 said yes (the Star Party - Topic release verifies core 1.000 at the right offset); lens 3 said no (best in-pool core across full slides is 0.704). I could not download It4P_OACs3I (yt-dlp returned \"The page needs to be reloaded\"), so I tested the same release via the Apple preview in the payload: it reads core 0.10-0.14 against the clip across all 30 s, but core 1.000 speed 0.9353 against the clean original at t=60. That means the release IS the clip's source audio and the preview simply covers a different section, which is consistent with lens 2 and does not contradict lens 3's statement about the six SHIPPED candidates. Both are right about different sets. Neither changes the label.\n\n3. Is measure_consensus short-clip-safe? Lens 1 asserted it \"only needs 2 pooled windows across all refs, so it tolerates a 7s clip fine\", and used that to argue confirm_ref is the sole defect. I measured it and lens 1 is partly wrong: on one aligned reference it returns confident=false, \"insufficient reference windows\". It survives here only because four references each contribute one window. Lens 3 measured the same thing and got the same result. This changes the fix, not the diagnosis: the patch has to go in _win_speeds, which feeds both functions, rather than anywhere in confirm_ref.\n\n4. Where to put the fix, gate or measurer. Lens 3 wanted a refusal-side change as well (pass the unconfident dict through so RULE 1 can distinguish \"unmeasurable\" from \"measured as posted\"). I left that out of the single surgical fix because with the stride corrected, RULE 1 no longer fires here at all, which I confirmed by simulating RULE 1 from the shipped regexes with both inputs. Lens 3's change is a good honesty improvement and should be its own ticket, but it is not needed to fix clip 26 and it widens the blast radius on a clip class the SIM-B note already flags."
 },
 {
  "clip_n": 21,
  "dominant_mechanism": "verify._speed_xcorr returns a CONFIDENT FALSE LOCK at lag 0 (speed exactly 1.0000) on this clip's audio, and every speed number in the payload is that one failure repeated. Ground truth measured here: the clip is slowed to 0.7937x of the official master. The engine's master-referenced path does not merely fail to see it, it confidently asserts the opposite: speed_from_master.measure_consensus(clip, [three correct clean masters]) returns confident=True, speed=1.0001, reason \"within deadband -> as posted\". _win_speeds reads 1.0000/1.0004 at conf 0.40-0.47, above conf_min 0.40/0.45 and 4-5x the `if sconf < 0.10: speed = 1.0` guard, so this is a measurement, not a default.\n\nCause of the lag-0 attractor, nailed to one line: verify.py:_detrend (~L147-152) is `s - np.convolve(s, np.ones(65)/65, mode=\"same\")`. mode=\"same\" ZERO-PADS, so the outer ~32 of the 512 log-spectrum bins get a \"moving average\" computed against zeros. This clip's log-spectrum ramps ~40 dB down at the top of the 60-8000 Hz band (normalised -0.96 at 3.2 kHz to -2.15 at 8 kHz, from the heavy bass boost plus a low-bandwidth encode), so the un-normalised edge taps leave a residual of rms 0.473 against an interior rms of 0.169 - the artifact is 2.8x the real signal. It sits at a fixed ARRAY POSITION, so it is byte-for-byte identical in both spectra whatever the pitch, and it pins the cross-correlation at lag 0. Measured contribution to the lag-0 correlation: 81.7% from the 3840-8000 Hz edge, 26.7% from the 60-120 Hz edge.\n\nBoth halves of the owner's note fall out of that single fabricated 1.0000. (a) \"should be slowed\": res[\"speed\"] stays the \"as posted\" placeholder. (b) \"careful with soundcloud knockoffs\": verify reads vspeed 1.0000 for FIVE of the six pool rows whose true clip/candidate ratios are 0.6936 to 1.0594, and candidate_speed_lock returns None for all six so the corroboration built for exactly this case (the \"Safe and Sound (hardtekk)\" fix) never fires. crate_engine rank_key's `_d = abs(log2(vcheck)); speed_exact = 0 if _d <= 0.03` therefore hands bucket 0 to the 1,701-play RL Grime SoundCloud row and bucket 1 to c3 - 67,033 plays, titled \"slowed/reverb\", the ONLY row the engine measured correctly (1.0390 vs true 1.0393) and the only genuine tempo-mate of the clip at a 3.9% gap. The tier meant to reward \"needed no speed correction\" instead rewards \"speed could not be measured\". The same placeholder then arms server.py _crown_contradicts RULE 2, which rejects the crown on tilt.",
  "root_cause_tag": "false_speed_lock",
  "evidence": "GROUND TRUTH, clip = 0.7937x (three independent locks):\n1. CQT log-frequency pitch shift vs the official master (yt iw754uoV6_k, 161.9s), 36 bins/octave, 1-octave whitening: CLIP -4.00 st r=0.381 (runner-up +8.00 r=0.265) => 0.7937x. The estimator was VALIDATED before use: 8/8 known-resample controls recovered on this very clip's audio (x1/0.75 -> +5.00 expect +4.98; 0.80 -> +4.00 / +3.86; 0.85 -> +2.67 / +2.81; 0.90 -> +1.67 / +1.82; 0.95 -> +1.00 / +0.89; 1.00 -> +0.00; 1.10 -> -1.67 / -1.65; 1.25 -> -4.00 / -3.86), and 8/8 on apple.wav.\n2. Same estimator reads every candidate consistently with its own title: c1 \"sped up\" +2.33 st = 1.1443x; c3 \"slowed/reverb\" -4.67 st = 0.7637x; c5 \"slowed + reverb\" -5.00 st = 0.7492x; apple preview, c0 RL Grime, c2 and c4 bass-boosted all exactly +0.00 st = 1.0000x.\n3. Closed by the ENGINE's own arithmetic: verify(clip, c3)[\"speed\"] = 1.0390. 0.7637 x 1.0390 = 0.7935. Two unrelated estimators agree to 0.03%.\n\nTHE ENGINE SAYS 1.0000, CONFIDENTLY:\n  _win_speeds(clip, m_aud)  0 windows\n  _win_speeds(clip, m_expl) 2 windows [(1.0000, 0.414), (1.0004, 0.403)]\n  _win_speeds(clip, apple)  1 window  [(1.0000, 0.469)]\n  measure_consensus(clip, [m_aud])                 confident=False \"insufficient reference windows\"\n  measure_consensus(clip, [m_aud, m_expl, apple])  confident=TRUE  speed=1.0001 \"within deadband -> as posted\"\nThe payload carries no speed_measured / speed_confirmed key at all (confirm_ref returned False for all three refs on min_win=3), but the plumbing is the secondary failure - the measurement itself is wrong even when handed the right references.\n\nTHE FAILURE IS CLIP-SPECIFIC, WITH EXACT GROUND TRUTH. V._speed_xcorr(V._avg_logspec(_hp(V._resample_by(x, 1/s))), V._avg_logspec(_hp(x))) - a file against a resampled copy of ITSELF, so the answer is known exactly (expect 1.250 / 1.111 / 0.870 for s = 0.80 / 0.90 / 1.15):\n  clip.wav        0/3   reads [1.000, 1.000, 1.000]   BLIND\n  m_aud, m_expl, apple, c0, c1, c2, c3, c4, c5   3/3 each, reads [1.246, 1.111, 0.866]\nPerfect separation, 9 files clean and only the clip blind. Cost 0.22s per file.\n\nTHE EDGE ARTIFACT IS THE CAUSE (clip.wav, _detrend win=65):\n  rms(detrended) whole 0.243 | interior[40:472] 0.169 | outer 80 bins 0.473\n  raw normalised _avg_logspec: 3221 Hz -0.960 -> 5945 Hz -2.145 -> 8000 Hz -2.153 (a ~40 dB ramp into the band edge)\n  Replace np.convolve(...,\"same\") with an edge-normalised moving average (divide by the actual tap count):\n    self-0.80x control  1.000x conf 0.46  ->  1.246x conf 0.92   (truth 1.250)\n    clip vs master      1.0000x           ->  0.8832x, then 0.8420x, 0.8027x  (a real slow, right direction)\n\nRANKING CONSEQUENCE (measured, not inferred). true clip/cand | verify vspeed | candidate_speed_lock | speed_exact bucket:\n  c0 RL Grime SC (1,701 plays)  0.7937 | 1.0000 | None | 0\n  c1 sped up YT                 0.6936 | 1.0000 | None | 0\n  c2 bass boosted YT            0.7937 | 1.0000 | None | 0\n  c3 slowed SC (67,033 plays)   1.0393 | 1.0390 | None | 1   <- the only correct reading, the only row penalised\n  c4 bass boost YT              0.7937 | 1.0000 | None | 0\n  c5 slowed YT                  1.0594 | 1.0000 | None | 0\n\nCORE SATURATION IS REAL AND CO-PRESENT, BUT SECONDARY. verify(clip, X): Nirvana \"Smells Like Teen Spirit\" core 1.0000 score 1.0000; Ed Sheeran \"Shape of You\" core 1.0000; c0 core 1.0000; c3 core 1.0000; the actual official master core 0.1463, apple preview core 0.1811. So the audio score is worthless for ordering this pool. It is not what missed the slow: the speed label deliberately never reads core (confirm_ref exists to bypass it), and measure_consensus with the correct clean masters still returns a confident \"as posted\".\n\nScripts and raw output: /private/tmp/claude-501/-Users-rohamghiasi-Desktop-Mover-Files/3ecec15c-7dea-411d-9158-e452f71c79ca/scratchpad/j21/{cqtshift.py, mech.py, mech2.py, edge.py, refwin.py, wouldfix.py, sweep.py, align.py, pitch.py, pitch2.py, logspec.py}. No Shazam call of any kind; no file in ~/crate modified (the _detrend fix was monkeypatched in memory); the running engine was never contacted.",
  "confidence": "high",
  "fix": "ONE CHANGE: make the clip prove the speed estimator can see it, before any speed is believed. In speed_from_master, once per lookup, run the ground-truth self test on the CLIP's decoded audio - V._speed_xcorr(V._avg_logspec(_hp(V._resample_by(xc, 1/s))), V._avg_logspec(_hp(xc))) for s in (0.80, 0.90, 1.15). The answer is known exactly because we made the copy. If it does not recover the known ratio within 3%, set `speed_blind` and (a) measure_consensus returns confident=False instead of a confident 1.0001 \"as posted\", (b) crate_engine rank_key treats every vspeed as UNMEASURED so no row can take bucket 0 on a fabricated exact - which alone drops the 1,701-play RL Grime SoundCloud row out of the top tier and stops c3 being the only row punished for being measured correctly.\n\nWhy this one and not the others: it has a hard ground truth rather than a threshold, it separated perfectly here (0/3 clip, 3/3 on all nine other files), it costs 0.22s once, and it can only WITHHOLD a fabricated exactness - it can never fabricate a slow, so it cannot flip a genuinely as-posted clip, which is the regression the hard rules and the SIM-B note both warn about. It also generalises: any clip whose spectrum ramps into the band edge trips the same lag-0 attractor.\n\nSecondary, correct, and worth doing but NOT sufficient alone: fix verify.py:_detrend's zero-padded edges - `num = np.convolve(s, np.ones(win)); den = np.convolve(np.ones_like(s), np.ones(win)); return s - num/den` (both mode=\"same\"). Measured: the ground-truth self control goes 1.000x -> 1.246x (truth 1.250) at conf 0.46 -> 0.92, and clip-vs-master goes 1.0000x -> 0.8832x. But _win_speeds' 12s windows still fail to form a cluster afterwards (nwin drops to 0-1), so on its own it converts a confident wrong label into no label rather than into \"slowed ~0.79x\". Ship it with the self test, not instead of it.\n\nDo NOT reach for the obvious third thing: this is not the decode-window bug. Sliding the 25s reference window across the whole 161.9s master in 10s steps produced 1.0000 or nothing at every one of 14 positions.",
  "disagreement": "The three lenses split on the one question that decides the clip. Lens 1 said the clip is 1.0000x \"high confidence, three independent references, 8.6 sigma\". Lens 3 said ~0.795x. Lens 2 said it could neither confirm nor refute. I resolved it by measurement, not by vote.\n\nLens 1 is wrong, and wrong in an instructive way: all three of its \"independent\" methods share one bias. verify._speed_xcorr against the full master, against the Apple preview, and lens 1's own onset-envelope alignment are all long-term-average correlations on this clip's audio, and all three have the same lag-0 attractor. Its 8.6-sigma claim is not evidence because its null distribution carries the same bias as its estimator. I fell into the identical trap twice before catching it: my first attempt (whitened 64-band log-mel, 2-D rate x offset sweep against the full 161.9s master) gave a flat curve, 0.15-0.25 with no peak and every rate landing at master t=82.6s; my second (whitened 2400-bin log-frequency shift, 27.7 bins/semitone) read +0.00 st for apple.wav against a copy of itself resampled to a KNOWN 0.75x. Both failed their own controls. Only a constant-Q representation passed - 8/8 known resamples on this clip's audio, 8/8 on the master - and it reads -4.00 semitones, 0.7937x.\n\nThe decider is the control lens 1 never ran: give the engine's own estimator a file and a resampled copy of that same file, where the answer is arithmetically certain. It recovers 1.246 / 1.111 / 0.866 on m_aud, m_expl, apple and all six candidates, and 1.000 / 1.000 / 1.000 on the clip. A 1.0000 that the estimator also returns when the truth is 1.250 is not a reading.\n\nLens 3 reached the right number (0.795 vs my 0.7937, a 0.2% gap) but by a chain through candidate ratios, and it attributed the cause to the speed grid and to _TILT_MAX. The actual cause is one line above both: _detrend's zero-padded convolution edges, which I isolated by band (81.7% of the lag-0 mass in 3840-8000 Hz) and by fix (edge-normalising the average flips the ground-truth control from 1.000 to 1.246).\n\nLens 2's core-saturation finding is real and I reproduced it independently (Teen Spirit core 1.0000 / score 1.0000, Shape of You core 1.0000, the true master 0.1463) - but it is not the dominant mechanism for the graded failure. The speed label is computed on a path that deliberately never touches core, and I showed measure_consensus still returns a confident \"as posted\" when handed the three correct clean masters. Core saturation flattens the 0.85 term and so contributes to the knockoff ordering; the false speed lock explains the missed slow on its own AND supplies the rank tier that put the knockoff on top. Lens 2 was right to refuse to call the speed; it just lacked a validated estimator."
 },
 {
  "clip_n": 30,
  "dominant_mechanism": "`speed_from_master.confirm_ref` is a mathematical no-op on any clip shorter than 14.0 seconds, so no clean normal-speed reference ever reaches the speed measurement, `measured` stays None, and the payload ships the phase-1 Shazam grid guess.\n\nThe chain, every link named and measured:\n\n1. `speed_from_master._win_speeds` (speed_from_master.py:112-137) walks the CLIP with a fixed `W=12.0, step=4.0` under `while t + 6.0 <= dur`. The number of windows it can ever emit is `floor((dur - 6)/4) + 1`. Clip 30 decodes at **10.799 s**, so the loop runs at t=0 and t=4 and stops. **Two windows, maximum, forever.**\n\n2. `confirm_ref` (speed_from_master.py:139-160) opens with `if len(ws) < min_win: return False`, and `min_win` defaults to **3**. Two is less than three. `confirm_ref` therefore returns False for EVERY possible reference on this clip, regardless of how good that reference is. The threshold is exactly `dur >= 14.0 s`.\n\n3. server.py:1689-1729 is the only place a clean reference can enter. Both of its arms are gated on `confirm_ref`: the `edit[\"ref_paths\"]` arm at 1694-1698 and the \"official audio\" rescue at 1701-1721. So `refs` is necessarily `[]`, `measure_consensus` is never called, and `measured` stays None.\n\n4. server.py:1740 `if measured: res[\"speed_confirmed\"] = True` sits at indent 12, outside the crown logic, and the payload carries **no `speed_confirmed`, no `speed_measured`, no `speed_refs`**. That is the payload proving `measured` was None, not an inference.\n\n5. Both label paths read `measured`. The crowned path (server.py:1820, inside `if top:` at 1811) and the no-crown path (server.py:1927, `elif measured and measured.get(\"label\") != \"as posted\"`) are two arms of one if/elif at indent 12. With `measured` None, neither fires, and `res[\"speed\"]` keeps its phase-1 value: the Shazam counter-sweep label `\"slowed ~0.93x\"`.\n\n6. Secondary ceiling: even a perfect sweep could not have answered. `crate_engine.FINE_SWEEP` (crate_engine.py:1815-1821) bottoms out at `(1.50, \"slowed ~0.67x\")`; the true counter-speed is 1/0.603 = 1.658. The grid has no cell for this clip.\n\nThe bass half has the same root. The only clean-reference channel is the one `confirm_ref` closes, and the surviving bass path (server.py:1898-1905) compares the clip only to the CROWNED candidate, which is itself another slowed+boosted edit, and requires `top` to survive the gates. `top` was None. Every bass number the engine had was edit-vs-edit (-0.7 to -6.0 dB), so a boost the whole family shares is structurally invisible.",
  "root_cause_tag": "short_clip_no_speed_ref",
  "evidence": "All measured by me. No Shazam call, no /find call, no fingerprint call, nothing written in ~/crate. Work dir /tmp/gdoc/c30J.\n\nTHE FLOOR, ARITHMETIC. Clip decodes at 10.799 s. Replaying `_win_speeds`' loop:\n  dur  8.0s -> 1 window   dur 10.0s -> 2   dur 10.8s -> 2   dur 12.0s -> 2   dur 13.9s -> 2\n  dur 14.0s -> 3          dur 18.0s -> 4   dur 25.0s -> 5\n`confirm_ref.__defaults__` = (25, 0.45, 3, 0.02), i.e. min_win=3. Impossible below 14.0 s.\n\nTHE FLOOR, REPRODUCED END TO END. I replayed server.py:1701-1721 exactly: `E.search_edits([\"SOPHIE Vyzee official audio\",\"SOPHIE Vyzee audio\"], 4)` returned 66 rows, the EDIT_WORDS/OTHER_RENDITION filter picked 5 clean masters, all 5 downloaded:\n  KXdRl6Ml37I \"SOPHIE - VYZEE (Official Stream)\"  -> confirm_ref False, nwin=0\n  uASrcwDjL4Q \"VYZEE\"                              -> False, nwin=0\n  AOGy6QbsrIw \"Sophie - Vyzee\"                     -> False, nwin=0\n  nik-rad \"Just Staring / SOPHIE - VYZEE\"          -> False, nwin=2, windows (0.5753, conf 0.56) and (0.5760, conf 0.48)\n  msmsmsm/sophie-vyzee (SOPHIE's own)              -> False, nwin=0\nThe fourth one is the case that decides it. Two windows, tightly clustered to 0.1%, both above conf_min 0.45. Refused on COUNT alone.\n\nTHE FIX, MEASURED. Only change: give a short clip a shorter stride, W=8.0 step=2.0, min_win left at 3.\n  om3 confirm_ref -> True, 3 windows (0.5751, 0.54) (0.5754, 0.46) (0.5760, 0.48)\n  measure_consensus -> {\"speed\": 0.5756, \"label\": \"slowed ~0.58x\", \"confident\": true, \"agree\": 1, \"spread\": 0.001}\n  server.py:1927 elif then ships res[\"speed\"] = \"slowed ~0.58x\". Owner's \"SUPER slowed\", instead of \"slowed ~0.93x\".\n\nTRUE SPEED = 0.603x, four independent chains:\n  onset-envelope tempo: clip 78.01 BPM vs master 129.20 BPM (stable at t=120/125/130/135) = 0.6038\n  trimmed log-spectral lag vs master t=120/130 windows                                    = 0.6042-0.6051\n  chain via om3: 0.5756 x 1.0465 (om3 is itself 4.65% FAST vs master)                     = 0.6024\n  chain via the crowned trfied upload: 0.6382 x 0.9533                                    = 0.6084\nEngine shipped 0.93x. Under-read by a factor of 1.54.\n\nCROSS-CLIP PROOF, and this is the part that makes it a mechanism rather than a story. Over all 33 saved payloads in /tmp/gdoc:\n  9 payloads have clip_secs < 14.0 -> n=1 (9.6), 9 (12.9), 24 (11.9), 26 (7.0), 27 (11.4), 28 (13.5), 29 (10.4), 30 (10.8), 31 (12.1)\n  ZERO of those 9 carry speed_confirmed, speed_measured or speed_refs.\n  16 payloads DO carry a measured speed. The shortest is n=33 at 14.2 s. Every single one is >= 14.2 s.\nPerfect separation at exactly the 14.0 s boundary where `_win_speeds` starts emitting a third window. That is not a correlation, it is the constant.\n\nWHY THE CROWN GATE IS NOT THE CAUSE. `_crown_tempo_mismatch` did refuse (|log2(0.6382)| = 0.648 vs _TEMPO_TOL 0.06), which is why the payload carries that refusal string. But server.py:1927 is a sibling `elif` at the same indent as `if top:` (1811), so a confident measurement ships the right label with NO crown at all. The refusal cost the exact-version row, not the speed number.\n\nBASS, SPEED-CORRECTED AND TIME-ALIGNED. Clip resampled x1.6562 to master pitch, then onset-envelope aligned against the master over 100-180 s: peak at t=149.11 s, z = 9.7. Band deltas (clip minus master, normalised on 500-1000 Hz):\n  20-60 Hz +18.16 | 60-120 +2.70 | 120-250 +6.21 | 250-500 +1.13 | 500-1k 0.00 | 1k-4k -0.30 | 4k-7k -5.61\nControls, real shelves applied to that same aligned master window:\n  +8 dB @100Hz  -> 20-60 +7.10,  60-120 +4.25,  _tilt_db +1.94\n  +14 dB @100Hz -> 20-60 +11.28, 60-120 +6.60,  _tilt_db +3.93\n  the clip      -> 20-60 +18.16,               _tilt_db +7.10\nThe clip's sub-60 Hz lift exceeds a real +14 dB shelf, and so does its `_tilt_db` gap. The owner's \"HEAVY bass boost\" is correct and it is large. `verify()` on that aligned pair returns bass_delta +7.1 and slope_delta +9.0 while core reads 0.0517, so the correct bass number is sitting right there and nothing in the engine will trust it.\n\nHEALTH. /tmp/gdoc/log2.txt brackets this clip with healthgate OK at 26 and at 31. Not throttle-contaminated.",
  "confidence": "high",
  "fix": "One change, in `speed_from_master._win_speeds` (speed_from_master.py:112-137): scale the window stride to the clip so a short clip can still emit `min_win` windows, instead of hard-coding `W=12.0, step=4.0`. Something like: compute `dur = len(xc)/SR` first, and if `floor((dur-6)/step)+1 < 3`, drop to `W=8.0, step=2.0` (and W=6.5/step=1.5 under ~9 s). Measured on this clip: the right reference goes from 2 windows (refused) to 3 tight windows at 0.5751/0.5754/0.5760, `confirm_ref` returns True with min_win untouched, `measure_consensus` returns confident \"slowed ~0.58x\", and server.py:1927 ships it. That is the whole delta between the card the owner graded wrong and a card that says super slowed.\n\nPrefer this over lowering `confirm_ref`'s `min_win` to 2. Both fix clip 30 identically (I measured min_win=2 -> the same 0.5756 / \"slowed ~0.58x\"), but lowering min_win weakens the same-recording guard on every long clip, where it currently works; changing the stride only touches clips that are presently guaranteed to fail. Regression exposure is the 9 short clips (1, 9, 24, 26, 27, 28, 29, 30, 31), which today get no measurement at all, so there is nothing there to break. The 16 clips at >= 14.2 s keep W=12/step=4 and are bit-identical.\n\nTwo follow-ons, worth their own tickets, NOT part of this fix:\n- Sliding the reference. Four of the five clean masters gave 0 windows because `E.dl_clip` hands `confirm_ref` the first 20 s of a 202.5 s track while the clip's section is at 2:29. This is why the fix lands on 0.58 (via one reference that happens to start near the right section, and is itself 4.65% fast) rather than the true 0.603. Same root as ticket #10, applied to references instead of candidates.\n- `verify._detrend` edge bins (verify.py:145-152): `np.convolve(..., mode=\"same\")` is not edge-normalised, and the residual manufactures a spurious lag-0 peak. Measured against the master at t=120/130, the shipped code returns [(0.605, conf 0.36), (1.002, conf 0.38)] - the FALSE reading outscoring the true one - and trimming 50 edge bins turns it into [(0.6047, 0.59), (0.6051, 0.47)]. Real, and it is what stops the true master from ever clustering. It only pays off once the window-count floor is lifted, so ship it second and re-measure.\n\nDo not touch `_tilt_db`'s bands or the transform_report gates in the same change. Speed and bass share the reference channel; fix the channel, re-measure the bass with a clean reference in hand, then decide.",
  "disagreement": "The three lenses agreed the label is a Shazam grid guess and disagreed on which defect made it one. Each named something real; I resolved it by finding which one is binding rather than counting votes, and by running the others' controls myself.\n\nLENS A said the dominant cause is `verify._detrend`'s unnormalised edge, making `_speed_xcorr` \"blind with HIGH confidence\", always returning 1.0000. Overstated, and I can show it. The payload's own candidate rows carry vspeed 0.6382 / 0.8104 / 0.7226 / 0.6823 / 0.6630 - `_speed_xcorr` measured all of those correctly. And against the master at t=120/130 the shipped code still found 0.605 in the first window; the artifact adds a competing lag-0 peak that wins the SECOND window, it does not blind the estimator. Lens A got a uniform 1.0000 because its control measured the clip against a reference the clip does not match (core 0.22-0.42 there), where lag 0 is the only peak available. The artifact is real and I reproduced its fix (trim 50 bins: [(0.605,0.36),(1.002,0.38)] becomes [(0.6047,0.59),(0.6051,0.47)]), but it is downstream of the window-count floor: with `confirm_ref` unable to accept anything, cleaning up the estimator changes no output. Second-order, not dominant.\n\nLENS B said `edit[\"ref_paths\"]` starved because all 14 downloads are edit-titled, and the official-audio rescue never locks. The first half is real and not decisive: I ran the rescue and it worked - 66 rows, 5 clean masters picked including SOPHIE's own SoundCloud upload, all 5 downloaded. The pool was fine. The second half is where Lens B was closest, but it read the cause off the wrong variable: it attributed the confirm_ref failures to the 20 s head. That is true for 4 of the 5, and false for the one that matters. The nik-rad reference IS the right section, produced two windows agreeing to 0.1% at conf 0.56 and 0.48, and was still refused - on count, not on quality. Lens B's proposed fix (reserve download slots for non-edit titles) would have changed nothing here: every reserved slot would have hit the same `2 < 3`.\n\nLENS C said `_crown_tempo_mismatch` plus the `_SPEED_CLAIM` title veto, and that the `elif _source_v is not None:` branch is unreachable. The reading of that branch is correct but it is the wrong branch. `res[\"speed\"]` has a second, crown-free path at server.py:1927 - a sibling `elif` at indent 12 against `if top:` at 1811 - that ships a measured label with no crown at all. So the gate cost the exact-version row, not the speed number. Lens C also mis-set its ground truth, measuring 0.5745 against \"Melodic Part Extended Edit\", itself an edit; and its \"clip is not the master, core maxes at 0.277\" came from a 111.64 s decode of a 202.5 s track, so it never reached the 120-150 s section where core is 0.417 and the tempo lock is clean.\n\nWHAT SETTLED IT. Every lens's mechanism is downstream of one variable: `measured`. The payload's missing `speed_confirmed` key proves `measured` was None (server.py:1740 sets it outside all crown logic). `measured` can only be non-None if `confirm_ref` returns True at least once, and `confirm_ref` cannot return True on a 10.799 s clip. The corpus then confirms it without any argument from me: of 33 payloads, all 9 under 14.0 s have no measured speed and all 16 with a measured speed are >= 14.2 s. Perfect separation at the exact boundary. Lenses A, B and C were each describing a real defect sitting behind a door that was nailed shut.\n\nHonest residual: the true speed is 0.603x and the min-stride fix ships 0.58x, because the one usable reference is itself 4.65% fast and the other four are the wrong 20 seconds. Right verdict, ~4% off on the number. Closing that gap needs the reference-sliding follow-on, not this fix."
 }
]